---
phase: 01-schema-scaffold-settings-permissions
plan: 08
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03", "01-04", "01-05", "01-06", "01-07"]
files_modified: []
autonomous: true
requirements: []
must_haves:
  truths:
    - "`make pint-test` exits 0 (PSR-12 compliance)"
    - "`make analyse` exits 0 (PHPStan level 10, zero new errors)"
    - "`make phpmd` exits 0 (zero violations on classes/components/models/Plugin.php)"
    - "`make test` exits 0 (full Pest test suite green, including all Plan 07 tests)"
    - "`make all` exits 0 — the composite gate the engineering quality bar requires"
    - "`updates/version.yaml` 1.0.1 block lists 4 migrations (verified by Plan 01)"
    - "`phpstan-baseline.neon` is unchanged from pre-Phase-1 state — NO new entries added (LOCKED engineering quality bar: new code adds nothing to baseline)"
  artifacts: []
  key_links:
    - from: "Makefile target `all`"
      to: "the 4 sub-targets pint-test, analyse, phpmd, test"
      via: "executes all 4 sequentially; failure of any halts the chain"
      pattern: "all: pint-test analyse phpmd test"
---

<objective>
Run the full QA gate (`make all`) and verify it exits 0. This is the SINGLE final gate for Phase 1 — it ensures the cumulative output of Plans 01-07 meets the LOCKED engineering quality bar (PSR-12 / PHPStan level 10 / PHPMD clean / Pest green) before the orchestrator commits.

Purpose: The engineering quality bar (PROJECT.md / CLAUDE.md) is a HARD gate. PHPStan level 10, PHPMD clean, Pint pass, Pest green — all four must hold. Running them in sequence catches integration issues across plans (e.g., a model's `@property` block referencing a column the migration forgot to create; a Settings constant misspelled in fields.yaml; a permission lang key typo). This plan does NOT modify code — it RUNS the gate. If any step fails, that's a regression that must be fixed BEFORE this plan completes (likely by re-opening the offending earlier plan).

Output: NO file modifications. Pure verification. The `done` state is "all 4 commands exited 0".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-schema-scaffold-settings-permissions/01-CONTEXT.md
@CLAUDE.md
@Makefile
@phpstan.neon
@phpstan-baseline.neon

<makefile_targets>
The plugin's `Makefile` defines (verified):
- `test`: runs `vendor/bin/pest --configuration plugins/.../phpunit.xml`
- `analyse`: runs `vendor/bin/phpstan analyse --configuration=plugins/.../phpstan.neon`
- `phpmd`: runs `vendor/bin/phpmd plugins/.../classes,plugins/.../components,plugins/.../models,plugins/.../Plugin.php text plugins/.../phpmd.xml`
- `pint-test`: runs `vendor/bin/pint plugins/... --config=plugins/.../pint.json --test`
- `all: pint-test analyse phpmd test` — composite, halts on first failure

All targets `cd $PROJECT_ROOT` first (so paths resolve from project root). Run from the plugin directory.
</makefile_targets>

<phpstan_baseline_contract>
**LOCKED engineering quality bar** (PROJECT.md "Static Analysis (HARD GATE)"):
> PHPStan level 10 (with Larastan) — zero new errors. **Baseline file is for legacy only; new code adds nothing.**

The baseline file `phpstan-baseline.neon` currently has 33 bytes — likely just a stub or a single comment. Phase 1 is the FIRST phase to introduce production PHP files (migrations, models, Settings, permissions). If PHPStan level 10 reports any errors that are then "fixed" by adding to the baseline, that violates the locked rule.

This plan ENFORCES the rule: the gate `make analyse` must exit 0 with NO baseline regeneration. If errors appear, they MUST be fixed in the originating plan (re-open Plan 01-NN), NOT silenced via baseline.

**Baseline regeneration is NOT permitted in this plan.** Do not run `make baseline` under any circumstance.
</phpstan_baseline_contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run pint-test (PSR-12 + ordered imports)</name>
  <files></files>
  <read_first>
    1. Read `Makefile` line 28-29 (pint-test target) — confirms command form
    2. Read `pint.json` (in @ context) — confirms preset psr12 + ordered_imports rule
  </read_first>
  <action>
Run `make pint-test` from the plugin directory.

```bash
make pint-test
```

Pint compares every file under the plugin (excluding paths configured in pint.json — note: pint.json's `preset: psr12` covers the whole plugin tree including `lang/`, `tests/`, `models/`, `classes/`, `Plugin.php`, etc.).

Expected output: `0 changes` or `XX files` with all green checkmarks. Exit code 0.

If pint-test fails (exit code non-zero):
- The diff shows which file(s) need formatting fixes
- Run `make pint` to auto-fix (BUT this is a code change — should have been caught in the originating plan)
- Re-run `make pint-test`
- If failure persists, the offending plan (01-NN) must be re-opened to fix the source code before this plan can proceed.

DO NOT auto-fix during this plan if you can avoid it — pint-test failure here means an earlier plan produced non-PSR-12 code, which is a defect of THAT plan's acceptance criteria. Document the failure in the SUMMARY and let the orchestrator decide.

For the first run, expect success — every preceding plan's acceptance criteria (Plans 01-02, 01-05, 01-06) included pint clean checks. If any of them missed it, this is the catch-net.
  </action>
  <acceptance_criteria>
    1. `make pint-test` exits 0
    2. Output contains the string "PASS" or "0 issues" (Pint's success markers) — implementation: capture exit code; non-zero is the only failure signal we strictly need
  </acceptance_criteria>
  <verify>
    <automated>make pint-test</automated>
  </verify>
  <done>pint-test exit 0 — entire plugin tree is PSR-12 + ordered-imports clean.</done>
</task>

<task type="auto">
  <name>Task 2: Run phpstan level 10 analysis</name>
  <files></files>
  <read_first>
    1. Read `phpstan.neon` (in @ context) — confirms level 10, includes baseline, paths = classes/components/models/Plugin.php, excludes tests/updates/lang/assets
    2. Read `phpstan-baseline.neon` to confirm current state — should be empty / stub (33 bytes)
  </read_first>
  <action>
Run `make analyse` from the plugin directory.

```bash
make analyse
```

PHPStan analyses the paths configured in `phpstan.neon`:
- `classes` — empty in Phase 1 (services arrive in Phase 2/3)
- `components` — empty in Phase 1
- `models` — Invoice, InvoiceLine, InitialResetSnapshot, Settings (4 files)
- `Plugin.php` — 1 file

So PHPStan analyses 5 PHP files at level 10. Each file has full `@property` PHPDoc, strict_types, explicit return types — they were authored to PHPStan level 10's expectations.

**Expected:** `[OK] No errors`. Exit 0.

**If PHPStan reports errors:**
- DO NOT run `make baseline` (that would silence errors permanently — violates the LOCKED engineering quality bar).
- Capture the error output to STDOUT.
- Identify which file/line is failing.
- Re-open the originating plan (Plan 01-02 for models, Plan 01-05 for Settings, Plan 01-06 for Plugin.php) and fix the type issue properly.
- Re-run `make analyse`.
- Document the error and fix in the SUMMARY.

**Common Phase-1 failure modes to be alert for:**
- Missing `@property` line for a column → fixable in Plan 01-02 model docblocks
- `Multisite` trait inducing dynamic-property warnings on Settings → may need `@mixin` augmentation
- Plugin.php's `registerSettings()` return type narrowness → may need `array<string, array<string, mixed>>` (already correct in plan)
  </action>
  <acceptance_criteria>
    1. `make analyse` exits 0
    2. Output contains "[OK] No errors" or equivalent (PHPStan success marker)
    3. **CRITICAL:** `phpstan-baseline.neon` is byte-identical to its pre-Phase-1 state (no new entries added).
       Verification: `git diff phpstan-baseline.neon` should be EMPTY (no changes since the scaffold commit).
       If diff is non-empty, the baseline was regenerated — this is a violation of the locked engineering rule. The plan FAILS until the diff is reverted and the underlying code is fixed.
  </acceptance_criteria>
  <verify>
    <automated>make analyse && [ -z "$(git diff phpstan-baseline.neon)" ]</automated>
  </verify>
  <done>PHPStan level 10 reports 0 errors AND baseline file is unmodified relative to scaffold commit.</done>
</task>

<task type="auto">
  <name>Task 3: Run phpmd (Lovata thresholds)</name>
  <files></files>
  <read_first>
    1. Read `phpmd.xml` (first 30 lines in @ context) — confirms cyclomatic complexity threshold 10, NPath 200, method length 100, class length 1000
    2. Confirm `phpmd` target in Makefile runs against `classes,components,models,Plugin.php`
  </read_first>
  <action>
Run `make phpmd` from the plugin directory.

```bash
make phpmd
```

PHPMD scans 5 files at most (4 models + Plugin.php) using Lovata thresholds. Phase 1 deliberately produces simple data classes — no methods exceed 5-10 lines, no nesting, no cyclomatic complexity above ~3.

**Expected:** zero violations. Exit 0.

**If PHPMD reports violations:**
- The text output identifies file:line and the rule violated.
- Common Phase-1 violations to watch for:
  - `UnusedPrivateMethod` if any helper accidentally added but never called → remove from originating plan
  - `LongVariable` (Lovata threshold ~30 chars) — Hungarian notation tends to be short, unlikely to trip
  - `BooleanArgumentFlag` — none of our methods take boolean flags in Phase 1
- Fix at source by re-opening the originating plan; do NOT silence in `phpmd.xml` thresholds.
  </action>
  <acceptance_criteria>
    1. `make phpmd` exits 0
    2. Output is empty (PHPMD prints nothing on success in `text` format) OR explicitly reports "0 violations"
  </acceptance_criteria>
  <verify>
    <automated>make phpmd</automated>
  </verify>
  <done>PHPMD reports 0 violations on all 5 production PHP files.</done>
</task>

<task type="auto">
  <name>Task 4: Run full Pest test suite (the green gate)</name>
  <files></files>
  <read_first>
    1. Read `phpunit.xml` (in @ context) — confirms tests in `./tests/unit`, SQLite in-memory, APP_ENV=testing
    2. Confirm Plan 01-07 added 3 test files (12 tests total)
  </read_first>
  <action>
Run `make test` from the plugin directory.

```bash
make test
```

This invokes Pest 4 / PHPUnit 12 against the `tests/unit/` directory using SQLite in-memory (configured in `phpunit.xml`).

**Expected output:**
- 12 passing tests (5 from SettingsIsMultisiteAwareTest + 3 from MultisiteContextSwitchClearsCacheTest + 4 from TearDownFlushesSingletonsTest)
- 0 failures
- 0 errors
- Exit 0

**If any test fails:**
- Capture the failure output verbatim
- Identify the failing test file/line
- Determine root cause:
  - **Test code defect** → fix in Plan 01-07 (re-open it)
  - **Production code defect uncovered by test** → fix in the originating plan (01-02 / 01-05 / 01-06)
  - **Environmental issue** (e.g., SQLite missing, October bootstrap failing) → debug from `tests/GoodsReceivedTestCase::resolveBootstrapPath()` outwards
- Re-run; the plan does not complete until 0 failures.

**If `make test` itself errors out (couldn't even start):**
- Most common cause: PHPUnit autoloader doesn't know about the test base. The `phpunit.xml` `bootstrap="../../../modules/system/tests/bootstrap.php"` line is the system-wide entrypoint. Confirm the file exists at that path.
- If the bootstrap path itself is broken, that's a deeper issue and may indicate the plugin is being run in a non-standard layout — defer to the orchestrator.
  </action>
  <acceptance_criteria>
    1. `make test` exits 0
    2. Output reports `Tests: XX passed` with XX >= 12 (the 12 new Plan-01-07 tests). If any pre-existing tests are also discovered, the count may be higher — that's fine, all must pass.
    3. **No failures, no errors, no warnings:** output line `Tests: ... failed, 0 errors` should report 0 failed AND 0 errors. (Warnings are acceptable if PHPUnit reports them as "risky" or "skipped" — but those should not exist for our tests.)
  </acceptance_criteria>
  <verify>
    <automated>make test</automated>
  </verify>
  <done>Full Pest suite green, all 12+ tests passing.</done>
</task>

<task type="auto">
  <name>Task 5: Run composite `make all` as final smoke</name>
  <files></files>
  <read_first>
    1. Read `Makefile` line 31 — `all: pint-test analyse phpmd test`
    2. Confirm tasks 1-4 of THIS plan all completed successfully
  </read_first>
  <action>
Run `make all` from the plugin directory.

```bash
make all
```

This runs ALL FOUR sub-targets in sequence: pint-test → analyse → phpmd → test. If ALL pass, exit 0. If any fails, the chain halts and exits non-zero at that step.

**Why run this AFTER Tasks 1-4 individually:** Tasks 1-4 ran each sub-gate in isolation, with clean context for failure diagnosis. `make all` is the canonical INTEGRATION gate that the engineering quality bar references. Running it last gives a single command exit code we can use as the final Phase-1 quality signal.

If Tasks 1-4 all passed, this task is essentially a smoke confirmation. It MUST also pass; if it doesn't, that's a clue that one of the sub-gates is non-deterministic (depends on filesystem state mutated by previous runs) — flag in SUMMARY and investigate.
  </action>
  <acceptance_criteria>
    1. `make all` exits 0
    2. Output shows all 4 sub-targets ran: search the output for the start of each (`pint-test`, `analyse`, `phpmd`, `test` markers — varies by tool but typically prints the command name)
    3. **Final consistency check:** `git diff phpstan-baseline.neon` returns empty (baseline still untouched after the full gate run)
  </acceptance_criteria>
  <verify>
    <automated>make all</automated>
  </verify>
  <done>Composite gate green; Phase 1 implementation meets the locked engineering quality bar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QA gate -> commit | The orchestrator commits all Phase-1 plans after this verification plan exits 0. The gate is the last barrier preventing defective code from landing on the master branch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-32 | Tampering | `phpstan-baseline.neon` regeneration | mitigate | Task 2 acceptance criterion #3 explicitly verifies `git diff phpstan-baseline.neon` is empty. Any baseline regeneration would shift the verifiable contract about new code from "level 10 clean" to "level 10 + waivers" — that's a silent quality erosion attack vector. Failing this check halts Phase 1 commit. |
| T-01-33 | Repudiation | "It worked on my machine" | mitigate | All 5 tasks run via the project's own `Makefile` from the plugin directory, using `vendor/bin/*` from the project root. Same toolchain everyone uses. The `make` commands are deterministic. |
| T-01-34 | Denial of Service | unbounded test suite runtime | accept | Phase 1 has 12 tests, all introspection or single-method-call tests. SQLite in-memory. Expected runtime: <2 seconds. No DoS surface. |
| T-01-35 | Elevation of Privilege | `make` running with broader filesystem access than necessary | accept | `make` is a developer tool run on the developer's machine. Not relevant to deployed runtime security. |
</threat_model>

<verification>
End-of-plan checks (this plan IS the verification — no separate "verification" beyond the 5 tasks):

1. **All 5 tasks completed with exit 0** — captured during execution
2. **No file modifications outside acceptable scope:**
   - `git status` should show NO changes to `phpstan-baseline.neon`
   - `git status` should show NO changes to source code (this plan is verify-only; if Pint or Phpstan re-ran a fixer accidentally, that's a defect)
3. **All previous plans' artifacts present and accounted for:**
   - 4 migration files in `updates/`
   - 4 model files in `models/` (Invoice, InvoiceLine, InitialResetSnapshot, Settings)
   - 1 fields.yaml in `models/settings/`
   - 4 lang files in `lang/{en,lv,no,ru}/`
   - 3 fixture HTM files + .gitkeep in `tests/fixtures/invoices/`
   - 1 extended Plugin.php
   - 1 extended GoodsReceivedTestCase.php
   - 3 new test files (12 tests total)
   - `version.yaml` updated with 1.0.1 block

Run `git status --short` to inspect the working-tree state. The orchestrator should see ONLY the files listed above as `??` (untracked) or `M` (modified) — no surprise files.
</verification>

<success_criteria>
Plan complete when:
- [ ] `make pint-test` exits 0
- [ ] `make analyse` exits 0 AND `phpstan-baseline.neon` is unchanged from scaffold commit (no new entries)
- [ ] `make phpmd` exits 0
- [ ] `make test` exits 0 with >= 12 tests passing
- [ ] `make all` (composite) exits 0
- [ ] No source files modified by this plan (verify via `git status`)
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-scaffold-settings-permissions/01-08-SUMMARY.md` documenting:
- Exit codes for all 5 commands (should be all 0)
- Test count (should be >= 12)
- Confirmation that `phpstan-baseline.neon` is unmodified
- `git status --short` output showing the full set of Phase-1 artifacts ready for commit
- ROADMAP.md success criteria (5 items) review:
  - [ ] criterion 1: 3 plugin tables + offers.active_managed_by — verified by Plan 01-01 acceptance + Plan 01-02 model existence
  - [ ] criterion 2: 4 toggles persist per-site — verified by Plan 01-05 + Plan 01-07 multisite tests
  - [ ] criterion 3: 4 split permissions — verified by Plan 01-06
  - [ ] criterion 4: tearDown flushes singletons — verified by Plan 01-07 TearDownFlushesSingletonsTest
  - [ ] criterion 5: EAN string(13) + invoice_number UNIQUE — verified by Plan 01-01 acceptance criteria #4 (EAN) and #5 (UNIQUE)
- Note for orchestrator: Phase 1 is ready for commit.
</output>
