---
phase: 01-schema-scaffold-settings-permissions
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/invoices/Nr_PRO026712_no_28112024.HTM
  - tests/fixtures/invoices/Nr_PRO029691_no_09072025.HTM
  - tests/fixtures/invoices/Nr_PRO033328_no_13042026.HTM
  - tests/fixtures/invoices/.gitkeep
autonomous: true
requirements: []
must_haves:
  truths:
    - "Three real distributor HTM files copied from `<project_root>/storage/app/uploads/invoices/` to `tests/fixtures/invoices/`"
    - "The 3 files span the distributor's 18-month timeline (earliest 2024-11-28, mid 2025-07-09, latest 2026-04-13) so any format drift is captured"
    - "Phase 2 parser tests will read EXCLUSIVELY from `tests/fixtures/invoices/` (hermetic) — they never touch `<project_root>/storage/app/uploads/invoices/`"
    - "Each fixture file is byte-identical to its production source (verified by md5sum)"
    - "The directory contains a `.gitkeep` so the fixture set ships with the plugin (not gitignored)"
  artifacts:
    - path: "tests/fixtures/invoices/Nr_PRO026712_no_28112024.HTM"
      provides: "Earliest fixture (2024-11-28) — captures original distributor format"
    - path: "tests/fixtures/invoices/Nr_PRO029691_no_09072025.HTM"
      provides: "Mid-2025 fixture — captures any mid-period format drift"
    - path: "tests/fixtures/invoices/Nr_PRO033328_no_13042026.HTM"
      provides: "Latest fixture (2026-04-13) — captures current distributor format"
    - path: "tests/fixtures/invoices/.gitkeep"
      provides: "Empty placeholder ensuring directory ships in git even if fixtures are gitignored"
  key_links:
    - from: "tests/fixtures/invoices/*.HTM"
      to: "Phase 2 parser tests (HtmInvoiceParser)"
      via: "hermetic test fixture pin"
      pattern: "tests/fixtures/invoices/Nr_PRO"
---

<objective>
Copy 3 representative real distributor `.HTM` delivery receipts from the production uploads directory to `tests/fixtures/invoices/`, so that Phase 2 parser tests are hermetic (never read outside `tests/`). Selection spans the distributor's 18-month timeline to capture any format drift.

Purpose: The plugin CLAUDE.md's testing convention demands hermetic fixtures: "HTM parser tests: pin against `<project_root>/storage/app/uploads/invoices/Nr_PRO*.HTM` fixtures (copy to `tests/fixtures/` to keep tests hermetic)". This plan PERFORMS the copy; Phase 2 USES the copies. After this plan, the plugin's test suite has no dependency on the production filesystem.
Output: 3 .HTM files + 1 `.gitkeep`. Total ~150-300 KB of static test data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-schema-scaffold-settings-permissions/01-CONTEXT.md
@CLAUDE.md

<source_path>
**Production source:** `/home/forge/nailscosmetics.lv/storage/app/uploads/invoices/`

Verified contents at scaffold time (15 .HTM files):
```
Nr_PRO026712_no_28112024.HTM   <-- COPY (earliest)
Nr_PRO027556_no_03022025.HTM
Nr_PRO028204_no_26032025.HTM
Nr_PRO028925_no_19052025.HTM
Nr_PRO029691_no_09072025.HTM   <-- COPY (mid-2025)
Nr_PRO030945_no_08102025.HTM
Nr_PRO031351_no_06112025.HTM
Nr_PRO032103_no_16122025.HTM
Nr_PRO032283_no_09012026.HTM
Nr_PRO032312_no_12012026.HTM
Nr_PRO032427_no_20012026.HTM
Nr_PRO032634_no_09022026.HTM
Nr_PRO032807_no_23022026.HTM
Nr_PRO033162_no_23032026.HTM
Nr_PRO033328_no_13042026.HTM   <-- COPY (latest)
```

Selection rationale (per CONTEXT.md D-20):
- 2024-11-28 (PRO026712) — earliest available, baseline
- 2025-07-09 (PRO029691) — mid-period, captures any subtle drift
- 2026-04-13 (PRO033328) — latest, current format

This 3-file spread maximizes format-drift coverage while minimizing fixture bytes shipped with the plugin.
</source_path>

<destination_path>
**Test fixture target:** `tests/fixtures/invoices/`

Currently does NOT exist. Plan creates the directory and populates it. Phase 2 parser tests will reference fixtures by relative path from the test file:

```php
// Phase 2 example (informational; not implemented in this plan):
$sFixture = __DIR__ . '/../../fixtures/invoices/Nr_PRO026712_no_28112024.HTM';
```
</destination_path>

<hermetic_test_constraint>
Per CONTEXT.md D-21 + plugin CLAUDE.md: tests are hermetic. Phase 2 parser tests MUST NEVER read from `<project_root>/storage/app/uploads/invoices/`. The production directory is for live operator uploads only, not test fixtures. This plan ensures the fixture set is fully self-contained inside the plugin.
</hermetic_test_constraint>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/fixtures/invoices/ directory and copy 3 fixtures</name>
  <files>tests/fixtures/invoices/Nr_PRO026712_no_28112024.HTM, tests/fixtures/invoices/Nr_PRO029691_no_09072025.HTM, tests/fixtures/invoices/Nr_PRO033328_no_13042026.HTM, tests/fixtures/invoices/.gitkeep</files>
  <read_first>
    1. Read CONTEXT.md D-20 (file selection: PRO026712, PRO029691, PRO033328) lines 96-100
    2. Read CONTEXT.md D-21 (hermetic constraint) line 101
    3. Read plugin CLAUDE.md "tests/fixtures/" line (HTM parser tests pin) lines 76-79
    4. Verify source files exist:
       - `test -f /home/forge/nailscosmetics.lv/storage/app/uploads/invoices/Nr_PRO026712_no_28112024.HTM`
       - `test -f /home/forge/nailscosmetics.lv/storage/app/uploads/invoices/Nr_PRO029691_no_09072025.HTM`
       - `test -f /home/forge/nailscosmetics.lv/storage/app/uploads/invoices/Nr_PRO033328_no_13042026.HTM`
    5. Verify destination directory does NOT yet exist: `test ! -d tests/fixtures/invoices`
  </read_first>
  <action>
Use the Bash tool to perform the directory creation + copy + placeholder:

```bash
mkdir -p tests/fixtures/invoices
cp /home/forge/nailscosmetics.lv/storage/app/uploads/invoices/Nr_PRO026712_no_28112024.HTM tests/fixtures/invoices/
cp /home/forge/nailscosmetics.lv/storage/app/uploads/invoices/Nr_PRO029691_no_09072025.HTM tests/fixtures/invoices/
cp /home/forge/nailscosmetics.lv/storage/app/uploads/invoices/Nr_PRO033328_no_13042026.HTM tests/fixtures/invoices/
touch tests/fixtures/invoices/.gitkeep
```

Implementation notes:
- `mkdir -p` is safe — succeeds whether or not the directory exists.
- Plain `cp` (no `-p`, no `--preserve`) — we don't need to preserve mtime/permissions; the file content is what matters for parser tests.
- `.gitkeep` is the conventional empty placeholder ensuring `tests/fixtures/invoices/` ships in git even if some fixture pattern is gitignored. The file is empty (zero bytes).
- DO NOT modify the .HTM file contents. They must be byte-identical to production. Phase 2 parser tests will pin against EXACT bytes (BOM, CRLF, unquoted attributes, decimal-comma — all real-world quirks).
- Filenames preserve the production pattern `Nr_PRO<num>_<country>_<DDMMYYYY>.HTM` because Phase 2's `InvoiceNumberResolver` uses this pattern as a fallback (PARSE-04 in REQUIREMENTS.md).

After copies complete, verify identity by comparing md5sums (acceptance criteria below).
  </action>
  <acceptance_criteria>
    1. `test -d tests/fixtures/invoices` exits 0
    2. `test -f tests/fixtures/invoices/Nr_PRO026712_no_28112024.HTM` exits 0
    3. `test -f tests/fixtures/invoices/Nr_PRO029691_no_09072025.HTM` exits 0
    4. `test -f tests/fixtures/invoices/Nr_PRO033328_no_13042026.HTM` exits 0
    5. `test -f tests/fixtures/invoices/.gitkeep` exits 0
    6. **Byte-identity to production source** (3 separate checks):
       - `cmp /home/forge/nailscosmetics.lv/storage/app/uploads/invoices/Nr_PRO026712_no_28112024.HTM tests/fixtures/invoices/Nr_PRO026712_no_28112024.HTM` exits 0 (silent = identical)
       - `cmp /home/forge/nailscosmetics.lv/storage/app/uploads/invoices/Nr_PRO029691_no_09072025.HTM tests/fixtures/invoices/Nr_PRO029691_no_09072025.HTM` exits 0
       - `cmp /home/forge/nailscosmetics.lv/storage/app/uploads/invoices/Nr_PRO033328_no_13042026.HTM tests/fixtures/invoices/Nr_PRO033328_no_13042026.HTM` exits 0
    7. **Sane file sizes (HTM files non-empty):** `[ "$(wc -c < tests/fixtures/invoices/Nr_PRO026712_no_28112024.HTM)" -gt 1000 ]` exits 0 (each fixture > 1KB)
    8. **`.gitkeep` is empty:** `[ "$(wc -c < tests/fixtures/invoices/.gitkeep)" -eq 0 ]` exits 0
    9. **Exactly 3 .HTM files** in the destination (no extras accidentally copied): `ls tests/fixtures/invoices/*.HTM | wc -l` returns 3
   10. **Filename format check:** `ls tests/fixtures/invoices/Nr_PRO*.HTM | grep -cE 'Nr_PRO[0-9]+_[a-z]{2}_[0-9]{8}\\.HTM$'` returns 3 (all 3 match the production filename pattern)
  </acceptance_criteria>
  <verify>
    <automated>cmp /home/forge/nailscosmetics.lv/storage/app/uploads/invoices/Nr_PRO026712_no_28112024.HTM tests/fixtures/invoices/Nr_PRO026712_no_28112024.HTM && cmp /home/forge/nailscosmetics.lv/storage/app/uploads/invoices/Nr_PRO029691_no_09072025.HTM tests/fixtures/invoices/Nr_PRO029691_no_09072025.HTM && cmp /home/forge/nailscosmetics.lv/storage/app/uploads/invoices/Nr_PRO033328_no_13042026.HTM tests/fixtures/invoices/Nr_PRO033328_no_13042026.HTM</automated>
  </verify>
  <done>3 fixture HTM files + .gitkeep present, byte-identical to production sources, located at `tests/fixtures/invoices/`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| production uploads dir -> test fixture dir | One-time copy of real distributor data into committed test fixtures. The data is non-sensitive (product names, EAN, delivery quantities — all distributor-side data, no PII, no internal identifiers beyond product codes already public on the storefront). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-15 | Information Disclosure | committed fixtures | accept | The 3 fixtures contain product names + EANs + quantities + (audit-only) prices. None of these are PII or trade secrets — product codes are visible on the public storefront, distributor pricing is operator-known and not consumer-facing in the catalog. The plugin repo is PUBLIC per D11, so committing these fixtures is intentional and accepted. |
| T-01-16 | Tampering | fixture files | mitigate | Acceptance criterion #6 verifies byte-identity to production source via `cmp`. Any post-copy tampering would break the parser tests' pinned expectations. |
| T-01-17 | Repudiation | fixture provenance | accept | Files are committed via git with full author/timestamp metadata. Source path documented in this plan and CONTEXT.md D-20 makes provenance traceable. |
</threat_model>

<verification>
End-of-plan checks:

1. **Directory + 4 files present:** `ls -la tests/fixtures/invoices/`
2. **All 3 HTM files byte-identical to source:** `for f in Nr_PRO026712_no_28112024.HTM Nr_PRO029691_no_09072025.HTM Nr_PRO033328_no_13042026.HTM; do cmp "/home/forge/nailscosmetics.lv/storage/app/uploads/invoices/$f" "tests/fixtures/invoices/$f" || exit 1; done`
3. **Hermetic constraint verifiable:** Phase 2 will add a grep-gate `! grep -r 'storage/app/uploads/invoices' tests/` to its test plan. This plan creates the conditions for that gate to pass.
</verification>

<success_criteria>
Plan complete when:
- [ ] Directory `tests/fixtures/invoices/` exists
- [ ] 3 .HTM files present, each byte-identical to its production source
- [ ] `.gitkeep` placeholder file present (zero bytes)
- [ ] Filenames preserve production pattern (`Nr_PRO<num>_<country>_<DDMMYYYY>.HTM`) — required for `InvoiceNumberResolver` filename-fallback test (Phase 2 PARSE-04)
- [ ] No other files accidentally copied into the directory (acceptance #9 enforces "exactly 3")
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-scaffold-settings-permissions/01-04-SUMMARY.md` documenting:
- 3 file paths copied + their byte sizes
- Confirmation of byte-identity (cmp results)
- Phase 2 hermetic-test guarantee: parser tests can now reference these without filesystem reach-out
</output>
