---
phase: 06-pass-3-variation-ean-matcher
plan: 03
type: execute
wave: 2
depends_on:
  - 02
files_modified:
  - classes/match/OfferCodeMatcher.php
  - classes/match/ProductCodeSingleOfferMatcher.php
autonomous: true
requirements:
  - MATCH-06
  - MATCH-07
must_haves:
  truths:
    - "OfferCodeMatcher implements MatchStrategy; issues exactly 1 query: Offer::whereIn('code', $arUnique)->get(['id', 'code'])"
    - "OfferCodeMatcher emits MatchedLine with match_strategy literal === 'offer_code'"
    - "ProductCodeSingleOfferMatcher implements MatchStrategy; issues exactly 1 query: Product::whereIn('code', $arEans)->has('offer', '=', 1)->select(['id', 'code'])->addSelect(['matched_offer_id' => Offer correlated subquery])->get()"
    - "ProductCodeSingleOfferMatcher emits MatchedLine with match_strategy literal === 'product_code_single_offer'"
    - "Both classes are final; both use #[\\Override] on match()"
    - "Existing EanMatcherService::matchBatch behavior is UNCHANGED at this point — extraction is COPY (new files), not move; refactor to chain runner happens in Plan 05"
    - "phpstan-baseline.neon SHA UNCHANGED at 4b3227fab5b697264e8532b59f5cdd96c86a0ff1fa484cc1a869af36ae91530a"
  artifacts:
    - path: "classes/match/OfferCodeMatcher.php"
      provides: "Pass 1 chain stage — Offer.code direct match (1 query)"
      contains: "final class OfferCodeMatcher implements MatchStrategy"
    - path: "classes/match/ProductCodeSingleOfferMatcher.php"
      provides: "Pass 2 chain stage — Product.code with single-offer guard (1 query)"
      contains: "final class ProductCodeSingleOfferMatcher implements MatchStrategy"
  key_links:
    - from: "classes/match/OfferCodeMatcher.php"
      to: "classes/match/MatchStrategy.php (Plan 02)"
      via: "implements MatchStrategy + #[\\Override] on match()"
      pattern: "implements MatchStrategy"
    - from: "classes/match/ProductCodeSingleOfferMatcher.php"
      to: "classes/match/MatchStrategy.php (Plan 02) + Lovata\\Shopaholic\\Models\\Product correlated subquery"
      via: "implements MatchStrategy + addSelect Offer correlated subquery"
      pattern: "->has\\('offer', '=', 1\\)"
---

<objective>
Extract Pass 1 (`OfferCodeMatcher`) and Pass 2 (`ProductCodeSingleOfferMatcher`) from the existing `EanMatcherService` into standalone `MatchStrategy` implementations. This isolates the per-stage logic so Plan 05 can refactor `EanMatcherService` into a pure chain-runner shell.

Purpose: MATCH-06 + MATCH-07. The extraction preserves existing two-pass behavior byte-for-byte at this stage — `EanMatcherService::matchBatch()` keeps working unchanged until Plan 05 swaps it out. New classes are COPY of the existing private methods, NOT a move.

Output: 2 new `final` classes in `classes/match/`, each implementing `MatchStrategy` with exactly 1 query.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-pass-3-variation-ean-matcher/06-CONTEXT.md
@CLAUDE.md
@classes/match/EanMatcherService.php
@classes/match/MatchStrategy.php
@classes/dto/ParsedLine.php
@classes/dto/MatchedLine.php
</context>

<interfaces>
<!-- The two private methods to extract VERBATIM from existing EanMatcherService -->

From classes/match/EanMatcherService.php (Pass 1, lines 151-167):
```php
private function lookupOffersByCode(array $arEans): array {
    $arOfferMap = [];
    $obRows = Offer::whereIn('code', $arEans)->get(['id', 'code']);
    foreach ($obRows as $obRow) {
        $sCode = (string) $obRow->code;
        $iOfferId = (int) $obRow->id;
        $arOfferMap[$sCode] = $iOfferId;
    }
    return $arOfferMap;
}
```

From classes/match/EanMatcherService.php (Pass 2, lines 188-225):
```php
private function lookupProductsWithSingleOffer(array $arEans): array {
    if ($arEans === []) { return []; }
    $arProductMap = [];
    $obRows = Product::whereIn('code', $arEans)
        ->has('offer', '=', 1)
        ->select(['id', 'code'])
        ->addSelect([
            'matched_offer_id' => Offer::select('id')
                ->whereColumn('product_id', 'lovata_shopaholic_products.id')
                ->limit(1),
        ])
        ->get();
    foreach ($obRows as $obRow) {
        $mOfferId = $obRow->matched_offer_id;
        if (! is_numeric($mOfferId)) { continue; }
        $sCode = (string) $obRow->code;
        $arProductMap[$sCode] = (int) $mOfferId;
    }
    return $arProductMap;
}
```

PHPStan-ignore directives for the Lovata model property-not-found warnings appear inline in the source — preserve them verbatim when copying.

The `MatchStrategy::match(array $arUnmatched): array` signature requires a `list<ParsedLine> → list<MatchedLine>` shape. Each new matcher must:
1. Extract EAN strings from each ParsedLine (`array_map(static fn($obLine) => $obLine->ean, $arUnmatched)`).
2. Run the single SELECT against the EAN list (deduplicated via `array_unique`).
3. For each ParsedLine whose EAN is in the result, append a MatchedLine with the appropriate strategy literal.
4. ParsedLines with no match are simply OMITTED from the return list (the chain runner forwards them to the next stage).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OfferCodeMatcher (Pass 1, MatchStrategy implementation)</name>
  <files>classes/match/OfferCodeMatcher.php, tests/unit/Match/OfferCodeMatcherTest.php</files>
  <read_first>
    - classes/match/EanMatcherService.php (lookupOffersByCode private method, lines 151-167 — extraction source)
    - classes/match/MatchStrategy.php (interface contract — read fully)
    - classes/dto/ParsedLine.php (input DTO — read fully)
    - classes/dto/MatchedLine.php (output DTO with widened 'variation' union — read fully)
    - tests/unit/Match/EanMatcherServiceTest.php (lines 44-128 — read setUp() + seedProduct() + seedOffer() helpers; OfferCodeMatcherTest will need the same hermetic schema for offers + products)
  </read_first>
  <behavior>
    Test 1 (single-hit): given 1 ParsedLine with EAN '4752307000097' and a seeded Offer with code='4752307000097' product_id=42, match() returns list with 1 MatchedLine where matched_offer_id === offer's id, match_strategy === 'offer_code', and line === input line.
    Test 2 (miss): given 1 ParsedLine with EAN '9999999999999' and NO matching offer.code seeded, match() returns empty list [] (residue forwarded to next chain stage).
    Test 3 (mixed): given 2 ParsedLines (one match + one miss), match() returns list with EXACTLY 1 MatchedLine (the matched one); the miss is omitted.
    Test 4 (query-count): wrap match() with DB::enableQueryLog(); count(DB::getQueryLog()) === 1 for any non-empty input.
    Test 5 (empty input): match([]) returns [] without firing any queries.
  </behavior>
  <action>
    RED step (commit 1):
    Create `tests/unit/Match/OfferCodeMatcherTest.php`. Reuse the `EanMatcherTestCase` abstract base from the existing `tests/unit/Match/EanMatcherServiceTest.php` (extend by `uses()` if present, or copy the hermetic schema setUp() inline). Five it() cases per <behavior>. Use `Logingrupa\GoodsReceivedShopaholic\Classes\Match\OfferCodeMatcher`. Run `make test` — expect FATAL (class not found). Commit:
      `test(06-03): add failing OfferCodeMatcher chain-stage tests (MATCH-06)`

    GREEN step (commit 2):
    Create `classes/match/OfferCodeMatcher.php` with EXACTLY this shape:

      <?php

      declare(strict_types=1);

      namespace Logingrupa\GoodsReceivedShopaholic\Classes\Match;

      use Logingrupa\GoodsReceivedShopaholic\Classes\Dto\MatchedLine;
      use Logingrupa\GoodsReceivedShopaholic\Classes\Dto\ParsedLine;
      use Lovata\Shopaholic\Models\Offer;

      /**
       * Pass 1 chain stage — direct EAN match against `lovata_shopaholic_offers.code`.
       *
       * Extracted from the legacy EanMatcherService::lookupOffersByCode private
       * method (Phase 2 plan 02-06) into a MatchStrategy implementation as
       * part of Phase 6 / D-25-update (chain runner refactor).
       *
       * Issues EXACTLY ONE query regardless of input size:
       *   `Offer::whereIn('code', $arUnique)->get(['id', 'code'])`
       *
       * Returns MatchedLine instances ONLY for ParsedLines whose EAN was found.
       * Unmatched ParsedLines are omitted — the chain runner forwards them to
       * the next stage (ProductCodeSingleOfferMatcher).
       */
      final class OfferCodeMatcher implements MatchStrategy
      {
          #[\Override]
          public function match(array $arUnmatched): array
          {
              if ($arUnmatched === []) {
                  return [];
              }

              $arEans = array_values(array_unique(array_map(
                  static fn (ParsedLine $obLine): string => $obLine->ean,
                  $arUnmatched,
              )));

              $arOfferMap = $this->lookupOffersByCode($arEans);

              $arResult = [];
              foreach ($arUnmatched as $obLine) {
                  if (! isset($arOfferMap[$obLine->ean])) {
                      continue;
                  }
                  $arResult[] = new MatchedLine(
                      line: $obLine,
                      matched_offer_id: $arOfferMap[$obLine->ean],
                      match_strategy: 'offer_code',
                  );
              }
              return $arResult;
          }

          /**
           * @param  list<string>  $arEans
           * @return array<string, int>
           */
          private function lookupOffersByCode(array $arEans): array
          {
              $arOfferMap = [];
              $obRows = Offer::whereIn('code', $arEans)->get(['id', 'code']);

              foreach ($obRows as $obRow) {
                  /** @phpstan-ignore-next-line property.notFound — Lovata Offer lacks IDE-helper PHPDoc; columns verified at DB layer */
                  $sCode = (string) $obRow->code;
                  /** @phpstan-ignore-next-line property.notFound — Lovata Offer lacks IDE-helper PHPDoc; columns verified at DB layer */
                  $iOfferId = (int) $obRow->id;
                  $arOfferMap[$sCode] = $iOfferId;
              }
              return $arOfferMap;
          }
      }

    Run `make test pint-test analyse phpmd` — all green. Commit:
      `feat(06-03): add OfferCodeMatcher Pass 1 chain stage (MATCH-06)`
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/goodsreceivedshopaholic &amp;&amp; make pint-test analyse phpmd test 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `classes/match/OfferCodeMatcher.php` exists.
    - Grep `grep -c 'final class OfferCodeMatcher implements MatchStrategy' classes/match/OfferCodeMatcher.php` == 1.
    - Grep `grep -c '#\[\\\\Override\]' classes/match/OfferCodeMatcher.php` == 1.
    - Grep -F `grep -F -c "match_strategy: 'offer_code'" classes/match/OfferCodeMatcher.php` == 1 (literal preserved).
    - Grep -F `grep -F -c "Offer::whereIn('code'," classes/match/OfferCodeMatcher.php` == 1 (Pass 1 SELECT preserved).
    - File `tests/unit/Match/OfferCodeMatcherTest.php` exists with 5 it() blocks: `grep -c "^it(" tests/unit/Match/OfferCodeMatcherTest.php` == 5.
    - `make test` exit 0 (suite count grows by exactly 5 vs prior baseline).
    - `make analyse phpmd pint-test` all exit 0.
    - `sha256sum phpstan-baseline.neon` first column == `4b3227fab5b697264e8532b59f5cdd96c86a0ff1fa484cc1a869af36ae91530a`.
    - 2 atomic commits: `test(06-03):` THEN `feat(06-03):`.
  </acceptance_criteria>
  <done>
    OfferCodeMatcher implements MatchStrategy; 1-query Pass 1 behavior preserved; 5 tests green; baseline SHA pinned. Existing EanMatcherService::matchBatch still works unchanged (Plan 05 swaps it out).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ProductCodeSingleOfferMatcher (Pass 2, MatchStrategy implementation)</name>
  <files>classes/match/ProductCodeSingleOfferMatcher.php, tests/unit/Match/ProductCodeSingleOfferMatcherTest.php</files>
  <read_first>
    - classes/match/EanMatcherService.php (lookupProductsWithSingleOffer private method, lines 188-225 — extraction source; preserve `has('offer', '=', 1)` + correlated `addSelect` verbatim)
    - classes/match/OfferCodeMatcher.php (Task 1 sibling — match the structural pattern: ean extraction → 1-query SELECT → MatchedLine emit loop)
    - tests/unit/Match/EanMatcherServiceTest.php (lines 44-128 hermetic schema setUp/seedProduct/seedOffer helpers — reuse pattern for the new test file)
  </read_first>
  <behavior>
    Test 1 (single-offer match): given 1 ParsedLine with EAN '4752307000200', seeded Product code='4752307000200' with EXACTLY 1 attached Offer id=99, match() returns list with 1 MatchedLine where matched_offer_id === 99, match_strategy === 'product_code_single_offer'.
    Test 2 (multi-offer skip): given 1 ParsedLine with EAN '4752307000300', seeded Product code='4752307000300' with TWO attached Offers, match() returns [] (single-offer guard fails — line falls through to next stage / 'none').
    Test 3 (no match): given 1 ParsedLine with EAN '9999999999999' and no matching product, match() returns [].
    Test 4 (query-count): DB::enableQueryLog() shows EXACTLY 1 query for non-empty input (the correlated addSelect subquery is part of the same SELECT, not a second round-trip — D-25 budget).
    Test 5 (empty input): match([]) returns [] with zero queries.
  </behavior>
  <action>
    RED step (commit 1):
    Create `tests/unit/Match/ProductCodeSingleOfferMatcherTest.php`. Reuse `EanMatcherTestCase` hermetic schema (offers + products tables). Five it() cases per <behavior>. Reference `Logingrupa\GoodsReceivedShopaholic\Classes\Match\ProductCodeSingleOfferMatcher`. Run `make test` — RED (class not found). Commit:
      `test(06-03): add failing ProductCodeSingleOfferMatcher tests (MATCH-07)`

    GREEN step (commit 2):
    Create `classes/match/ProductCodeSingleOfferMatcher.php` with EXACTLY this shape:

      <?php

      declare(strict_types=1);

      namespace Logingrupa\GoodsReceivedShopaholic\Classes\Match;

      use Logingrupa\GoodsReceivedShopaholic\Classes\Dto\MatchedLine;
      use Logingrupa\GoodsReceivedShopaholic\Classes\Dto\ParsedLine;
      use Lovata\Shopaholic\Models\Offer;
      use Lovata\Shopaholic\Models\Product;

      /**
       * Pass 2 chain stage — Product.code match with single-offer guard.
       *
       * Extracted from the legacy EanMatcherService::lookupProductsWithSingleOffer
       * private method (Phase 2 plan 02-06) into a MatchStrategy implementation
       * as part of Phase 6 / D-25-update (chain runner refactor).
       *
       * Issues EXACTLY ONE query — `has('offer', '=', 1)` is a correlated COUNT
       * in the WHERE clause; `addSelect(...subquery...)` inlines the sole offer
       * id via a correlated SELECT subquery. Same SQL statement, NOT a second
       * round-trip. `->limit(1)` defense-in-depth keeps the subquery
       * deterministic if the WHERE guard ever drifts.
       */
      final class ProductCodeSingleOfferMatcher implements MatchStrategy
      {
          #[\Override]
          public function match(array $arUnmatched): array
          {
              if ($arUnmatched === []) {
                  return [];
              }

              $arEans = array_values(array_unique(array_map(
                  static fn (ParsedLine $obLine): string => $obLine->ean,
                  $arUnmatched,
              )));

              $arProductMap = $this->lookupProductsWithSingleOffer($arEans);

              $arResult = [];
              foreach ($arUnmatched as $obLine) {
                  if (! isset($arProductMap[$obLine->ean])) {
                      continue;
                  }
                  $arResult[] = new MatchedLine(
                      line: $obLine,
                      matched_offer_id: $arProductMap[$obLine->ean],
                      match_strategy: 'product_code_single_offer',
                  );
              }
              return $arResult;
          }

          /**
           * @param  list<string>  $arEans
           * @return array<string, int>
           */
          private function lookupProductsWithSingleOffer(array $arEans): array
          {
              $arProductMap = [];
              $obRows = Product::whereIn('code', $arEans)
                  ->has('offer', '=', 1)
                  ->select(['id', 'code'])
                  ->addSelect([
                      'matched_offer_id' => Offer::select('id')
                          ->whereColumn('product_id', 'lovata_shopaholic_products.id')
                          ->limit(1),
                  ])
                  ->get();

              foreach ($obRows as $obRow) {
                  /** @phpstan-ignore-next-line property.notFound — Lovata Product lacks IDE-helper PHPDoc; correlated `addSelect` exposes `matched_offer_id` at runtime */
                  $mOfferId = $obRow->matched_offer_id;
                  if (! is_numeric($mOfferId)) {
                      continue;
                  }
                  /** @phpstan-ignore-next-line property.notFound — Lovata Product lacks IDE-helper PHPDoc; columns verified at DB layer */
                  $sCode = (string) $obRow->code;
                  $arProductMap[$sCode] = (int) $mOfferId;
              }
              return $arProductMap;
          }
      }

    Run `make test pint-test analyse phpmd` — all green. Commit:
      `feat(06-03): add ProductCodeSingleOfferMatcher Pass 2 chain stage (MATCH-07)`
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/goodsreceivedshopaholic &amp;&amp; make pint-test analyse phpmd test 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `classes/match/ProductCodeSingleOfferMatcher.php` exists.
    - Grep `grep -c 'final class ProductCodeSingleOfferMatcher implements MatchStrategy' classes/match/ProductCodeSingleOfferMatcher.php` == 1.
    - Grep `grep -c '#\[\\\\Override\]' classes/match/ProductCodeSingleOfferMatcher.php` == 1.
    - Grep -F `grep -F -c "match_strategy: 'product_code_single_offer'" classes/match/ProductCodeSingleOfferMatcher.php` == 1.
    - Grep -F `grep -F -c "->has('offer', '=', 1)" classes/match/ProductCodeSingleOfferMatcher.php` == 1 (single-offer guard preserved).
    - Grep -F `grep -F -c "'matched_offer_id' => Offer::select('id')" classes/match/ProductCodeSingleOfferMatcher.php` == 1 (correlated subquery preserved).
    - File `tests/unit/Match/ProductCodeSingleOfferMatcherTest.php` exists with 5 it() blocks: `grep -c "^it(" tests/unit/Match/ProductCodeSingleOfferMatcherTest.php` == 5.
    - `make test` exit 0 (suite count grows by 5 more vs after Task 1).
    - `make analyse phpmd pint-test` all exit 0.
    - `sha256sum phpstan-baseline.neon` first column == `4b3227fab5b697264e8532b59f5cdd96c86a0ff1fa484cc1a869af36ae91530a`.
    - 2 atomic commits: `test(06-03):` THEN `feat(06-03):` for Task 2.
  </acceptance_criteria>
  <done>
    ProductCodeSingleOfferMatcher implements MatchStrategy with the verbatim Pass 2 SQL; 1-query budget proven by query-log test; 5 cases green; baseline SHA pinned. Existing EanMatcherService::matchBatch still works unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ParsedLine residue → matcher SQL | Internal pipeline; ean values are already 13-digit-validated by Phase 2 HtmInvoiceParser + EanMatcherService::assertAllEansValid (defense-in-depth gate stays in EanMatcherService chain runner — Plan 05). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | T (Tampering / SQL injection) | OfferCodeMatcher / ProductCodeSingleOfferMatcher | mitigate | Eloquent `whereIn` parameterizes IN clause via PDO bind variables; attacker strings never touch SQL. Defense-in-depth: chain runner (Plan 05) preserves the pre-existing EAN regex `^\d{13}$` guard upstream of the SELECT. |
| T-06-03-02 | I (Information disclosure via correlated subquery) | ProductCodeSingleOfferMatcher addSelect subquery | accept | Subquery returns only `offer.id` (already exposed via Pass 1); `whereColumn('product_id', 'lovata_shopaholic_products.id')` joins by primary key only. No PII / pricing / external_id leak path. |
| T-06-03-03 | T (Multi-offer ambiguity) | ProductCodeSingleOfferMatcher single-offer guard | mitigate | `has('offer', '=', 1)` enforces single-offer. Multi-offer products are deterministically skipped (line falls through to chain runner residue and ultimately match_strategy='none'). Pinned by Test 2 in <behavior>. |
</threat_model>

<verification>
- Both new matcher classes type-check at PHPStan L10.
- Both new test files pass with the hermetic offers/products schema (existing EanMatcherTestCase pattern).
- Existing EanMatcherServiceTest still passes (no behavioral change to matchBatch yet).
- `make all` exit 0; phpstan-baseline.neon SHA unchanged.
- Suite count grows by exactly 10 (5 OfferCodeMatcher + 5 ProductCodeSingleOfferMatcher) vs. Plan 02 close.
</verification>

<success_criteria>
1. `classes/match/OfferCodeMatcher.php` and `classes/match/ProductCodeSingleOfferMatcher.php` both exist as final classes implementing `MatchStrategy`.
2. Each emits exactly its strategy literal (`'offer_code'` or `'product_code_single_offer'`).
3. Each fires exactly 1 query per non-empty input (counter-pinned by `DB::enableQueryLog`).
4. Existing `EanMatcherServiceTest` continues to pass — `matchBatch` behavior unchanged at this stage.
5. `make all` exit 0; phpstan-baseline.neon SHA == `4b3227fab5b697264e8532b59f5cdd96c86a0ff1fa484cc1a869af36ae91530a`.
6. 4 atomic commits in TDD order across both tasks (test → feat → test → feat).
</success_criteria>

<output>
After completion, create `.planning/phases/06-pass-3-variation-ean-matcher/06-03-SUMMARY.md`.
</output>
