---
phase: 06-pass-3-variation-ean-matcher
verified: 2026-05-01T00:00:00Z
status: passed
score: 19/19 must-haves verified (6 ROADMAP success criteria + 13 REQ-IDs)
overrides_applied: 0
---

# Phase 6: Pass 3 Variation EAN Matcher — Verification Report

**Phase Goal:** Recover invoice lines whose EAN matches neither `offer.code` (Pass 1) nor `product.code` single-offer (Pass 2) by adding a deterministic third pass that matches on the offer-name `variation` token (last comma-segment of "Product, Variation"), with a single-offer guard for ambiguity safety. Refactor `EanMatcherService` into a chain runner backed by a `MatchStrategy` interface; lift D-25 query budget 2 → 3; widen `MatchedLine.strategy` union with `'variation'`; add orange + asterisk render branch in `_column_product_name.htm`.

**Verified:** 2026-05-01
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| #   | Truth                                                                                                      | Status     | Evidence                                                                                                                                                                                                                                                                                                                                  |
| --- | ---------------------------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `EanMatcherService::matchLines(list<ParsedLine>): list<MatchedLine>` runs 3-stage chain; old signature gone | ✓ VERIFIED | `classes/match/EanMatcherService.php:92` declares `public function matchLines(array $arLines): array`. Constructor wires `[OfferCodeMatcher, ProductCodeSingleOfferMatcher, VariationMatcher]` (lines 67-74). Plugin-wide grep for `matchBatch` returns ONE match in a docblock comment in `EanMatcherServiceTest.php:18` (legacy reference, not a callable shim). `ParseAndPersistOrchestrator.php:318` calls `$this->obMatcher->matchLines($obParsed->lines)`. EanMatcherServiceTest pins ≤3 queries for full-cascade input (test "runs at most 3 queries"). |
| 2   | `VariationMatcher` issues exactly 1 query; single-offer hit → 'variation'; ≥2 → 'none'; empty/no-comma → 'none'; leading-zero EAN preserved | ✓ VERIFIED | `classes/match/VariationMatcher.php:91-93` issues exactly `Offer::whereIn('variation', $arUnique)->select(['id','variation','product_id'])->get()`. Single-offer guard at line 71 (`count($arOfferIds) !== 1` → continue). Empty/no-comma input handled via `VariationExtractor::extract` returning `null` (line 43). VariationMatcherTest passes 5 cases including "issues EXACTLY 1 query", "skips ambiguous variation", "omits no-comma / empty", "preserves leading-zero EAN as STRING". |
| 3   | `VariationExtractor::extract` is the SOLE regex source — both VariationMatcher and partial consume it       | ✓ VERIFIED | Plugin-wide grep `grep -RFn -- '/^(.+),\s+([^,]+)$/u' classes/` returns EXACTLY ONE hit at `classes/support/VariationExtractor.php:28` (private const VARIATION_REGEX). `VariationMatcher.php:42` calls `VariationExtractor::extract($obLine->product_name_raw)`. `_column_product_name.htm:33,41` imports `use Logingrupa\GoodsReceivedShopaholic\Classes\Support\VariationExtractor;` and calls `VariationExtractor::extract($sName)`. ColumnProductNameVariationBranchTest pins DRY invariant via shell-level grep assertion. |
| 4   | `MatchedLine.strategy` union widened to 4 literals; PHPStan L10 still 0 errors; zero migrations             | ✓ VERIFIED | `classes/dto/MatchedLine.php:21` declares `@property-read 'offer_code'\|'product_code_single_offer'\|'variation'\|'none' $match_strategy`; constructor `@param` widened identically (line 26). `make analyse` returns "[OK] No errors" (38/38 files at PHPStan L10). No new files in `updates/` for Phase 6 (column `match_strategy varchar(32)` already accommodates the new literal). |
| 5   | `_column_product_name.htm` renders 'variation' branch as orange + asterisk + product name; existing branches preserved | ✓ VERIFIED | `models/invoiceline/_column_product_name.htm:67-79` renders the 'variation' branch with `style="color: orange"` + `<?= e($sCleanName) ?>*` (asterisk literal) + offer link in parens. Existing branches for 'offer_code' / 'product_code_single_offer' (lines 84-91) and 'none' (lines 51-57) are byte-for-byte preserved. ColumnProductNameVariationBranchTest passes 7 render-snapshot cases including "renders matched offer_code branch with black product link", "renders NEW variation branch with ORANGE styling + ASTERISK", "renders unmatched none branch as plain text". |
| 6   | `make all` carve-out: pint-test/lint-settings-accessor/analyse/phpmd green; baseline SHA UNCHANGED; QA-09 grep gate green; new tests pass | ✓ VERIFIED | `make pint-test` → `{"result":"pass"}`. `make lint-settings-accessor` → `==> QA-09 grep gate: Settings::get( must appear only in classes/support/SettingsAccessor.php` (no violations). `make analyse` → `[OK] No errors` (38/38 PHPStan L10). `make phpmd` → 0 violations. `sha256sum phpstan-baseline.neon` → `4b3227fab5b697264e8532b59f5cdd96c86a0ff1fa484cc1a869af36ae91530a` (UNCHANGED). 35 new Phase 6 tests / 116 assertions → all PASS. |

**Score:** 6/6 ROADMAP success criteria verified.

### Required Artifacts

| Artifact                                                       | Expected                                              | Status     | Details                                                                                                                                                                                       |
| -------------------------------------------------------------- | ----------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `classes/match/MatchStrategy.php`                              | Interface with `match(list<ParsedLine>): list<MatchedLine>` | ✓ VERIFIED | 38 LoC, declare(strict_types=1), namespace correct, single method `match(array $arUnmatched): array` with PHPStan-narrowed @param/@return.                                                  |
| `classes/match/OfferCodeMatcher.php`                            | final class implements MatchStrategy, 1 query, 'offer_code' | ✓ VERIFIED | 75 LoC, `final class` + `#[\Override]`, single SELECT `Offer::whereIn('code', $arEans)->get(['id', 'code'])` (line 63), strategy literal `'offer_code'` (line 49).                          |
| `classes/match/ProductCodeSingleOfferMatcher.php`              | final class implements MatchStrategy, 1 query (correlated), 'product_code_single_offer' | ✓ VERIFIED | 86 LoC, single query via `Product::whereIn('code')->has('offer','=',1)->addSelect(...subquery...)` (line 63), strategy literal `'product_code_single_offer'` (line 49).                     |
| `classes/match/VariationMatcher.php`                            | final class implements MatchStrategy, 1 query, single-offer guard, 'variation' | ✓ VERIFIED | 108 LoC, `final class` + `#[\Override]`, `Offer::whereIn('variation',...)->select(['id','variation','product_id'])->get()` (line 91), single-offer guard (line 71), strategy `'variation'` (line 78). |
| `classes/match/EanMatcherService.php`                           | Chain runner with `matchLines` signature, 3-stage chain, EAN regex defense | ✓ VERIFIED | 188 LoC. Constructor wires the 3 stages (lines 67-74). `matchLines()` runs each stage on residue (lines 105-123), wraps unmatched residue as 'none' (lines 126-132), reorders to input order (line 137). |
| `classes/support/VariationExtractor.php`                        | final readonly class, static `extract(string): ?string`, sole regex source | ✓ VERIFIED | 49 LoC, `final readonly class`, `private const string VARIATION_REGEX = '/^(.+),\s+([^,]+)$/u'` (line 28), static method returns `?string` (line 35). The regex literal occurs exactly ONCE in `classes/`. |
| `classes/dto/MatchedLine.php`                                   | strategy union widened to 4 literals; runtime stays string  | ✓ VERIFIED | `@property-read` widened (line 21); constructor `@param` widened (line 26); runtime parameter still `string $match_strategy` (line 31, D-26 portability).                                    |
| `classes/orchestrator/ParseAndPersistOrchestrator.php`          | Caller updated to `matchLines($obParsed->lines)`           | ✓ VERIFIED | Line 318: `return $this->obMatcher->matchLines($obParsed->lines);`. No other call sites.                                                                                                    |
| `models/invoiceline/_column_product_name.htm`                   | 4-branch dispatch with orange+asterisk for 'variation'; DRY VariationExtractor | ✓ VERIFIED | 91 LoC, imports VariationExtractor (line 33), calls `VariationExtractor::extract($sName)` (line 41), 4 branches dispatched on `$sStrategy`: 'none' (51-57), 'variation' orange+asterisk (67-79), 'offer_code'/'product_code_single_offer' (84-91). |
| `tests/unit/Support/VariationExtractorTest.php`                 | 5 Pest cases pinning regex behavior                         | ✓ VERIFIED | All 5 cases pass: last-comma greedy, no-comma → null, multi-comma split at LAST comma, whitespace trim, empty input → null.                                                                  |
| `tests/unit/Match/OfferCodeMatcherTest.php`                     | Pest cases pinning Pass 1 behavior                          | ✓ VERIFIED | Test file PASS — full output shows it pins single-EAN match, miss, mixed input, exactly 1 query.                                                                                            |
| `tests/unit/Match/ProductCodeSingleOfferMatcherTest.php`        | Pest cases pinning Pass 2 behavior                          | ✓ VERIFIED | Test file PASS — pins single-offer match, multi-offer skip, no match, exactly 1 query (correlated subquery).                                                                                |
| `tests/unit/Match/VariationMatcherTest.php`                     | 5 Pest cases pinning Pass 3 behavior                        | ✓ VERIFIED | All 5 cases pass: single-offer-hit emits 'variation', ambiguous skip (≥2 offers), no-comma/empty omitted with 0 queries, leading-zero EAN preserved, mixed input.                            |
| `tests/unit/Match/EanMatcherServiceTest.php`                    | Chain integration rewrite with ≤3-query counter-pin         | ✓ VERIFIED | All 8 cases pass: full chain end-to-end with 4 strategy literals, ≤3 queries (D-25-update budget), 1 query when all match Pass 1, leading-zero preservation, InvalidEanException pre-DB, empty input zero queries, input-order preservation, Pass 3 single-offer guard. |
| `tests/unit/Models/ColumnProductNameVariationBranchTest.php`    | Render-snapshot test for 4-branch dispatch + DRY assertion   | ✓ VERIFIED | All 7 cases pass: black 'offer_code' branch, identical 'product_code_single_offer' branch, NEW orange 'variation' branch, plain-text 'none' branch with asterisk, no-comma 'none', defensive variation-with-no-comma, DRY grep invariant. |

### Key Link Verification

| From                                       | To                          | Via                                            | Status     | Details                                                                                  |
| ------------------------------------------ | --------------------------- | ---------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------- |
| `EanMatcherService`                        | `OfferCodeMatcher`          | `new OfferCodeMatcher()` in constructor       | ✓ WIRED    | Line 70.                                                                                 |
| `EanMatcherService`                        | `ProductCodeSingleOfferMatcher` | `new ProductCodeSingleOfferMatcher()`        | ✓ WIRED    | Line 71.                                                                                 |
| `EanMatcherService`                        | `VariationMatcher`          | `new VariationMatcher()`                       | ✓ WIRED    | Line 72. Pass 3 plugged into chain.                                                      |
| `VariationMatcher`                         | `VariationExtractor`        | `VariationExtractor::extract($obLine->product_name_raw)` | ✓ WIRED    | Line 42. Sole regex source consumed; Hungarian-prefixed local var.                       |
| `_column_product_name.htm`                 | `VariationExtractor`        | `VariationExtractor::extract($sName)`          | ✓ WIRED    | Line 41. Inline regex deleted; partial defers to sole source.                            |
| `ParseAndPersistOrchestrator`              | `EanMatcherService::matchLines` | `$this->obMatcher->matchLines($obParsed->lines)` | ✓ WIRED    | Line 318. Old `matchBatch` signature gone; orchestrator passes typed `list<ParsedLine>`. |
| `MatchedLine.strategy`                     | `'variation'` literal       | `@property-read` + `@param` union              | ✓ WIRED    | DTO emits PHPStan-narrowed widened literal; runtime DB column unchanged at varchar(32).  |

### Behavioral Spot-Checks

| Behavior                                                            | Command                                                                                                                                                  | Result                                                                                                                                                                                                                                          | Status |
| ------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 35 new Phase 6 tests pass in isolation                              | `pest tests/unit/{Support/VariationExtractorTest,Match/{OfferCode,ProductCode,Variation,EanMatcherService}Test,Models/ColumnProductNameVariationBranchTest}.php` | `Tests: 35 passed (116 assertions) Duration: 4.00s`                                                                                                                                                                                            | ✓ PASS |
| `phpstan-baseline.neon` SHA pin                                     | `sha256sum phpstan-baseline.neon`                                                                                                                       | `4b3227fab5b697264e8532b59f5cdd96c86a0ff1fa484cc1a869af36ae91530a phpstan-baseline.neon` (UNCHANGED)                                                                                                                                            | ✓ PASS |
| PHPStan L10 zero errors                                             | `make analyse`                                                                                                                                          | `[OK] No errors` (38/38 files)                                                                                                                                                                                                                  | ✓ PASS |
| Pint style clean                                                    | `make pint-test`                                                                                                                                        | `{"result":"pass"}`                                                                                                                                                                                                                              | ✓ PASS |
| PHPMD clean (Lovata ruleset)                                        | `make phpmd`                                                                                                                                            | 0 violations                                                                                                                                                                                                                                    | ✓ PASS |
| QA-09 grep gate (Settings::get DRY)                                 | `make lint-settings-accessor`                                                                                                                           | `==> QA-09 grep gate: Settings::get( must appear only in classes/support/SettingsAccessor.php` (no violation lines)                                                                                                                            | ✓ PASS |
| DRY: variation regex literal occurs exactly ONCE in `classes/`      | `grep -RFn -- '/^(.+),\s+([^,]+)$/u' classes/`                                                                                                          | `classes/support/VariationExtractor.php:28: private const string VARIATION_REGEX = '/^(.+),\s+([^,]+)$/u';` (1 hit)                                                                                                                              | ✓ PASS |
| `matchBatch` legacy method completely gone (no shim)                | `grep -rn 'matchBatch' classes/`                                                                                                                        | (no matches in `classes/`)                                                                                                                                                                                                                      | ✓ PASS |
| `make all` exit code (with carve-out for pre-existing failures)     | `make all` → fails on `test` step due to 125 pre-existing failures                                                                                       | `make: *** [Makefile:8: test] Error 2` — but pint-test/lint-settings-accessor/analyse/phpmd all pass; pre-existing failures documented in `deferred-items.md` and confirmed unrelated (MySQL identifier-length + Mockery facade-set drift)     | ⚠️ DEFERRED |

### Requirements Coverage

| Requirement | Source Plan | Description                                                                                                                                                                              | Status      | Evidence                                                                                                                                                                |
| ----------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| MATCH-03    | 06-01-PLAN  | `VariationExtractor::extract(string): ?string` — last-comma greedy regex `/^(.+),\s+([^,]+)$/u`, whitespace trim, no-comma → null, empty → null                                          | ✓ SATISFIED | `classes/support/VariationExtractor.php` ships final readonly class with the locked regex (line 28) and contract (lines 35-48); 5 Pest cases pin behavior.              |
| MATCH-04    | 06-04-PLAN  | `VariationMatcher` issues exactly 1 query; single-offer hit → 'variation'; ≥2 → omit; empty/no-comma → omit                                                                              | ✓ SATISFIED | `classes/match/VariationMatcher.php` ships exactly 1 SELECT (line 91) + single-offer guard (line 71); VariationMatcherTest pins single-hit / ambiguous / empty / 1-query / leading-zero. |
| MATCH-05    | 06-02-PLAN  | `MatchStrategy` interface declares `match(list<ParsedLine>): list<MatchedLine>` chain-stage contract                                                                                      | ✓ SATISFIED | `classes/match/MatchStrategy.php` declares the contract; 3 implementers (`OfferCodeMatcher`, `ProductCodeSingleOfferMatcher`, `VariationMatcher`) all implement.        |
| MATCH-06    | 06-03-PLAN  | `OfferCodeMatcher` implements MatchStrategy; 1 query against `offer.code`; strategy literal `'offer_code'`                                                                                | ✓ SATISFIED | `classes/match/OfferCodeMatcher.php:25-75`; OfferCodeMatcherTest passes.                                                                                              |
| MATCH-07    | 06-03-PLAN  | `ProductCodeSingleOfferMatcher` implements MatchStrategy; 1 correlated query against `product.code` with single-offer guard; strategy literal `'product_code_single_offer'`              | ✓ SATISFIED | `classes/match/ProductCodeSingleOfferMatcher.php:25-86`; ProductCodeSingleOfferMatcherTest passes.                                                                   |
| MATCH-08    | 06-05-PLAN  | `EanMatcherService` chain runner; `matchLines(list<ParsedLine>): list<MatchedLine>`; 3-pass chain; total query budget = 3                                                                | ✓ SATISFIED | `classes/match/EanMatcherService.php:92` + chain wiring lines 67-74 + EanMatcherServiceTest "runs at most 3 queries" pin.                                              |
| MATCH-09    | 06-02-PLAN  | `MatchedLine.strategy` union widened with `'variation'` literal; no migration                                                                                                            | ✓ SATISFIED | `classes/dto/MatchedLine.php:21,26` 4-literal union; `updates/` directory has no new migrations from Phase 6.                                                          |
| MATCH-10    | 06-05-PLAN  | `ParseAndPersistOrchestrator.php:319` updated to consume `matchLines(list<ParsedLine>)`                                                                                                  | ✓ SATISFIED | `classes/orchestrator/ParseAndPersistOrchestrator.php:318` calls `matchLines($obParsed->lines)`.                                                                       |
| UI-13       | 06-06-PLAN  | `_column_product_name.htm` adds 'variation' render branch — orange + asterisk + product name; existing branches preserved; DRY-replaced inline regex                                       | ✓ SATISFIED | `models/invoiceline/_column_product_name.htm:67-79` orange+asterisk; lines 84-91 preserved 'offer_code'/'product_code_single_offer' branches; lines 51-57 preserved 'none'; line 41 calls `VariationExtractor::extract`. |
| QA-12       | 06-04-PLAN  | `VariationMatcherTest` pins single-hit, ambiguous-skip, empty-skip, leading-zero EAN, query count = 1                                                                                    | ✓ SATISFIED | All 5 cases pass.                                                                                                                                                       |
| QA-13       | 06-01-PLAN  | `VariationExtractorTest` pins last-comma greedy, no-comma → null, multi-comma split, whitespace trim, empty input → null                                                                 | ✓ SATISFIED | All 5 cases pass (verified by Plan 06-01 commit `f1063fa`).                                                                                                            |
| QA-14       | 06-05-PLAN  | `EanMatcherServiceTest` chain integration — ≤3 queries per `matchLines()`, strategy literal preservation, signature pin                                                                  | ✓ SATISFIED | All 8 cases pass; counter-pin assertion `runs at most 3 queries for full-cascade input (D-25-update budget)` green.                                                    |
| QA-15       | 06-06-PLAN  | `_column_product_name.htm` snapshot/render test for 'variation' branch; existing branches' snapshots unchanged                                                                            | ✓ SATISFIED | `tests/unit/Models/ColumnProductNameVariationBranchTest.php` 7 cases all pass; includes DRY grep invariant assertion.                                                 |

**Coverage:** 13/13 REQ-IDs satisfied. No orphans.

### Anti-Patterns Found

None of significance.

| File                              | Line  | Pattern                                  | Severity | Impact                                                                                                            |
| --------------------------------- | ----- | ---------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------- |
| `EanMatcherServiceTest.php`       | 18    | Comment: "matchBatch and buildMatchedLines are GONE" | ℹ️ Info  | Docblock-only; not a callable. Legacy method removed verbatim — confirms "no shim" contract. Not a stub.       |

### Human Verification Required

None. All checks programmatically verified — no UI visual / real-time / external service / multi-site behavior on the Phase 6 surface. (Phase 6 surface is internal matcher refactor + backend column partial; existing render-snapshot test pins UI byte sequence.)

### Gaps Summary

**No gaps.** All 6 ROADMAP success criteria + 13 REQ-IDs are observably delivered:

1. The 3-stage chain runner ships in `EanMatcherService.php` with the new `matchLines(list<ParsedLine>): list<MatchedLine>` signature; old `matchBatch` is gone (confirmed by plugin-wide grep — only matches a docblock comment in a test). Single caller site at `ParseAndPersistOrchestrator.php:318` updated.
2. `VariationMatcher` issues exactly 1 query, applies the single-offer guard, and emits the 'variation' strategy literal. 5-case Pest test (`VariationMatcherTest`) pins all locked behaviors including leading-zero EAN preservation.
3. `VariationExtractor` is the SOLE regex source — plugin-wide grep on the regex literal `/^(.+),\s+([^,]+)$/u` returns exactly one hit (in `VariationExtractor.php:28`); both `VariationMatcher` and `_column_product_name.htm` consume the static method instead of re-implementing.
4. `MatchedLine.strategy` union widened to `'offer_code'|'product_code_single_offer'|'variation'|'none'`. PHPStan L10 reports 0 errors; baseline SHA UNCHANGED at `4b3227fab5b697264e8532b59f5cdd96c86a0ff1fa484cc1a869af36ae91530a`. No new migration shipped (confirmed by directory listing).
5. The render branch for 'variation' (orange + asterisk + product name) is in `_column_product_name.htm` lines 67-79; existing 3 branches (matched / unmatched) byte-for-byte preserved; `ColumnProductNameVariationBranchTest` snapshot tests all pass including the DRY grep invariant.
6. `make pint-test`, `make lint-settings-accessor`, `make analyse`, `make phpmd` all pass; the 35 new Phase 6 tests pass cleanly with 116 assertions. The 125 pre-existing test failures (`make test` step in `make all`) are documented in `deferred-items.md` as MySQL identifier-length / Mockery facade-set infrastructure drift unrelated to Phase 6 — the verification context explicitly carves these out: "Phase 6's contract is 'no NEW failures introduced; all NEW tests pass; phpstan-baseline.neon SHA unchanged'". Test count delta = +22 passing, 0 new failures.

The implementation honors Tiger-Style discipline throughout: every new class is `final` (or `final readonly`), every override decorated with `#[\Override]`, all files lead with `declare(strict_types=1);`, Hungarian notation consistent (`$arUnmatched`, `$obLine`, `$sVariation`, `$arOfferIds`), bounded loops with single-SELECT data fetching per stage, defense-in-depth EAN regex preserved at chain runner boundary.

---

_Verified: 2026-05-01_
_Verifier: Claude (gsd-verifier)_
