---
phase: 06-pass-3-variation-ean-matcher
plan: 04
type: tdd
wave: 2
depends_on:
  - 01
  - 02
files_modified:
  - classes/match/VariationMatcher.php
  - tests/unit/Match/VariationMatcherTest.php
autonomous: true
requirements:
  - MATCH-04
  - QA-12
must_haves:
  truths:
    - "VariationMatcher implements MatchStrategy"
    - "VariationMatcher issues EXACTLY 1 query: Offer::whereIn('variation', $arUnique)->select('id', 'variation', 'product_id')->get()"
    - "Single-offer guard: variation hits exactly 1 offer → MatchedLine with match_strategy='variation'; ≥2 offers → omitted (residue → 'none' in chain runner)"
    - "Empty / no-comma offer name (VariationExtractor::extract returns null) → ParsedLine omitted from match() output"
    - "Leading-zero EANs preserved (EAN passthrough; matcher runs on variation column, not EAN — EAN is preserved on the carried ParsedLine)"
    - "phpstan-baseline.neon SHA UNCHANGED at 4b3227fab5b697264e8532b59f5cdd96c86a0ff1fa484cc1a869af36ae91530a"
  artifacts:
    - path: "classes/match/VariationMatcher.php"
      provides: "Pass 3 chain stage — Offer.variation last-comma-segment match with single-offer guard (1 query)"
      contains: "final class VariationMatcher implements MatchStrategy"
    - path: "tests/unit/Match/VariationMatcherTest.php"
      provides: "Pest 4 cases: single hit / ambiguous skip / empty skip / leading-zero EAN passthrough / query count = 1"
      contains: "VariationMatcher"
  key_links:
    - from: "classes/match/VariationMatcher.php"
      to: "classes/support/VariationExtractor.php (Plan 01)"
      via: "VariationExtractor::extract($obLine->product_name_raw)"
      pattern: "VariationExtractor::extract"
    - from: "classes/match/VariationMatcher.php"
      to: "classes/match/MatchStrategy.php (Plan 02)"
      via: "implements MatchStrategy + #[\\Override] on match()"
      pattern: "implements MatchStrategy"
    - from: "classes/match/VariationMatcher.php"
      to: "lovata_shopaholic_offers.variation column"
      via: "Offer::whereIn('variation', $arUnique)->select(['id', 'variation', 'product_id'])->get()"
      pattern: "->whereIn\\('variation'"
---

<objective>
Implement Pass 3 — `VariationMatcher` — the new chain stage that recovers ParsedLines whose EAN missed both Pass 1 and Pass 2, by matching the offer-name's last-comma variation token against `lovata_shopaholic_offers.variation`.

Purpose: MATCH-04 + QA-12. This is the ONLY new query the phase introduces — D-25 budget lifts 2 → 3.

Output: 1 new `final` `MatchStrategy` implementation (~70 LoC) + 1 Pest test file with 5 cases including a 1-query counter-pin.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-pass-3-variation-ean-matcher/06-CONTEXT.md
@CLAUDE.md
@classes/match/MatchStrategy.php
@classes/match/OfferCodeMatcher.php
@classes/support/VariationExtractor.php
@classes/dto/ParsedLine.php
@classes/dto/MatchedLine.php
@tests/unit/Match/EanMatcherServiceTest.php
</context>

<interfaces>
<!-- Source-of-truth schema reference -->

The `variation` column is added to `lovata_shopaholic_offers` by sibling plugin `logingrupa/storeextender`:
From plugins/logingrupa/storeextender/updates/update_table_lovata_shopaholic_offers.php:21:
```php
$obTable->string('variation')->nullable()->after('description');
```

The hermetic test schema in `tests/unit/Match/EanMatcherServiceTest.php` (the `EanMatcherTestCase` base, lines 62-76) does NOT yet include the `variation` column. The new VariationMatcherTest will need to extend or copy the hermetic schema and ADD the `variation` column:
```php
$obTable->string('variation')->nullable();
```

VariationExtractor (from Plan 01) is the SOLE regex source. Use it to derive the variation token from each ParsedLine's `product_name_raw`. Lines whose extract() returns null are OMITTED from the SELECT input AND from the result.

Chain-stage contract reminder (from MatchStrategy):
- Input: `list<ParsedLine>` residue (lines unmatched by Pass 1 + Pass 2)
- Output: `list<MatchedLine>` for ONLY the ParsedLines this stage matched
- Lines this stage cannot match are simply omitted — chain runner forwards as residue → eventually 'none'.

Single-offer guard (Tiger-Style determinism per D-25-update):
- Group result rows by variation string.
- variation hits EXACTLY 1 offer → emit MatchedLine with that offer.id and `match_strategy='variation'`.
- variation hits ≥ 2 offers → OMIT (ambiguous; never silently best-guess; line falls through chain to 'none').
</interfaces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED → GREEN): VariationMatcher class + 5 Pest cases</name>
  <files>classes/match/VariationMatcher.php, tests/unit/Match/VariationMatcherTest.php</files>
  <read_first>
    - classes/match/MatchStrategy.php (the interface contract — read fully)
    - classes/support/VariationExtractor.php (Plan 01 — sole regex source; the matcher MUST consume `VariationExtractor::extract`, never re-implement the regex)
    - classes/match/OfferCodeMatcher.php (Plan 03 sibling — match the structural pattern: derive unique inputs, 1 SELECT, emit MatchedLines for hits, omit misses)
    - classes/dto/ParsedLine.php (read fully — `product_name_raw` is the variation source field; canonical 9-field DTO)
    - classes/dto/MatchedLine.php (4-literal union; verify 'variation' is in `@param`/`@property-read`)
    - tests/unit/Match/EanMatcherServiceTest.php (lines 44-128: hermetic schema setUp() + seedProduct() + seedOffer() — VariationMatcherTest extends/reuses but ALSO adds the `variation` column)
    - plugins/logingrupa/storeextender/updates/update_table_lovata_shopaholic_offers.php (source-of-truth for the variation column shape: nullable string)
  </read_first>
  <behavior>
    Test 1 (single hit, query count = 1):
      Seed Offer (id=42, product_id=10, variation='1081', code='IRRELEVANT').
      Input: 1 ParsedLine with product_name_raw='Gel Polish UV/LED, 12ml, 1081', ean='9999999999999' (any 13-digit).
      Call DB::enableQueryLog() before match(); after, count(DB::getQueryLog()) === 1.
      match() returns list with 1 MatchedLine: matched_offer_id===42, match_strategy==='variation', line is the input ParsedLine.

    Test 2 (ambiguous skip, ≥2 offers → 'none' residue):
      Seed TWO Offers both with variation='1081' (id=42 product_id=10 + id=43 product_id=11).
      Input: 1 ParsedLine with product_name_raw='Gel Polish UV/LED, 12ml, 1081'.
      match() returns [] (single-offer guard fails; ambiguity skip per Tiger-Style).

    Test 3 (no-comma / empty extract → omit):
      Seed Offer (id=42, variation='1081').
      Input: 2 ParsedLines:
        - product_name_raw='SingleWordName' (VariationExtractor returns null)
        - product_name_raw='' (VariationExtractor returns null)
      match() returns [] AND DB::getQueryLog() count === 0 (no SELECT issued because $arUnique was empty after extraction).

    Test 4 (leading-zero EAN passthrough):
      Seed Offer (id=42, product_id=10, variation='1081').
      Input: 1 ParsedLine with ean='0000000012345' (leading zeros), product_name_raw='X, 1081'.
      match() returns 1 MatchedLine; the ParsedLine on it has ean === '0000000012345' (STRING preserved). Assert `$arResult[0]->line->ean === '0000000012345'`.

    Test 5 (mixed input — single hit + ambiguous + miss combined, query count = 1):
      Seed Offer A (id=42, variation='1081'), and TWO Offers with variation='2002', plus NO offer with variation='9999'.
      Input: 3 ParsedLines:
        - product_name_raw='X, 1081' → matched
        - product_name_raw='Y, 2002' → ambiguous, skipped
        - product_name_raw='Z, 9999' → no offer in DB
      match() returns list with EXACTLY 1 MatchedLine (the '1081' one). DB::getQueryLog() count === 1.
  </behavior>
  <action>
    RED step (commit 1):
    Create `tests/unit/Match/VariationMatcherTest.php`. Define a local `VariationMatcherTestCase` extending `GoodsReceivedTestCase` (matching the existing `EanMatcherTestCase` pattern) with hermetic schema CREATING `lovata_shopaholic_offers` AND `lovata_shopaholic_products` AND including the `variation` column on the offers table:

      \Schema::create('lovata_shopaholic_offers', function ($obTable): void {
          $obTable->increments('id');
          $obTable->integer('product_id')->unsigned()->nullable();
          $obTable->string('code')->nullable();
          $obTable->string('name');
          $obTable->string('variation')->nullable();    // <-- key addition (storeextender column)
          $obTable->boolean('active')->default(true);
          $obTable->integer('quantity')->default(0);
          $obTable->integer('sort_order')->default(0);
          $obTable->timestamps();
          $obTable->softDeletes();
          $obTable->index('code');
          $obTable->index('product_id');
          $obTable->index('variation');
      });

    Add a local `seedOfferWithVariation(int $iProductId, string $sVariation, ?string $sCode = null): Offer` helper. Then write 5 it() cases per <behavior>. Run `make test` — RED (class not found). Commit:
      `test(06-04): add failing VariationMatcher Pass 3 tests (QA-12)`

    GREEN step (commit 2):
    Create `classes/match/VariationMatcher.php` with EXACTLY this shape:

      <?php

      declare(strict_types=1);

      namespace Logingrupa\GoodsReceivedShopaholic\Classes\Match;

      use Logingrupa\GoodsReceivedShopaholic\Classes\Dto\MatchedLine;
      use Logingrupa\GoodsReceivedShopaholic\Classes\Dto\ParsedLine;
      use Logingrupa\GoodsReceivedShopaholic\Classes\Support\VariationExtractor;
      use Lovata\Shopaholic\Models\Offer;

      /**
       * Pass 3 chain stage — Offer.variation last-comma-segment match with
       * single-offer guard (Phase 6 / D-25-update).
       *
       * Recovers ParsedLines whose EAN missed both Pass 1 (offer.code) and
       * Pass 2 (product.code single-offer) by extracting the trailing
       * comma-segment from `product_name_raw` ("Product, Variation") via
       * VariationExtractor and matching against `lovata_shopaholic_offers.variation`
       * (column added by sibling plugin logingrupa/storeextender).
       *
       * Issues EXACTLY ONE query for any non-empty deduped variation list:
       *   `Offer::whereIn('variation', $arUnique)->select(['id','variation','product_id'])->get()`
       *
       * Tiger-Style deterministic ambiguity skip: variation hits ≥2 offers →
       * line is OMITTED (chain residue → 'none'); never silently best-guess.
       *
       * DRY contract: variation extraction is delegated to VariationExtractor —
       * the regex /^(.+),\s+([^,]+)$/u appears EXACTLY ONCE in classes/.
       */
      final class VariationMatcher implements MatchStrategy
      {
          #[\Override]
          public function match(array $arUnmatched): array
          {
              if ($arUnmatched === []) {
                  return [];
              }

              // Per-line variation extraction (delegate regex to sole source).
              $arLineVariation = [];
              foreach ($arUnmatched as $iIdx => $obLine) {
                  $sVariation = VariationExtractor::extract($obLine->product_name_raw);
                  if ($sVariation === null) {
                      continue;
                  }
                  $arLineVariation[$iIdx] = $sVariation;
              }

              if ($arLineVariation === []) {
                  return [];
              }

              $arUnique = array_values(array_unique($arLineVariation));

              // Single SELECT for the whole batch (D-25-update query #3).
              $arOfferRows = $this->lookupOffersByVariation($arUnique);

              // Group offer ids by variation for the single-offer guard.
              $arGrouped = [];
              foreach ($arOfferRows as $arRow) {
                  $arGrouped[$arRow['variation']][] = $arRow['id'];
              }

              $arResult = [];
              foreach ($arUnmatched as $iIdx => $obLine) {
                  if (! isset($arLineVariation[$iIdx])) {
                      continue;
                  }
                  $sVariation = $arLineVariation[$iIdx];
                  $arOfferIds = $arGrouped[$sVariation] ?? [];
                  if (count($arOfferIds) !== 1) {
                      // 0 hits OR ≥2 hits — skip (residue → 'none').
                      continue;
                  }
                  $arResult[] = new MatchedLine(
                      line: $obLine,
                      matched_offer_id: $arOfferIds[0],
                      match_strategy: 'variation',
                  );
              }
              return $arResult;
          }

          /**
           * @param  list<string>  $arUniqueVariations
           * @return list<array{id: int, variation: string, product_id: int}>
           */
          private function lookupOffersByVariation(array $arUniqueVariations): array
          {
              $obRows = Offer::whereIn('variation', $arUniqueVariations)
                  ->select(['id', 'variation', 'product_id'])
                  ->get();

              $arOut = [];
              foreach ($obRows as $obRow) {
                  /** @phpstan-ignore-next-line property.notFound — Lovata Offer lacks IDE-helper PHPDoc; columns verified at DB layer */
                  $sVariation = (string) $obRow->variation;
                  /** @phpstan-ignore-next-line property.notFound — Lovata Offer lacks IDE-helper PHPDoc; columns verified at DB layer */
                  $iId = (int) $obRow->id;
                  /** @phpstan-ignore-next-line property.notFound — Lovata Offer lacks IDE-helper PHPDoc; columns verified at DB layer */
                  $iProductId = (int) $obRow->product_id;
                  $arOut[] = ['id' => $iId, 'variation' => $sVariation, 'product_id' => $iProductId];
              }
              return $arOut;
          }
      }

    Run `make test pint-test analyse phpmd` — all green. Commit:
      `feat(06-04): add VariationMatcher Pass 3 chain stage (MATCH-04)`
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/goodsreceivedshopaholic &amp;&amp; make pint-test analyse phpmd test 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `classes/match/VariationMatcher.php` exists.
    - Grep `grep -c 'final class VariationMatcher implements MatchStrategy' classes/match/VariationMatcher.php` == 1.
    - Grep `grep -c '#\[\\\\Override\]' classes/match/VariationMatcher.php` == 1.
    - Grep -F `grep -F -c "match_strategy: 'variation'" classes/match/VariationMatcher.php` == 1 (literal preserved).
    - Grep -F `grep -F -c "VariationExtractor::extract" classes/match/VariationMatcher.php` == 1 (DRY delegation; no inline regex).
    - DRY invariant: `grep -RFn -- "/^(.+),\\s+([^,]+)\$/u" classes/ | wc -l` == 1 (regex still appears exactly once — only in VariationExtractor).
    - Grep -F `grep -F -c "->whereIn('variation'" classes/match/VariationMatcher.php` == 1 (Pass 3 SELECT pinned).
    - Grep -F `grep -F -c "->select(['id', 'variation', 'product_id'])" classes/match/VariationMatcher.php` == 1 (column projection pinned).
    - Single-offer guard pinned: `grep -F -c 'count($arOfferIds) !== 1' classes/match/VariationMatcher.php` == 1.
    - File `tests/unit/Match/VariationMatcherTest.php` exists with 5 it() blocks: `grep -c "^it(" tests/unit/Match/VariationMatcherTest.php` == 5.
    - `tests/unit/Match/VariationMatcherTest.php` includes a query-count assertion: `grep -c 'DB::enableQueryLog' tests/unit/Match/VariationMatcherTest.php` >= 2 (1-query test + mixed-input test).
    - `make test` exit 0 (suite count grows by exactly 5 vs Plan 03 close).
    - `make analyse phpmd pint-test` all exit 0.
    - `sha256sum phpstan-baseline.neon` first column == `4b3227fab5b697264e8532b59f5cdd96c86a0ff1fa484cc1a869af36ae91530a`.
    - 2 atomic commits: `test(06-04):` THEN `feat(06-04):`.
  </acceptance_criteria>
  <done>
    VariationMatcher implements MatchStrategy with the locked 1-query SELECT + single-offer guard; 5 cases green including the 1-query counter-pin; baseline SHA pinned; ready for Plan 05 chain runner integration.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ParsedLine.product_name_raw → variation extraction | Already sanitized upstream by HtmInvoiceParser DOMDocument; VariationExtractor uses anchored UTF-8-safe regex (no ReDoS path). |
| variation token → SELECT WHERE IN | Eloquent parameterizes via PDO bind variables; Tiger-Style: even if a token contained SQL fragments it cannot escape the bind boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | T (Tampering / SQL injection) | VariationMatcher whereIn('variation', ...) | mitigate | Eloquent parameterized binding; variation tokens are also constrained by the regex `^(.+),\s+([^,]+)$` (no newline / null-byte path through the trailing capture group). |
| T-06-04-02 | I (Information disclosure via ambiguous skip) | Single-offer guard | accept | Skip-on-ambiguity is the safer choice (never silently merge stock to wrong offer). Alternative — silently picking first — would leak ordering as side channel. Pinned by Test 2 in <behavior>. |
| T-06-04-03 | T (Multi-comma split corruption) | VariationExtractor delegation | mitigate | DRY delegation to VariationExtractor::extract guarantees consistent split semantics with the UI render branch. Plan 06 will assert the same regex source serves both the matcher and the view; QA-09-style grep gate enforces 1-occurrence invariant under classes/. |
| T-06-04-04 | D (Denial of service via giant variation list) | whereIn('variation', $arUnique) | accept | array_unique deduplicates; bound is the count of distinct invoice lines (real-world invoices are 200-line chunks per existing 200-line apply test in plan 03-03). MySQL's `IN` clause handles hundreds of bound parameters trivially. |
</threat_model>

<verification>
- `make test` shows VariationMatcherTest's 5 it() cases green.
- 1-query counter-pin via `DB::enableQueryLog` proves D-25 budget #3 (1 SELECT for non-empty input, 0 for fully-no-comma input).
- DRY invariant grep returns 1 occurrence of the regex literal in `classes/`.
- `make all` exit 0; phpstan-baseline.neon SHA unchanged.
</verification>

<success_criteria>
1. `classes/match/VariationMatcher.php` exists as `final class VariationMatcher implements MatchStrategy` consuming `VariationExtractor::extract`.
2. The matcher fires EXACTLY 1 query for non-empty inputs, 0 queries when no input line yields a variation.
3. Ambiguous variation (≥ 2 offers with same variation string) deterministically OMITS the line — never silently best-guesses.
4. `tests/unit/Match/VariationMatcherTest.php` has 5 it() cases including 2 query-count counter-pins.
5. `make all` exit 0; phpstan-baseline.neon SHA == `4b3227fab5b697264e8532b59f5cdd96c86a0ff1fa484cc1a869af36ae91530a`.
6. 2 atomic commits in TDD order (test → feat).
</success_criteria>

<output>
After completion, create `.planning/phases/06-pass-3-variation-ean-matcher/06-04-SUMMARY.md`.
</output>
