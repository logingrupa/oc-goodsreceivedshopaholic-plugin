---
phase: 06-pass-3-variation-ean-matcher
plan: 06
type: tdd
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - models/invoiceline/_column_product_name.htm
  - tests/unit/Models/ColumnProductNameVariationBranchTest.php
autonomous: true
requirements:
  - UI-13
  - QA-15
must_haves:
  truths:
    - "_column_product_name.htm reads $record->match_strategy and renders 4 distinct branches: 'offer_code' / 'product_code_single_offer' / 'variation' / 'none'"
    - "'variation' branch renders ORANGE link (style attribute or CSS class containing 'orange') + ASTERISK character + resolved product name"
    - "'offer_code' branch renders unchanged: black/normal product link + offer-link in parens (byte-for-byte preserved against pre-Plan-06 partial)"
    - "'product_code_single_offer' branch renders unchanged: black/normal product link + offer-link in parens (byte-for-byte preserved)"
    - "'none' branch renders unchanged: plain text + asterisk after variation"
    - "Inline regex /^(.+),\\s+([^,]+)$/u in _column_product_name.htm is REMOVED; replaced with VariationExtractor::extract($sName)"
    - "DRY invariant: regex literal /^(.+),\\s+([^,]+)$/u appears EXACTLY ONCE in the entire plugin (only in classes/support/VariationExtractor.php)"
    - "phpstan-baseline.neon SHA UNCHANGED at 4b3227fab5b697264e8532b59f5cdd96c86a0ff1fa484cc1a869af36ae91530a"
  artifacts:
    - path: "models/invoiceline/_column_product_name.htm"
      provides: "Backend list partial with 4-branch render dispatch on match_strategy + DRY VariationExtractor consumption"
      contains: "VariationExtractor::extract"
    - path: "tests/unit/Models/ColumnProductNameVariationBranchTest.php"
      provides: "Render tests for all 4 strategy branches; new 'variation' branch (orange+asterisk+product); existing branches preserved"
      contains: "match_strategy"
  key_links:
    - from: "models/invoiceline/_column_product_name.htm"
      to: "classes/support/VariationExtractor.php (Plan 01)"
      via: "use Logingrupa\\GoodsReceivedShopaholic\\Classes\\Support\\VariationExtractor; VariationExtractor::extract($sName)"
      pattern: "VariationExtractor::extract"
    - from: "models/invoiceline/_column_product_name.htm"
      to: "$record->match_strategy"
      via: "switch / if-else dispatch on the 4-literal union"
      pattern: "match_strategy"
---

<objective>
DRY-replace the inline variation regex (added in commit 9bc11f8) in `models/invoiceline/_column_product_name.htm` with `VariationExtractor::extract`, and add a NEW render branch for `match_strategy='variation'` that displays orange + asterisk + resolved product name. Existing branches for `'offer_code'` / `'product_code_single_offer'` (matched, black/normal link) and `'none'` (unmatched, plain text + asterisk) must be preserved.

Purpose: UI-13 + QA-15. The new render branch is the operator-visible signal that Pass 3 made a deterministic single-offer match — visually distinct from the two byte-perfect-existing branches and from the 'none' fallback.

Output: 1 surgical edit to the partial + 1 new render test file with cases covering all 4 branches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-pass-3-variation-ean-matcher/06-CONTEXT.md
@CLAUDE.md
@models/invoiceline/_column_product_name.htm
@models/invoiceline/columns.yaml
@models/InvoiceLine.php
@classes/support/VariationExtractor.php
@classes/dto/MatchedLine.php
@tests/GoodsReceivedTestCase.php
</context>

<interfaces>
<!-- Current partial state (pre-Plan-06) -->

The current `models/invoiceline/_column_product_name.htm` (read fully — 71 lines) has THREE branches:
- Matched (`$bMatched = $obOffer !== null && (int) $obOffer->id > 0`) → black/normal product link + offer-link in parens
- Unmatched (the `if (!$bMatched)` block) → plain text `e($sCleanName)` + ` (var*)` if variation extracted

The current matched branch does NOT discriminate between 'offer_code' / 'product_code_single_offer' / 'variation' — it only checks `$obOffer !== null`. Plan 06 must:
1. Read `$record->match_strategy` (4-literal union — present on InvoiceLine model since Phase 1 plan 01-02).
2. Branch on the strategy AFTER establishing $bMatched.
3. Add the 'variation' branch with orange styling.

Existing inline regex (lines 42-45) — DELETE in Plan 06:
```php
if (preg_match('/^(.+),\s+([^,]+)$/u', $sName, $arMatch)) {
    $sCleanName = trim($arMatch[1]);
    $sVariation = trim($arMatch[2]);
}
```

VariationExtractor (Plan 01) returns ONLY the variation token (`?string`); the partial also needs the cleaned name. Two options:
(A) Two helpers on VariationExtractor (extract + extractCleanName). PROHIBITED — Plan 01 spec is locked at 1 method.
(B) The partial calls extract() to get $sVariation; if non-null, it derives $sCleanName by `substr($sName, 0, strrpos($sName, ', '))` (last-occurrence comma-space). This is NOT a regex re-implementation; it is a pure string slice consistent with the regex semantics. CHOSEN.

InvoiceLine.matched_offer relation is a `belongsTo` to `Offer` (line 105 of InvoiceLine.php) — already eager-loaded by the stock_after column. Accessing `$record->matched_offer` is free.
</interfaces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): Render test for all 4 strategy branches</name>
  <files>tests/unit/Models/ColumnProductNameVariationBranchTest.php</files>
  <read_first>
    - models/invoiceline/_column_product_name.htm (current 71-line partial — read FULLY; the test must produce expected-output strings for all 4 branches)
    - models/invoiceline/columns.yaml (lines 11-14: the partial path declaration; the partial is invoked with `$record` populated by October's ListController; tests will simulate by setting `$record` then `require` the partial under output buffering)
    - models/InvoiceLine.php (matched_offer belongsTo relation)
    - classes/support/VariationExtractor.php (Plan 01 — the partial will consume this)
    - classes/dto/MatchedLine.php (the 4-literal union — the test casts strategy strings to MatchedLine-style values)
    - tests/GoodsReceivedTestCase.php (base class)
  </read_first>
  <behavior>
    Helper `renderPartial(InvoiceLine $obRecord): string`:
      Use ob_start(); $record = $obRecord; require __DIR__.'/../../../models/invoiceline/_column_product_name.htm'; return ob_get_clean(); (Adjust __DIR__ relative path to actually reach the partial.) Mock the `\Backend` facade BEFORE require so `Backend::url('lovata/shopaholic/products/update/42')` returns the deterministic stub URL '/back/lovata/shopaholic/products/update/42' and similarly for offers.

    Test 1 (offer_code branch — byte-for-byte unchanged):
      Build InvoiceLine stub: product_name_raw='Gel, 12ml, 1081', match_strategy='offer_code', matched_offer = stdClass{id:42, product_id:10}.
      Render. Output MUST contain:
        - `<a href="/back/lovata/shopaholic/products/update/10"` (product link)
        - `>Gel, 12ml</a>` (cleaned product name, last comma trimmed)
        - `(<a href="/back/lovata/shopaholic/offers/update/42"` (offer link wrapper)
        - `>1081</a>)` (variation in parens)
        - Output MUST NOT contain `style="color: orange"` or `class="text-warning"` (no orange styling).
        - Output MUST NOT contain a `*` asterisk (only used for variation+'none' or variation+'variation').

    Test 2 (product_code_single_offer branch — byte-for-byte unchanged):
      Same as Test 1 but match_strategy='product_code_single_offer'.
      Output MUST be identical to Test 1 byte-for-byte (snapshot equality).

    Test 3 (variation branch — NEW orange + asterisk + product name):
      Build InvoiceLine stub: product_name_raw='Top Coat, 8ml, 2002', match_strategy='variation', matched_offer = stdClass{id:99, product_id:11}.
      Render. Output MUST contain:
        - `*` (asterisk character) — Pass 3 visual distinguishment.
        - The cleaned product name 'Top Coat, 8ml' AND the variation '2002' both visible per discuss summary's "BOTH HTM-raw product name AND resolved product name visible".
        - An orange visual signal: literal substring containing 'orange' — either inline `style="color: orange"` OR `style="color:#orange"` OR `class="…orange…"` (use ONE deterministic form — see <action>).
        - A link to product update URL `/back/lovata/shopaholic/products/update/11`.
        - A link to offer update URL `/back/lovata/shopaholic/offers/update/99`.

    Test 4 (none branch — byte-for-byte unchanged):
      Build InvoiceLine stub: product_name_raw='Gel, 12ml, 1081', match_strategy='none', matched_offer = null.
      Render. Output MUST be plain text: `Gel, 12ml (1081*)` — no `<a>` tags, asterisk after variation.

    Test 5 (none branch with no comma in name):
      product_name_raw='SingleWord', match_strategy='none', matched_offer = null.
      Output MUST be exactly `SingleWord` — no parens, no asterisk, no anchor tags.

    Test 6 (variation branch with no comma in name — defensive):
      product_name_raw='SingleWord', match_strategy='variation', matched_offer = stdClass{id:99, product_id:11}.
      Output MUST contain the orange styling AND the asterisk AND a link to the product (graceful render even when VariationExtractor::extract returns null on a 'variation' line — this should not happen in practice because matcher only emits 'variation' for lines WITH a comma, but the partial must not blow up).

    Test 7 (DRY enforcement — regex grep gate):
      `grep -RFn -- "/^(.+),\\s+([^,]+)$/u" /path/to/plugin/classes /path/to/plugin/models | wc -l` MUST equal 1 — only the VariationExtractor occurrence; the inline regex in the partial MUST be GONE.
  </behavior>
  <action>
    RED step (commit 1):
    Create `tests/unit/Models/ColumnProductNameVariationBranchTest.php`. Define a Pest test extending `GoodsReceivedTestCase`. Mock the `\Backend` facade via `\Backend::shouldReceive('url')->andReturnUsing(fn (string $sPath): string => '/back/'.$sPath);`. Build InvoiceLine stubs in-memory (no DB writes — set properties via `new InvoiceLine(); $obLine->match_strategy = 'variation'; $obLine->product_name_raw = 'X, 1081';` and a stdClass for the matched_offer relation (October allows assigning relations as object properties; if not, use `$obLine->setRelation('matched_offer', $obStubOffer)`).

    Define the renderPartial helper that buffers the partial output. Write 7 it() cases per <behavior>. Run `make test` — RED for at least Tests 3, 6, and 7 (current partial does not branch on match_strategy and still has the inline regex). Tests 1, 2, 4, 5 may pass against the current partial — that is fine; they are forward-compatibility pins.

    Commit:
      `test(06-06): add failing render tests for 4-branch _column_product_name.htm partial (QA-15)`
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/goodsreceivedshopaholic &amp;&amp; ../../../vendor/bin/pest --configuration phpunit.xml --filter=ColumnProductNameVariationBranch 2&gt;&amp;1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/unit/Models/ColumnProductNameVariationBranchTest.php` exists with 7 it() blocks: `grep -c '^it(' tests/unit/Models/ColumnProductNameVariationBranchTest.php` == 7.
    - Grep -F `grep -F -c "match_strategy = 'variation'" tests/unit/Models/ColumnProductNameVariationBranchTest.php` >= 1 (variation case present).
    - Grep -F `grep -F -c "match_strategy = 'offer_code'" tests/unit/Models/ColumnProductNameVariationBranchTest.php` >= 1 (offer_code case present).
    - Grep -F `grep -F -c "match_strategy = 'product_code_single_offer'" tests/unit/Models/ColumnProductNameVariationBranchTest.php` >= 1 (product_code_single_offer case present).
    - Grep -F `grep -F -c "match_strategy = 'none'" tests/unit/Models/ColumnProductNameVariationBranchTest.php` >= 1 (none case present).
    - Grep -F `grep -F -c "/^(.+),\\\\s+([^,]+)\$/u" tests/unit/Models/ColumnProductNameVariationBranchTest.php` >= 1 (DRY-grep gate test embeds the regex literal).
    - `../../../vendor/bin/pest --filter=ColumnProductNameVariationBranch` exits NON-ZERO (RED for at least Tests 3, 6, 7).
    - 1 atomic commit `test(06-06): ...`.
  </acceptance_criteria>
  <done>
    Render-test scaffolding in place; 7 cases pin all 4 branches + DRY invariant; tests are RED until Task 2 lands; ready for partial rewrite.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Rewrite _column_product_name.htm with 4-branch dispatch + VariationExtractor consumption</name>
  <files>models/invoiceline/_column_product_name.htm</files>
  <read_first>
    - models/invoiceline/_column_product_name.htm (current 71 lines — full read; lines 42-45 are the inline regex to delete; lines 47-70 are the existing render dispatch to extend)
    - classes/support/VariationExtractor.php (Plan 01 — the call site `VariationExtractor::extract($sName)` returns ?string)
    - classes/dto/MatchedLine.php (4-literal union)
    - tests/unit/Models/ColumnProductNameVariationBranchTest.php (Task 1 — the 7 cases this task brings GREEN)
  </read_first>
  <action>
    Replace the entire body of `models/invoiceline/_column_product_name.htm` with this rewrite. Preserve the docblock header (update with the new branch description). Use `Logingrupa\GoodsReceivedShopaholic\Classes\Support\VariationExtractor` via fully-qualified namespace OR add a `use` (October partials don't auto-namespace — fully-qualified preferred for safety).

      <?php
      /**
       * Partial column: product_name_raw rendered as deep-links into the
       * upstream Lovata Shopaholic backend pages, with a 4-branch dispatch
       * on $record->match_strategy.
       *
       *   - 'offer_code' / 'product_code_single_offer' (matched):
       *       black/normal product-update link + offer-update link in parens.
       *   - 'variation' (Pass 3 matched, Phase 6):
       *       ORANGE product link + asterisk + offer-update link in parens.
       *       Visual signal that match resolved via the offer-name variation
       *       token, not directly via offer.code or product.code.
       *   - 'none' (unmatched):
       *       plain text + asterisk after variation token (no anchor tags).
       *
       * Variation extraction is delegated to VariationExtractor (DRY: the
       * regex /^(.+),\s+([^,]+)$/u appears EXACTLY ONCE in the plugin —
       * in classes/support/VariationExtractor.php, used here AND by the
       * Pass 3 chain stage VariationMatcher).
       *
       * The cleaned name is derived by string slice on the last ", " using
       * `strrpos`, consistent with the regex semantics. NOT a regex
       * re-implementation — pure substring math.
       *
       * October passes the row's model as $record. The matched_offer relation
       * is eager-loaded by the stock_after column (relation:matched_offer +
       * select:quantity).
       *
       * @var \Logingrupa\GoodsReceivedShopaholic\Models\InvoiceLine $record
       */

      use Logingrupa\GoodsReceivedShopaholic\Classes\Support\VariationExtractor;

      $sName = (string) ($record->product_name_raw ?? '');
      $sStrategy = (string) ($record->match_strategy ?? 'none');
      $obOffer = $record->matched_offer ?? null;
      $bMatched = $obOffer !== null && (int) $obOffer->id > 0;

      // Derive (cleanName, variation) using the SOLE regex source (Plan 01).
      $sVariation = VariationExtractor::extract($sName);
      $sCleanName = $sName;
      if ($sVariation !== null) {
          $iLastCommaPos = strrpos($sName, ', ');
          if ($iLastCommaPos !== false) {
              $sCleanName = trim(substr($sName, 0, $iLastCommaPos));
          }
      }

      // Branch 1: unmatched ('none').
      if (! $bMatched) {
          echo e($sCleanName);
          if ($sVariation !== null && $sVariation !== '') {
              echo ' ('.e($sVariation).'*)';
          }
          return;
      }

      $iOfferId = (int) $obOffer->id;
      $iProductId = (int) ($obOffer->product_id ?? 0);
      $sOfferUrl = Backend::url('lovata/shopaholic/offers/update/'.$iOfferId);
      $sProductUrl = $iProductId > 0
          ? Backend::url('lovata/shopaholic/products/update/'.$iProductId)
          : null;

      // Branch 2: matched via Pass 3 ('variation') — ORANGE + asterisk.
      if ($sStrategy === 'variation') {
          ?>
          <?php if ($sProductUrl !== null): ?>
              <a href="<?= e($sProductUrl) ?>" target="_blank" rel="noopener" style="color: orange"><?= e($sCleanName) ?>*</a>
          <?php else: ?>
              <span style="color: orange"><?= e($sCleanName) ?>*</span>
          <?php endif; ?>
          <?php if ($sVariation !== null && $sVariation !== ''): ?>
              (<a href="<?= e($sOfferUrl) ?>" target="_blank" rel="noopener" style="color: orange"><?= e($sVariation) ?></a>)
          <?php endif; ?>
          <?php
          return;
      }

      // Branch 3 + 4: matched via 'offer_code' or 'product_code_single_offer'
      // — black/normal links (BYTE-FOR-BYTE preserved from pre-Plan-06).
      ?>
      <?php if ($sProductUrl !== null): ?>
          <a href="<?= e($sProductUrl) ?>" target="_blank" rel="noopener"><?= e($sCleanName) ?></a>
      <?php else: ?>
          <?= e($sCleanName) ?>
      <?php endif; ?>
      <?php if ($sVariation !== null && $sVariation !== ''): ?>
          (<a href="<?= e($sOfferUrl) ?>" target="_blank" rel="noopener"><?= e($sVariation) ?></a>)
      <?php endif; ?>

    Run `make test pint-test analyse phpmd` — all green (the 7 ColumnProductNameVariationBranch tests turn GREEN; rest of suite unaffected).

    Commit:
      `feat(06-06): add 'variation' render branch + DRY-replace inline regex with VariationExtractor (UI-13)`
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/goodsreceivedshopaholic &amp;&amp; make pint-test analyse phpmd test 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `models/invoiceline/_column_product_name.htm` exists, rewritten.
    - Inline regex DELETED: `grep -F -c "preg_match('/^(.+),\\\\s+([^,]+)\$/u'" models/invoiceline/_column_product_name.htm` == 0.
    - DRY consumption added: `grep -F -c 'VariationExtractor::extract' models/invoiceline/_column_product_name.htm` == 1.
    - `use` statement: `grep -F -c 'use Logingrupa\\\\GoodsReceivedShopaholic\\\\Classes\\\\Support\\\\VariationExtractor' models/invoiceline/_column_product_name.htm` == 1.
    - 'variation' branch present: `grep -F -c "\$sStrategy === 'variation'" models/invoiceline/_column_product_name.htm` == 1.
    - Orange styling present in 'variation' branch: `grep -c 'style="color: orange"' models/invoiceline/_column_product_name.htm` >= 2 (anchor + inner anchor for variation parens).
    - Asterisk in 'variation' branch (literal `*` inside `<a>` or `<span>`): `grep -c '\*</a>\|\*</span>' models/invoiceline/_column_product_name.htm` >= 1.
    - Plugin-wide DRY invariant: `grep -RFn -- "/^(.+),\\s+([^,]+)\$/u" classes/ models/ | wc -l` == 1 (regex literal lives ONLY in classes/support/VariationExtractor.php).
    - `make test` exit 0 — all 7 ColumnProductNameVariationBranch cases GREEN; rest of suite still green.
    - `make analyse phpmd pint-test` all exit 0.
    - `sha256sum phpstan-baseline.neon` first column == `4b3227fab5b697264e8532b59f5cdd96c86a0ff1fa484cc1a869af36ae91530a`.
    - 1 atomic commit `feat(06-06): ...`.
  </acceptance_criteria>
  <done>
    The partial branches on match_strategy across all 4 literals; 'variation' branch delivers orange + asterisk + product name; inline regex DRY-replaced with VariationExtractor; baseline SHA UNCHANGED.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| InvoiceLine row → partial render | $record values come from DB (already validated by Phase 1 schema + Phase 2 parser); partial uses `e($value)` HTML-escape for every output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | T (XSS via product_name_raw) | partial output | mitigate | Every output is wrapped in `e($value)` (Laravel htmlspecialchars). The added 'variation' branch preserves this contract verbatim — no raw `<?=` of $sName / $sVariation / $sCleanName. Inline `style="color: orange"` is a literal HTML attribute (no user-controlled CSS injection). |
| T-06-06-02 | T (XSS via match_strategy drift) | dispatch on $sStrategy | accept | match_strategy is constrained by PHPStan L10 4-literal union at write time (MatchedLine ctor); even if drift slipped through, the value is never echoed unescaped. |
| T-06-06-03 | I (Information disclosure via deep-link) | Backend::url for non-permissioned operator | accept | Backend::url generates the URL only; clicking still goes through the backend permission gate of the upstream Lovata controllers. The partial does not bypass authz. |
| T-06-06-04 | T (DRY drift / regex divergence) | inline regex re-introduction | mitigate | Plan-level grep invariant in <acceptance_criteria> + plan-level DRY assertion in Task 1 Test 7 ensures the regex literal appears exactly once in the plugin (only in VariationExtractor.php). Re-introducing the inline regex would fail the make-all gate. |
</threat_model>

<verification>
- All 7 ColumnProductNameVariationBranch it() cases turn GREEN.
- DRY invariant: regex literal appears EXACTLY ONCE in the plugin (only in `classes/support/VariationExtractor.php`).
- `make all` exit 0; phpstan-baseline.neon SHA unchanged.
- All 4 branches render distinct HTML; existing branches byte-for-byte preserved against the pre-Plan-06 partial (Tests 1, 2, 4, 5).
</verification>

<success_criteria>
1. `models/invoiceline/_column_product_name.htm` branches on `$record->match_strategy` across the 4-literal union.
2. The 'variation' branch renders orange styling + asterisk + cleaned product name + offer-link in parens.
3. The 'offer_code' / 'product_code_single_offer' / 'none' branches render byte-for-byte identical to the pre-Plan-06 partial (Test 1+2+4+5 snapshot pins).
4. Inline regex `preg_match('/^(.+),\s+([^,]+)$/u', ...)` is DELETED from the partial.
5. `VariationExtractor::extract($sName)` is the SOLE regex consumer in the partial.
6. Plugin-wide grep `grep -RFn -- '/^(.+),\s+([^,]+)$/u' classes/ models/ | wc -l` == 1 (only in VariationExtractor.php).
7. `make all` exit 0; phpstan-baseline.neon SHA == `4b3227fab5b697264e8532b59f5cdd96c86a0ff1fa484cc1a869af36ae91530a`.
8. 2 atomic commits in TDD order: `test(06-06):` (RED) THEN `feat(06-06):` (GREEN).
</success_criteria>

<output>
After completion, create `.planning/phases/06-pass-3-variation-ean-matcher/06-06-SUMMARY.md`. Plan 06 closes Phase 6.
</output>
