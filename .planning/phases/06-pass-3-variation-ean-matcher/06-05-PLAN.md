---
phase: 06-pass-3-variation-ean-matcher
plan: 05
type: tdd
wave: 3
depends_on:
  - 01
  - 02
  - 03
  - 04
files_modified:
  - classes/match/EanMatcherService.php
  - classes/orchestrator/ParseAndPersistOrchestrator.php
  - tests/unit/Match/EanMatcherServiceTest.php
autonomous: true
requirements:
  - MATCH-08
  - MATCH-10
  - QA-14
must_haves:
  truths:
    - "EanMatcherService is a chain runner; orchestrates Pass 1 (OfferCodeMatcher) → Pass 2 (ProductCodeSingleOfferMatcher) → Pass 3 (VariationMatcher) over residue"
    - "Public signature is matchLines(list<ParsedLine> $arLines): list<MatchedLine> — singular method, no matchBatch shim, no buildMatchedLines"
    - "Total query budget for matchLines() is EXACTLY 3 for any non-empty input — counter-pinned by DB::enableQueryLog assertion"
    - "Output preserves input order; lines unmatched by all 3 stages emit MatchedLine with match_strategy='none'"
    - "Defense-in-depth EAN regex /^\\d{13}\\$/ guard preserved at chain runner boundary (throws InvalidEanException BEFORE any DB query)"
    - "ParseAndPersistOrchestrator.php:319 consumes new signature: $arMatched = $this->obMatcher->matchLines($obParsed->lines)"
    - "tests/unit/Match/EanMatcherServiceTest.php is REWRITTEN to test matchLines(); old matchBatch tests removed; ZERO grep occurrences of matchBatch( anywhere in classes/ + controllers/ + components/ + console/ + Plugin.php"
    - "phpstan-baseline.neon SHA UNCHANGED at 4b3227fab5b697264e8532b59f5cdd96c86a0ff1fa484cc1a869af36ae91530a"
  artifacts:
    - path: "classes/match/EanMatcherService.php"
      provides: "Chain runner orchestrating 3 MatchStrategy stages over ParsedLine residue"
      contains: "public function matchLines(array $arLines): array"
    - path: "classes/orchestrator/ParseAndPersistOrchestrator.php"
      provides: "Caller fix on the matchAllLines() helper to consume matchLines() signature"
      contains: "$this->obMatcher->matchLines($obParsed->lines)"
    - path: "tests/unit/Match/EanMatcherServiceTest.php"
      provides: "Chain integration tests pinning ≤3 queries, all 4 strategy literals, signature, leading-zero passthrough, InvalidEanException defense-in-depth"
      contains: "->matchLines("
  key_links:
    - from: "classes/match/EanMatcherService.php"
      to: "classes/match/OfferCodeMatcher.php + classes/match/ProductCodeSingleOfferMatcher.php + classes/match/VariationMatcher.php"
      via: "constructor instantiates the 3 stages; matchLines iterates the chain"
      pattern: "new OfferCodeMatcher\\(\\)|new ProductCodeSingleOfferMatcher\\(\\)|new VariationMatcher\\(\\)"
    - from: "classes/orchestrator/ParseAndPersistOrchestrator.php (line ~319)"
      to: "classes/match/EanMatcherService.php"
      via: "matchAllLines() helper now calls $this->obMatcher->matchLines($obParsed->lines)"
      pattern: "->matchLines\\("
---

<objective>
Refactor `EanMatcherService` from a 2-pass monolith with `matchBatch(array $arEans)` + `buildMatchedLines()` into a chain runner with the single public method `matchLines(list<ParsedLine>): list<MatchedLine>` orchestrating the 3 `MatchStrategy` stages built in Plans 03 + 04. Update the sole caller (`ParseAndPersistOrchestrator.php:319`). Rewrite the existing `EanMatcherServiceTest` to pin the new contract and the 3-query D-25-update budget.

Purpose: MATCH-08 (chain runner refactor + signature change) + MATCH-10 (caller fix) + QA-14 (chain integration test). NO BACK-COMPAT SHIM — the spec explicitly forbids `matchBatch` keeping a wrapper. The orchestrator is the SOLE caller (verified by grep in Plan 06 prep notes).

Output: 3 file edits — `EanMatcherService.php` (rewrite), `ParseAndPersistOrchestrator.php` (line 319 caller), `EanMatcherServiceTest.php` (rewrite).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-pass-3-variation-ean-matcher/06-CONTEXT.md
@CLAUDE.md
@classes/match/EanMatcherService.php
@classes/match/MatchStrategy.php
@classes/match/OfferCodeMatcher.php
@classes/match/ProductCodeSingleOfferMatcher.php
@classes/match/VariationMatcher.php
@classes/dto/ParsedLine.php
@classes/dto/MatchedLine.php
@classes/orchestrator/ParseAndPersistOrchestrator.php
@tests/unit/Match/EanMatcherServiceTest.php
</context>

<interfaces>
<!-- Sole caller site (line 316-322) -->

From classes/orchestrator/ParseAndPersistOrchestrator.php:
```php
private function matchAllLines(ParsedInvoice $obParsed): array
{
    $arEans = array_map(static fn ($obLine) => $obLine->ean, $obParsed->lines);
    $arMatchMap = $this->obMatcher->matchBatch($arEans);

    return $this->obMatcher->buildMatchedLines($obParsed->lines, $arMatchMap);
}
```

After Plan 05 it MUST become:
```php
private function matchAllLines(ParsedInvoice $obParsed): array
{
    return $this->obMatcher->matchLines($obParsed->lines);
}
```

The matcher constructor in the orchestrator (line 102-106) is unchanged:
```php
public function __construct(
    ?HtmInvoiceParser $obParser = null,
    ?InvoiceNumberResolver $obResolver = null,
    ?EanMatcherService $obMatcher = null,
) {
    ...
    $this->obMatcher = $obMatcher ?? new EanMatcherService();
}
```

Defense-in-depth EAN regex (Phase 2 plan 02-06 invariant — KEEP at chain runner boundary):
```php
private const string EAN_REGEX = '/^\d{13}$/';
private function assertAllEansValid(array $arEans): void { ... }
```
The `InvalidEanException` for non-13-digit EANs MUST throw BEFORE any DB query — preserved by the chain runner. Existing tests pin this contract:
- "InvalidEanException thrown BEFORE any DB query" (line ~245)
- "InvalidEanException thrown for non-numeric input" (line ~257)
</interfaces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): Rewrite EanMatcherServiceTest as chain integration suite</name>
  <files>tests/unit/Match/EanMatcherServiceTest.php</files>
  <read_first>
    - tests/unit/Match/EanMatcherServiceTest.php (READ FULLY — every existing it() case; identify which pin invariants we KEEP (leading-zero passthrough, InvalidEanException defense-in-depth) and which we REPLACE (matchBatch signature, buildMatchedLines, "exactly TWO queries"))
    - classes/match/EanMatcherService.php (current implementation — pre-refactor; defines the EanMatcherTestCase schema requirements)
    - classes/match/MatchStrategy.php
    - classes/match/OfferCodeMatcher.php
    - classes/match/ProductCodeSingleOfferMatcher.php
    - classes/match/VariationMatcher.php
    - classes/dto/ParsedLine.php
    - tests/unit/Match/VariationMatcherTest.php (Plan 04 — reuse the variation-aware hermetic schema pattern; the rewritten EanMatcherServiceTest needs the `variation` column on offers, same as Plan 04)
  </read_first>
  <behavior>
    Test 1 (chain integration: 4 strategy literals across 3-pass cascade):
      Seed offers/products covering all 3 success paths:
        - offer A: code='4752307000097' (Pass 1 hit)
        - offer B: code='OFFER-B', product code='4752307000200' single-offer (Pass 2 hit)
        - offer C: variation='1081', name='X, 1081' (Pass 3 hit; need name + variation column)
        - no seed for ean='9999999999999' (Pass 3 miss → 'none')
      Build 4 ParsedLines with the matching ean + product_name_raw.
      Call $obService->matchLines($arLines) — assert returns list<MatchedLine> length 4 with strategies in order: 'offer_code', 'product_code_single_offer', 'variation', 'none'.

    Test 2 (D-25-update query budget = 3 hard ceiling):
      Same seed as Test 1.
      DB::enableQueryLog before matchLines; count(DB::getQueryLog()) === 3.

    Test 3 (≤2 queries when Pass 3 unneeded — short-circuit):
      Seed only offer.code matches for input lines (Pass 1 catches everything).
      DB::getQueryLog count === 1 (Pass 2 + Pass 3 inputs are empty → no SELECT — preserve existing "exactly ONE query when all matched in Pass 1" semantics).

    Test 4 (leading-zero EAN preservation through chain):
      Seed offer with code='0000000012345'.
      Input ParsedLine with ean='0000000012345'.
      matchLines returns list with 1 MatchedLine; the carried line.ean === '0000000012345' (STRING preserved).

    Test 5 (InvalidEanException BEFORE any DB query — defense-in-depth):
      Input ParsedLine with ean='abcdefghijklm' (or '1234' / '12345678901234').
      DB::enableQueryLog; expect matchLines() throws InvalidEanException; count(DB::getQueryLog()) === 0.

    Test 6 (empty input → empty output, zero queries):
      matchLines([]) returns [] AND DB::getQueryLog count === 0.

    Test 7 (output order preserved):
      Input 5 ParsedLines in a specific order across all 4 strategies.
      Output list MUST be in the same order — assert by row_index sequence on the carried lines.

    Test 8 (Pass 3 single-offer guard escapes ambiguous as 'none'):
      Seed TWO offers with variation='2002'.
      Input ParsedLine with product_name_raw='Y, 2002' and ean='3333333333333' (no Pass 1 / Pass 2 match either).
      matchLines returns list with 1 MatchedLine; match_strategy === 'none' (residue from all 3 stages).
  </behavior>
  <action>
    REWRITE step (commit 1, RED):
    Open `tests/unit/Match/EanMatcherServiceTest.php` and REPLACE the entire file. Keep the `EanMatcherTestCase` abstract base class structure but ADD the `variation` column to the offers schema:

      $obTable->string('variation')->nullable();    // <-- new column for Pass 3
      $obTable->index('variation');

    REMOVE existing it() cases that test `matchBatch(array $arEans): array<string, MatchResult>` and `buildMatchedLines(...)` shapes (these methods will no longer exist). KEEP the `seedProduct()` and `seedOffer()` helpers and ADD a `seedOfferWithVariation(int $iProductId, string $sVariation, ?string $sCode = null, string $sName = 'X, …'): Offer` helper.

    Write 8 it() cases per <behavior>. Use the type `Logingrupa\GoodsReceivedShopaholic\Classes\Dto\ParsedLine` to construct inputs with all 9 required fields (row_index, ean, product_name_raw, unit, qty, unit_price, discount, line_price, total). Run `make test` — RED (matchLines doesn't exist yet, signature error). Commit:
      `test(06-05): rewrite EanMatcherServiceTest as chain integration with ≤3-query pin (QA-14)`
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/goodsreceivedshopaholic &amp;&amp; ../../../vendor/bin/pest --configuration phpunit.xml --filter=EanMatcherServiceTest 2&gt;&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/unit/Match/EanMatcherServiceTest.php` exists.
    - Grep `grep -c '^it(' tests/unit/Match/EanMatcherServiceTest.php` == 8.
    - Grep -F `grep -F -c '->matchLines(' tests/unit/Match/EanMatcherServiceTest.php` >= 5 (most tests call the new signature).
    - Grep -F `grep -F -c '->matchBatch(' tests/unit/Match/EanMatcherServiceTest.php` == 0 (old signature gone).
    - Grep -F `grep -F -c '->buildMatchedLines(' tests/unit/Match/EanMatcherServiceTest.php` == 0 (old helper gone).
    - Grep `grep -c 'expect.*toBe(3)' tests/unit/Match/EanMatcherServiceTest.php` >= 1 (3-query budget pin).
    - Grep `grep -c "->variation" tests/unit/Match/EanMatcherServiceTest.php` >= 1 (variation column referenced in seed helper).
    - Grep -F `grep -F -c "\$obTable->string('variation')->nullable()" tests/unit/Match/EanMatcherServiceTest.php` == 1 (hermetic schema includes the variation column).
    - `../../../vendor/bin/pest --filter=EanMatcherServiceTest` exits NON-ZERO (the test file refers to `matchLines` which doesn't exist yet — RED phase confirmed).
    - 1 atomic commit `test(06-05): rewrite EanMatcherServiceTest ...`.
  </acceptance_criteria>
  <done>
    EanMatcherServiceTest has been rewritten with 8 cases pinning chain integration; tests are RED because `matchLines()` does not exist yet; ready for Task 2 (refactor) to bring them GREEN.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Refactor EanMatcherService into chain runner + caller fix</name>
  <files>classes/match/EanMatcherService.php, classes/orchestrator/ParseAndPersistOrchestrator.php</files>
  <read_first>
    - classes/match/EanMatcherService.php (current 268-line implementation — read FULLY; identify which behavior carries over: assertAllEansValid + EAN_REGEX const + InvalidEanException throw)
    - classes/match/MatchStrategy.php
    - classes/match/OfferCodeMatcher.php
    - classes/match/ProductCodeSingleOfferMatcher.php
    - classes/match/VariationMatcher.php
    - classes/dto/ParsedLine.php
    - classes/dto/MatchedLine.php
    - classes/orchestrator/ParseAndPersistOrchestrator.php (lines 80-110 constructor + lines 310-322 matchAllLines; the line numbering may drift — locate by grep for 'matchBatch' and 'buildMatchedLines')
    - tests/unit/Match/EanMatcherServiceTest.php (Task 1 output — the 8 cases this task brings GREEN)
  </read_first>
  <action>
    REWRITE step (commit 2, GREEN):
    Replace the entirety of `classes/match/EanMatcherService.php` with the chain runner shape below. Preserve the `EAN_REGEX` const + `assertAllEansValid()` private method byte-for-byte (defense-in-depth gate). Drop `matchBatch`, `buildMatchedLines`, `lookupOffersByCode`, `lookupProductsWithSingleOffer`, `assembleResultMap`. Add the new `matchLines()` chain runner public method.

      <?php

      declare(strict_types=1);

      namespace Logingrupa\GoodsReceivedShopaholic\Classes\Match;

      use Logingrupa\GoodsReceivedShopaholic\Classes\Dto\MatchedLine;
      use Logingrupa\GoodsReceivedShopaholic\Classes\Dto\ParsedLine;
      use Logingrupa\GoodsReceivedShopaholic\Classes\Exception\InvalidEanException;

      /**
       * EAN match chain runner — Phase 6 / D-25-update.
       *
       * Orchestrates 3 MatchStrategy stages over ParsedLine residue:
       *   1. OfferCodeMatcher                — Offer.code direct match (1 query)
       *   2. ProductCodeSingleOfferMatcher   — Product.code with single-offer guard (1 query)
       *   3. VariationMatcher                — Offer.variation last-comma-segment with single-offer guard (1 query)
       *
       * Total query budget = 3 (one per stage, regardless of input size).
       * Counter-pinned by EanMatcherServiceTest::it_runs_at_most_3_queries.
       *
       * Public surface:
       *   matchLines(list<ParsedLine>): list<MatchedLine>
       *
       * Defense-in-depth: every EAN is regex-validated against /^\d{13}$/ BEFORE
       * any DB query (T-02-06-01 mitigation; preserved from Phase 2 plan 02-06).
       *
       * Hungarian notation throughout per Lovata Toolbox standard.
       */
      final class EanMatcherService
      {
          private const string EAN_REGEX = '/^\d{13}$/';

          /**
           * @var list<MatchStrategy>  Chain stages in deterministic execution order.
           */
          private readonly array $arStages;

          public function __construct()
          {
              $this->arStages = [
                  new OfferCodeMatcher(),
                  new ProductCodeSingleOfferMatcher(),
                  new VariationMatcher(),
              ];
          }

          /**
           * Run the EAN match chain over a list of ParsedLines.
           *
           * Each stage receives only the residue unmatched by prior stages.
           * Lines unmatched by ALL three stages emerge as MatchedLine with
           * match_strategy='none'. Output order matches input order.
           *
           * @param  list<ParsedLine>  $arLines
           * @return list<MatchedLine>
           *
           * @throws InvalidEanException  on any input line whose EAN is not exactly 13 digits.
           */
          public function matchLines(array $arLines): array
          {
              if ($arLines === []) {
                  return [];
              }

              $arEans = array_map(static fn (ParsedLine $obLine): string => $obLine->ean, $arLines);
              $this->assertAllEansValid($arEans);

              // Run the chain: each stage filters residue from prior stages.
              $arMatched = [];     // list<MatchedLine>  (accumulated per-stage matches)
              $arRemaining = $arLines;

              foreach ($this->arStages as $obStage) {
                  if ($arRemaining === []) {
                      break;
                  }
                  $arStageMatches = $obStage->match($arRemaining);
                  if ($arStageMatches === []) {
                      continue;
                  }
                  $arMatched = array_merge($arMatched, $arStageMatches);
                  $arMatchedRowIndices = [];
                  foreach ($arStageMatches as $obMatched) {
                      $arMatchedRowIndices[$obMatched->line->row_index] = true;
                  }
                  $arRemaining = array_values(array_filter(
                      $arRemaining,
                      static fn (ParsedLine $obLine): bool => ! isset($arMatchedRowIndices[$obLine->row_index]),
                  ));
              }

              // Wrap residue (lines unmatched by all stages) as 'none'.
              foreach ($arRemaining as $obLine) {
                  $arMatched[] = new MatchedLine(
                      line: $obLine,
                      matched_offer_id: null,
                      match_strategy: 'none',
                  );
              }

              // Restore input order (chain stages may have produced matches out of order).
              return $this->reorderToInput($arLines, $arMatched);
          }

          /**
           * Defense-in-depth EAN regex guard. Throws BEFORE any DB query so an
           * invalid EAN cannot reach an IN-clause builder (T-02-06-01).
           *
           * @param  list<string>  $arEans
           *
           * @throws InvalidEanException
           */
          private function assertAllEansValid(array $arEans): void
          {
              foreach ($arEans as $sEan) {
                  if (preg_match(self::EAN_REGEX, $sEan) === 1) {
                      continue;
                  }
                  throw new InvalidEanException(
                      (string) \Lang::get('logingrupa.goodsreceivedshopaholic::lang.exception.invalid_ean'),
                      ['raw' => $sEan, 'expected_format' => '13 digits'],
                  );
              }
          }

          /**
           * Reorder MatchedLine results to match the order of input ParsedLines
           * (keyed by row_index). Stages may match out of order; the contract
           * for downstream `persistLines` (orchestrator) is input-order
           * preservation.
           *
           * @param  list<ParsedLine>  $arInput
           * @param  list<MatchedLine>  $arUnordered
           * @return list<MatchedLine>
           */
          private function reorderToInput(array $arInput, array $arUnordered): array
          {
              $arByRowIndex = [];
              foreach ($arUnordered as $obMatched) {
                  $arByRowIndex[$obMatched->line->row_index] = $obMatched;
              }
              $arResult = [];
              foreach ($arInput as $obLine) {
                  if (isset($arByRowIndex[$obLine->row_index])) {
                      $arResult[] = $arByRowIndex[$obLine->row_index];
                  }
              }
              return $arResult;
          }
      }

    Now edit `classes/orchestrator/ParseAndPersistOrchestrator.php`. Locate the `matchAllLines()` private method (currently lines ~316-322; find via `grep -n 'private function matchAllLines'`). Replace its body with:

      private function matchAllLines(ParsedInvoice $obParsed): array
      {
          return $this->obMatcher->matchLines($obParsed->lines);
      }

    Drop the local `$arEans` + `$arMatchMap` variables and the second `buildMatchedLines` call. The method body shrinks from 4 lines to 1.

    Run `make test pint-test analyse phpmd` — all 8 EanMatcherServiceTest cases must turn GREEN; existing ParseAndPersistOrchestratorTest + downstream Apply tests still GREEN. Commit:
      `feat(06-05): refactor EanMatcherService into 3-stage chain runner + caller fix (MATCH-08, MATCH-10)`
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/goodsreceivedshopaholic &amp;&amp; make pint-test analyse phpmd test 2&gt;&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File `classes/match/EanMatcherService.php` exists, rewritten.
    - Grep `grep -c 'final class EanMatcherService' classes/match/EanMatcherService.php` == 1.
    - Grep -F `grep -F -c 'public function matchLines(array $arLines): array' classes/match/EanMatcherService.php` == 1 (new public signature).
    - Grep -F `grep -F -c 'public function matchBatch' classes/match/EanMatcherService.php` == 0 (old signature deleted; no shim).
    - Grep -F `grep -F -c 'public function buildMatchedLines' classes/match/EanMatcherService.php` == 0 (helper deleted).
    - Grep -F `grep -F -c 'new OfferCodeMatcher()' classes/match/EanMatcherService.php` == 1.
    - Grep -F `grep -F -c 'new ProductCodeSingleOfferMatcher()' classes/match/EanMatcherService.php` == 1.
    - Grep -F `grep -F -c 'new VariationMatcher()' classes/match/EanMatcherService.php` == 1.
    - Defense-in-depth EAN regex preserved: `grep -F -c "'/^\\\\d{13}\$/'" classes/match/EanMatcherService.php` == 1.
    - assertAllEansValid preserved: `grep -c 'private function assertAllEansValid' classes/match/EanMatcherService.php` == 1.
    - Caller fix in orchestrator: `grep -F -c '$this->obMatcher->matchLines($obParsed->lines)' classes/orchestrator/ParseAndPersistOrchestrator.php` == 1.
    - Old caller GONE: `grep -F -c '$this->obMatcher->matchBatch' classes/orchestrator/ParseAndPersistOrchestrator.php` == 0.
    - Old caller GONE: `grep -F -c '$this->obMatcher->buildMatchedLines' classes/orchestrator/ParseAndPersistOrchestrator.php` == 0.
    - Repo-wide `matchBatch(` survey: `grep -RFn 'matchBatch(' classes/ controllers/ components/ console/ Plugin.php 2>/dev/null | wc -l` == 0 (no production code references the dropped method).
    - `make test` exit 0 — all 8 new EanMatcherServiceTest cases GREEN; existing ParseAndPersistOrchestratorTest + downstream Apply suite GREEN.
    - 3-query budget pinned: at least one test asserts query count === 3.
    - `make analyse phpmd pint-test` all exit 0.
    - `sha256sum phpstan-baseline.neon` first column == `4b3227fab5b697264e8532b59f5cdd96c86a0ff1fa484cc1a869af36ae91530a`.
    - 1 atomic commit `feat(06-05): refactor EanMatcherService ...`.
  </acceptance_criteria>
  <done>
    EanMatcherService is a chain runner with the matchLines signature; orchestrator caller fixed; 3-query budget counter-pinned; Pass 3 'variation' strategy literal end-to-end; baseline SHA UNCHANGED.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ParsedLine list → chain runner | Already-parsed and EAN-format-validated upstream by HtmInvoiceParser; chain runner re-asserts EAN regex defense-in-depth before delegating to any stage. |
| MatchStrategy stage → next stage | Internal type contract under PHPStan L10; residue calculation is pure (filter by row_index set). |
| chain runner output → ParseAndPersistOrchestrator persist | Validated by `MatchedLine` 4-literal union; PHPStan L10 catches any leak of an out-of-union literal at compile time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | T (Tampering) | EAN regex defense-in-depth at chain runner boundary | mitigate | `assertAllEansValid` is preserved verbatim from Phase 2 — runs BEFORE any chain stage SELECT. Pinned by EanMatcherServiceTest "InvalidEanException BEFORE any DB query" case (Test 5). |
| T-06-05-02 | I (Information disclosure via order leak) | reorderToInput post-stage | accept | Output order matches input order; chain stages may match out of order but final order is input-canonical. No information leak — order does not encode anything sensitive. |
| T-06-05-03 | T (Caller signature drift) | ParseAndPersistOrchestrator caller fix | mitigate | Only ONE caller exists in repo (`grep -RFn 'matchBatch(' classes/` returns 0 after Plan 05). PHPStan L10 would catch any new caller drift in subsequent phases. |
| T-06-05-04 | D (Denial of service via residue explosion) | Chain stage residue forwarding | accept | Residue size monotonically decreases (each stage either matches or omits). Worst case is a 200-line invoice with all lines at Pass 3 — bounded by parser's MAX_ROWS=10000 guard. |
</threat_model>

<verification>
- All 8 EanMatcherServiceTest cases turn GREEN.
- DB::enableQueryLog asserts 3-query budget for full-cascade input (Test 2), 1-query short-circuit when Pass 1 catches everything (Test 3), 0 queries on InvalidEan (Test 5) and empty input (Test 6).
- Existing ParseAndPersistOrchestratorTest + Apply suite continue passing — orchestrator behavior unchanged at the public boundary.
- `make all` exit 0; phpstan-baseline.neon SHA unchanged.
- Production code under `classes/ controllers/ components/ console/ Plugin.php` contains zero `matchBatch(` references.
</verification>

<success_criteria>
1. `classes/match/EanMatcherService.php` is a chain runner with `matchLines(list<ParsedLine>): list<MatchedLine>` as the single public method; `matchBatch` and `buildMatchedLines` are deleted.
2. Defense-in-depth EAN regex `/^\d{13}$/` + `assertAllEansValid` + `InvalidEanException` are preserved at the chain runner boundary.
3. `ParseAndPersistOrchestrator.php` calls `$this->obMatcher->matchLines($obParsed->lines)` exactly once (the line ~319 caller fix).
4. `tests/unit/Match/EanMatcherServiceTest.php` has 8 it() cases pinning chain integration, 3-query budget, all 4 strategy literals, leading-zero passthrough, InvalidEanException defense-in-depth, empty input, order preservation, ambiguous-Pass-3 → 'none'.
5. `make all` exit 0; phpstan-baseline.neon SHA == `4b3227fab5b697264e8532b59f5cdd96c86a0ff1fa484cc1a869af36ae91530a`.
6. Repo-wide `grep -RFn 'matchBatch(' classes/ controllers/ components/ console/ Plugin.php | wc -l` == 0.
7. 2 atomic commits in TDD order: `test(06-05):` (RED) THEN `feat(06-05):` (GREEN).
</success_criteria>

<output>
After completion, create `.planning/phases/06-pass-3-variation-ean-matcher/06-05-SUMMARY.md`.
</output>
