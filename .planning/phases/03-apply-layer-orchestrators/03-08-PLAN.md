---
phase: 03-apply-layer-orchestrators
plan: 08
type: execute
wave: 5
depends_on: [03-07]
files_modified:
  - .planning/phases/03-apply-layer-orchestrators/03-08-SUMMARY.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "make all green: pint-test + lint-settings-accessor + analyse + phpmd + test all pass"
    - "phpstan-baseline.neon SHA unchanged from Phase 2 close (4b3227fa…) — Phase 3 introduced zero baseline-suppressed errors"
    - "Every Phase 3 requirement APPLY-01..10 + QA-03/04/05/06/08/09 has a SUMMARY entry confirming closure"
    - "QA-09 grep gate (`make lint-settings-accessor`) returns exit 0; Settings::get( appears in EXACTLY one file (classes/support/SettingsAccessor.php)"
    - "QA-04 cache-flush count is concretely measured and recorded in the final SUMMARY (not hand-waved)"
    - "REQUIREMENTS.md traceability table updated — all 16 Phase 3 entries marked Closed with the closing plan reference"
    - "STATE.md updated to reflect Phase 3 COMPLETE (3/5 phases, 23/23 plans)"
  artifacts:
    - path: .planning/phases/03-apply-layer-orchestrators/03-08-SUMMARY.md
      provides: "Final phase 3 QA gate report — pasted output of make all, baseline SHA, requirement closure table"
      min_lines: 60
    - path: .planning/REQUIREMENTS.md
      provides: "Updated traceability table marking 16 phase 3 entries Closed with date + plan ref"
    - path: .planning/STATE.md
      provides: "Updated metadata: completed_phases=3, completed_plans=23, status block reflects Phase 3 done"
    - path: .planning/ROADMAP.md
      provides: "Phase 3 progress row marked Complete with completion date"
  key_links:
    - from: 03-08 Plan
      to: All Phase 3 plans (03-01 through 03-07)
      via: "Reads each plan's SUMMARY.md and confirms requirement IDs are closed"
      pattern: "APPLY-\\d{2}|QA-\\d{2}"
---

<objective>
Final QA gate for Phase 3. Run `make all`, capture exit code + duration, verify `phpstan-baseline.neon` SHA unchanged, confirm every Phase 3 requirement has a SUMMARY-level closure note, write the phase-final SUMMARY, update REQUIREMENTS.md / STATE.md / ROADMAP.md.

Purpose: Phase 1 and Phase 2 each ended with a final QA gate plan (01-08 and 02-07). The gate catches drift introduced by parallel plans in earlier waves AND provides a single clean checkpoint for the orchestrator to confirm phase complete before moving to Phase 4.

Output: 4 doc artifacts: 03-08-SUMMARY.md (new), REQUIREMENTS.md (edits), STATE.md (edits), ROADMAP.md (edits). NO new production code. NO new test code. May regenerate phpstan-baseline.neon ONLY if a legitimate Phase 3-introduced finding requires baseline-level acceptance — in which case the diff is documented in the SUMMARY (per Phase 2 plan 02-07 precedent: phpmd.xml threshold tunes were documented).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-apply-layer-orchestrators/03-CONTEXT.md
@.planning/phases/03-apply-layer-orchestrators/03-01-SUMMARY.md
@.planning/phases/03-apply-layer-orchestrators/03-02-SUMMARY.md
@.planning/phases/03-apply-layer-orchestrators/03-03-SUMMARY.md
@.planning/phases/03-apply-layer-orchestrators/03-04-SUMMARY.md
@.planning/phases/03-apply-layer-orchestrators/03-05-SUMMARY.md
@.planning/phases/03-apply-layer-orchestrators/03-06-SUMMARY.md
@.planning/phases/03-apply-layer-orchestrators/03-07-SUMMARY.md
@.planning/phases/02-pure-parsers-dtos-exceptions-ean-matcher/02-07-SUMMARY.md
@phpstan-baseline.neon
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run make all + capture output + verify Phase 3 gates</name>
  <files>.planning/phases/03-apply-layer-orchestrators/03-08-SUMMARY.md</files>
  <action>
    1) Run the full QA pipeline and capture output:
       ```bash
       cd /home/forge/nailscosmetics.lv/plugins/logingrupa/goodsreceivedshopaholic
       time make all 2>&1 | tee /tmp/03-08-make-all.log
       echo "---"
       sha256sum phpstan-baseline.neon
       echo "---"
       grep -rn 'Settings::get(' classes/ components/ models/ Plugin.php 2>/dev/null | grep -v 'classes/support/SettingsAccessor.php' || echo "[QA-09 OK: no offenders]"
       echo "---"
       wc -l \
         classes/apply/StockApplyService.php \
         classes/apply/StockApplyOutcome.php \
         classes/apply/ActiveFlagService.php \
         classes/apply/InitialResetService.php \
         classes/orchestrator/ParseAndPersistOrchestrator.php \
         classes/orchestrator/ApplyOrchestrator.php \
         classes/support/SettingsAccessor.php \
         classes/support/ImportAuditService.php
       ```

    2) Inspect the test count: grep the make all output for "Tests:" or Pest's summary line; record the number of tests that pass and total assertions. Compare to Phase 2 close (which showed 92/92 tests + 264 assertions). Phase 3 should add ~30-50 tests (rough budget: 7 SettingsAccessor + 1 grep gate + 5 ImportAudit + 8+2 StockApply + 7 ActiveFlag + 1 SkipsManuallyDeactivated + 5 InitialReset + 5 ParseAndPersist + 1 ApplyOrch happy + 4 QA-03 dedicated + 2 QA-08 dedicated = ~48 new tests → expected total ~140 tests).

    3) Verify SHA on baseline:
       - Phase 2 close recorded sha256 prefix `4b3227fa…` in STATE.md (line 28).
       - Paste current SHA AND compare. If unchanged: PASS. If changed: investigate. Either revert the new entries (preferred — Phase 3 should add NO new baseline-suppressed errors) OR document the legitimate addition with rationale (precedent: Phase 2 plan 02-07 accepted phpmd.xml tunes for canonical DTO field names).

    4) Verify QA-09 grep gate one more time (belt + suspenders):
       ```bash
       grep -rn 'Settings::get(' classes/ components/ models/ Plugin.php 2>/dev/null | grep -v 'classes/support/SettingsAccessor.php' || echo NO-OFFENDERS
       make lint-settings-accessor && echo "GATE OK"
       ```
       Both MUST return success indicators (no offenders + GATE OK).

    5) Confirm requirement closure. For each of the 16 Phase 3 requirements, verify in the per-plan SUMMARY:

       | REQ | Closing Plan | Confirmed (Y/N) |
       |-----|--------------|-----------------|
       | APPLY-01 (StockApply saveQuietly) | 03-03 | |
       | APPLY-02 (Batched cache flush) | 03-03 | |
       | APPLY-03 (ActiveFlag provenance skip) | 03-04 | |
       | APPLY-04 (ActiveFlag matrix) | 03-04 | |
       | APPLY-05 (InitialReset) | 03-05 | |
       | APPLY-06 (ParseAndPersist) | 03-06 | |
       | APPLY-07 (ApplyOrchestrator) | 03-07 | |
       | APPLY-08 (Override-reimport — parse + apply sides) | 03-06 + 03-07 | |
       | APPLY-09 (SettingsAccessor) | 03-01 | |
       | APPLY-10 (ImportAuditService) | 03-02 | |
       | QA-03 (Idempotency 4 tests) | 03-07 | |
       | QA-04 (200-line cache flush) | 03-03 | |
       | QA-05 (ActiveFlag matrix + skip-operator) | 03-04 | |
       | QA-06 (InitialReset 5 tests) | 03-05 | |
       | QA-08 (Partial rollback + ActiveFlag-in-tx) | 03-07 | |
       | QA-09 (SettingsAccessor sole consumer) | 03-01 | |

    6) Write `.planning/phases/03-apply-layer-orchestrators/03-08-SUMMARY.md` with the following sections:

       a) **Phase 3 Final QA Gate Report** — header section
       b) **make all output**
          - Exit code (must be 0)
          - Duration (record actual)
          - Pint output (must show 0 style issues)
          - PHPStan output (0 errors at level 10)
          - PHPMD output (0 violations)
          - Test count: `XX/YY tests, NNN assertions, all green` (filled in from actual run)
       c) **QA-04 concrete cache-flush measurement** — paste from 03-03 SUMMARY: list-store flush count + 200-line apply query count
       d) **QA-09 grep gate result** — paste output of `make lint-settings-accessor` and the grep command
       e) **File line counts table** — all 8 Phase 3 production files with LoC
       f) **phpstan-baseline.neon SHA**
          - BEFORE: `4b3227fa…` (Phase 2 recorded)
          - AFTER: `<measured>`
          - Status: unchanged | changed-with-rationale
       g) **16-row requirement closure table** (Y/N column from item 5 above)
       h) **Phase 3 status statement**: "Phase 3 status: COMPLETE — orchestrator may proceed to Phase 4 (Backend Controller, Upload/Preview/Apply UI, Console)"
       i) **Phase 3 cumulative artifacts**: count of new production files (8), test files (~13), total LoC added (sum of wc -l)

    7) Update `.planning/REQUIREMENTS.md` traceability table (lines ~169-205) — for each Phase 3 row, change `Pending` to `Closed (2026-04-29) — plan NN-NN`:
       ```
       APPLY-01 | Phase 3 | Closed (2026-04-29) — plan 03-03
       APPLY-02 | Phase 3 | Closed (2026-04-29) — plan 03-03
       APPLY-03 | Phase 3 | Closed (2026-04-29) — plan 03-04
       APPLY-04 | Phase 3 | Closed (2026-04-29) — plan 03-04
       APPLY-05 | Phase 3 | Closed (2026-04-29) — plan 03-05
       APPLY-06 | Phase 3 | Closed (2026-04-29) — plan 03-06
       APPLY-07 | Phase 3 | Closed (2026-04-29) — plan 03-07
       APPLY-08 | Phase 3 | Closed (2026-04-29) — plans 03-06 + 03-07
       APPLY-09 | Phase 3 | Closed (2026-04-29) — plan 03-01
       APPLY-10 | Phase 3 | Closed (2026-04-29) — plan 03-02
       QA-03    | Phase 3 | Closed (2026-04-29) — plan 03-07
       QA-04    | Phase 3 | Closed (2026-04-29) — plan 03-03
       QA-05    | Phase 3 | Closed (2026-04-29) — plan 03-04
       QA-06    | Phase 3 | Closed (2026-04-29) — plan 03-05
       QA-08    | Phase 3 | Closed (2026-04-29) — plan 03-07
       QA-09    | Phase 3 | Closed (2026-04-29) — plan 03-01
       ```
       Also update the active checkboxes from `[ ]` to `[x]` for each Phase 3 requirement in the body sections (lines 48-57 for APPLY + lines 87-93 for QA).

    8) Update `.planning/STATE.md`:
       - Phase: 3 of 5 (Apply Layer + Orchestrators) — COMPLETE
       - Status block (line 28 area): "All 16 Phase 3 requirements satisfied — APPLY-01..10 + QA-03/04/05/06/08/09. Phase 3 final QA gate (03-08) verified `make all` green: NN/MM tests passed (PPP assertions), PHPStan L10 clean, Pint clean, PHPMD clean, lint-settings-accessor clean (Settings::get( appears in 1 file). phpstan-baseline.neon sha256 unchanged."
       - Last activity (line 29 area): "2026-04-29 — plan 03-08 complete. Phase 3 COMPLETE. Next: Phase 4 (Backend Controller, Upload/Preview/Apply UI, Console)."
       - Last session (line 84): 2026-04-29
       - Stopped at (line 85): "Phase 3 COMPLETE — all 8 plans closed, all 16 requirements satisfied, make all green. Next: Phase 4 plan 04-01 kickoff."
       - Resume file (line 86): `.planning/phases/03-apply-layer-orchestrators/03-08-SUMMARY.md`
       - Frontmatter:
         - `progress.completed_phases: 3`
         - `progress.total_plans: 23`
         - `progress.completed_plans: 23`
         - `stopped_at: "Phase 3 COMPLETE (all 8 plans, all 16 requirements APPLY-01..10 + QA-03..06 + QA-08 + QA-09 closed). Next: Phase 4 plan 04-01 (Backend Controller kickoff)."`
         - `last_updated: "2026-04-29T<...>"`
       - Update progress bar: `[██████░░░░] 41%` (3/5 phases × 100% truncated to bar segments — pick what scaled correctly; precedent in line 31 was 21% for 1.5/5 progress).

    9) Update `.planning/ROADMAP.md` Progress table (lines ~117-129):
       ```
       | 3. Apply Layer + Orchestrators | 8/8 | Complete | 2026-04-29 |
       ```
       Also flip the phase checkbox from `[ ]` to `[x]` in the phase list (line 23).

  </action>
  <verify>
    <automated>make all && grep -rn 'Settings::get(' classes/ components/ models/ Plugin.php 2>/dev/null | grep -v 'classes/support/SettingsAccessor.php' | wc -l | grep -q '^0$' && test -f .planning/phases/03-apply-layer-orchestrators/03-08-SUMMARY.md</automated>
  </verify>
  <done>
    - `make all` exits 0
    - `make lint-settings-accessor` exits 0
    - `grep -rn 'Settings::get(' classes/ components/ models/ Plugin.php | grep -v 'classes/support/SettingsAccessor.php'` returns no lines
    - `phpstan-baseline.neon` SHA matches Phase 2 close (`4b3227fa…`) OR a documented diff
    - `.planning/phases/03-apply-layer-orchestrators/03-08-SUMMARY.md` exists with the 9 sections from item 6
    - `.planning/REQUIREMENTS.md` shows all 16 Phase 3 rows with `Closed (2026-04-29) — plan NN-NN`
    - `.planning/STATE.md` shows completed_phases: 3 + completed_plans: 23
    - `.planning/ROADMAP.md` shows Phase 3 row Complete with date
    - All 16 Phase 3 requirements have closure-confirmation rows (Y/N table all Y)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 3 plans → final-gate verification | Drift between parallel-wave plans (03-01 through 03-07) lands here; this plan is the integration check |
| Documentation state → Phase 4 kickoff | STATE.md / REQUIREMENTS.md / ROADMAP.md are the resume contract for the next session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08-01 | Tampering | Phase 3 silently regresses Phase 2 tests | mitigate | `make all` runs the FULL Pest suite (Phase 1 + 2 + 3 tests) — any regression blocks the gate |
| T-03-08-02 | Repudiation | A requirement marked Closed but no test backs it | mitigate | The 16-row closure table in 03-08-SUMMARY.md cross-references each requirement to a closing plan's SUMMARY which itself cites concrete tests + grep gates |
| T-03-08-03 | Tampering | New phpstan baseline entries hide level-10 errors | mitigate | SHA comparison against Phase 2 close. Any change requires explicit rationale in the SUMMARY (precedent: Phase 2 plan 02-07 accepted phpmd.xml tunes with documented justification) |
| T-03-08-04 | Information disclosure | Resume context drift between sessions | mitigate | STATE.md updated with concrete next-step plan reference (04-01 kickoff) so the next session immediately knows what to plan |
</threat_model>

<verification>
- `make all` exits 0
- `make lint-settings-accessor` exits 0
- All 16 Phase 3 REQs marked Closed in REQUIREMENTS.md traceability table
- 03-08-SUMMARY.md exists with all 9 sections
- STATE.md frontmatter `completed_phases: 3`, `completed_plans: 23`
- ROADMAP.md Phase 3 row marked Complete
</verification>

<success_criteria>
1. `make all` green; phpstan-baseline.neon SHA unchanged (or documented diff)
2. QA-09 grep gate clean: `Settings::get(` appears in EXACTLY one file
3. 03-08-SUMMARY.md written with concrete numbers (test count, line counts, baseline SHA, cache flush count)
4. REQUIREMENTS.md traceability updated: all 16 Phase 3 entries Closed
5. STATE.md reflects Phase 3 COMPLETE
6. ROADMAP.md Progress table updated
7. Orchestrator can immediately move to `/gsd-discuss-phase` for Phase 4 OR `/gsd-plan-phase` if Phase 4 already has a CONTEXT.md
</success_criteria>

<output>
After completion, the file `.planning/phases/03-apply-layer-orchestrators/03-08-SUMMARY.md` IS the deliverable. The 03-08-SUMMARY itself is the success artifact (no separate execute-plan SUMMARY needed for a QA-gate-only plan; this plan IS its own summary).
</output>
