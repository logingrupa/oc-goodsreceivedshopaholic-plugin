---
phase: 04-backend-controller-upload-preview-apply-ui-console
plan: 08
type: execute
wave: 7
depends_on: [04-01, 04-02, 04-03, 04-04, 04-05, 04-06, 04-07]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - phpstan-baseline.neon
autonomous: true
requirements: []
must_haves:
  truths:
    - "`make all` exits 0 (pint-test + lint-settings-accessor + analyse + phpmd + test all green)."
    - "phpstan-baseline.neon SHA UNCHANGED relative to Phase 3 close (4b3227fab5b697264e8532b59f5cdd96c86a0ff1fa484cc1a869af36ae91530a)."
    - "REQUIREMENTS.md traceability table reflects 13 Phase 4 entries flipped to `Closed (YYYY-MM-DD) — plan NN-NN`."
    - "ROADMAP.md Phase 4 row marked Complete with completion timestamp."
    - "STATE.md current position points at Phase 5 plan 05-01 kickoff."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "13 Phase 4 entries Closed in traceability table"
      contains: "Closed (2026-04-29)"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4 row Complete + plan list with 8 plans"
    - path: ".planning/STATE.md"
      provides: "current position updated; Phase 5 next"
    - path: "phpstan-baseline.neon"
      provides: "SHA unchanged"
      min_lines: 1
  key_links: []
---

<objective>
Phase 4 final QA gate — same shape as Phase 2 plan 02-07 and Phase 3 plan 03-08. NO production code in this plan; this is a pure verification + documentation pass.

Purpose: confirm Phase 4 introduced ZERO regressions in the static-analysis baseline + locks the traceability artifacts so subsequent phases / milestone close can read them as authoritative.

Output:
- `make all` exit 0 verified.
- phpstan-baseline.neon SHA verified UNCHANGED at the Phase 3 close hash.
- REQUIREMENTS.md / ROADMAP.md / STATE.md updated.
- Phase 4 SUMMARY ships the closure table for UI-01..UI-12 + QA-10 cross-referenced to plan + test pin.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@.planning/phases/04-backend-controller-upload-preview-apply-ui-console/04-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run `make all` from a fresh invocation; verify baseline SHA unchanged</name>
  <files>(verification only — no code edits in this task)</files>
  <action>
A) From the plugin directory, run the full pipeline:

```bash
cd plugins/logingrupa/goodsreceivedshopaholic
make all
```

Confirm exit code 0. Capture wall-clock + Pest counts (test count, assertion count) for the SUMMARY. The expected counts (Phase 3 close + Phase 4 additions):
- Phase 3 closed at 145 tests / 708 assertions.
- Phase 4 plans 04-01..04-07 add roughly:
  - 04-01: 6 it() cases (parseIniSize 2 + boot warn 4)
  - 04-02: 5 it() cases
  - 04-03: 7 it() cases (controller structure)
  - 04-04: ~14 it() cases (5 upload + 4 dup + 5 update_line)
  - 04-05: ~10 it() cases (6 apply + 4 debounce)
  - 04-06: ~13 it() cases (6 override + 7 reset)
  - 04-07: 12 it() cases (4 permission gates × 3)
  - **Total Phase 4 new: ~67 it() cases**, expected end-of-Phase-4 count: ~212 tests / ~900-1000 assertions.

Numbers are approximate — actual counts depend on inline sub-asserts. Whatever the actual count, record it.

B) Verify phpstan-baseline.neon SHA:

```bash
sha256sum plugins/logingrupa/goodsreceivedshopaholic/phpstan-baseline.neon
```

Expected: `4b3227fab5b697264e8532b59f5cdd96c86a0ff1fa484cc1a869af36ae91530a` (Phase 3 close hash, recorded in STATE.md).

If the SHA has CHANGED, this is a deviation that must be analyzed:
- A new entry suppresses an unfixed L10 finding (anti-pattern — fix at source).
- A removed entry means an existing baseline issue was actually fixed (acceptable, but log a Decision).
- ANY change blocks Phase 4 close until reconciled.

C) Verify the 4 toolchain stages individually:

```bash
make pint-test
make lint-settings-accessor
make analyse
make phpmd
make test
```

Record per-stage status + timing in the SUMMARY.

If any stage fails, this plan is BLOCKED — fix the failing stage in the appropriate plan (04-01 for Plugin.php / parseIniSize, 04-04 for upload handlers, etc.) and re-run from the top. Do NOT add baseline entries to suppress L10 findings — fix at source per Phase 3 D-03-03-03 / D-03-04-01 / D-03-07-03 patterns.
  </action>
  <verify>
    <automated>cd $(git rev-parse --show-toplevel) && cd plugins/logingrupa/goodsreceivedshopaholic && make all 2>&1 | tail -20 && echo "---baseline SHA---" && sha256sum phpstan-baseline.neon</automated>
  </verify>
  <done>
    - make all exits 0.
    - phpstan-baseline.neon SHA matches Phase 3 close hash.
    - Per-stage + Pest counts captured.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Flip REQUIREMENTS.md traceability + update ROADMAP.md + STATE.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
A) `.planning/REQUIREMENTS.md`:

Locate the v1 Requirements traceability table near the bottom. Find these 13 rows:

| UI-01 | Phase 4 | Pending |
| UI-02 | Phase 4 | Pending |
| UI-03 | Phase 4 | Pending |
| UI-04 | Phase 4 | Pending |
| UI-05 | Phase 4 | Pending |
| UI-06 | Phase 4 | Pending |
| UI-07 | Phase 4 | Pending |
| UI-08 | Phase 4 | Pending |
| UI-09 | Phase 4 | Pending |
| UI-10 | Phase 4 | Pending |
| UI-11 | Phase 4 | Pending |
| UI-12 | Phase 4 | Pending |
| QA-10 | Phase 4 | Pending |

Flip each Status to `Closed (YYYY-MM-DD) — plan NN-NN`. Use today's date and the originating plan number per this mapping:

- UI-01 → 04-03
- UI-02 → 04-04
- UI-03 → 04-04
- UI-04 → 04-05
- UI-05 → 04-03
- UI-06 → 04-03
- UI-07 → 04-03 + 04-04 (use the multi-plan format `04-03 + 04-04` per Phase 3 APPLY-08 precedent)
- UI-08 → 04-06
- UI-09 → 04-04
- UI-10 → 04-06
- UI-11 → 04-02
- UI-12 → 04-01
- QA-10 → 04-07

Also flip the inline checklist boxes near the top of REQUIREMENTS.md from `- [ ]` to `- [x]` for each of the 13 entries (UI-01..12 + QA-10) — add a closure note in italics per Phase 3 precedent (e.g., "*(closed 2026-04-29 in plan 04-04 — onUpload + multi-file iteration; ...)*"). Keep notes concise (1-3 sentences each).

B) `.planning/ROADMAP.md`:

1. Update the **Phase 4** section header `### Phase 4:` block.
2. Update `**Plans:** 8 plans`.
3. Replace `**Plans**: TBD` with the actual 8-plan list:

```markdown
**Plans**: 8 plans
  - [x] 04-01-PLAN.md - Plugin boot self-check (UI-12) + parseIniSize() helper *(closed YYYY-MM-DD)*
  - [x] 04-02-PLAN.md - Console command goodsreceived:recompute_active_from_stock (UI-11) *(closed YYYY-MM-DD)*
  - [x] 04-03-PLAN.md - Backend controller foundation + audit history list + Invoice attachOne (UI-01 + UI-05 + UI-06 + UI-07) *(closed YYYY-MM-DD)*
  - [x] 04-04-PLAN.md - onUpload + onUpdateLine + pre-parse duplicate detection (UI-02 + UI-03 + UI-07 + UI-09) *(closed YYYY-MM-DD)*
  - [x] 04-05-PLAN.md - onApply + Cache::lock debounce + confirmation modal (UI-04) *(closed YYYY-MM-DD)*
  - [x] 04-06-PLAN.md - Override-and-reimport + initial-reset typed gates (UI-08 + UI-10) *(closed YYYY-MM-DD)*
  - [x] 04-07-PLAN.md - 4 permission gate tests (QA-10) *(closed YYYY-MM-DD)*
  - [x] 04-08-PLAN.md - Phase 4 final QA gate; baseline unchanged *(closed YYYY-MM-DD)*
```

4. Update the **Progress** table at the bottom of ROADMAP.md:
```
| 4. Backend Controller, Upload/Preview/Apply UI, Console | 8/8 | Complete | YYYY-MM-DD |
```

C) `.planning/STATE.md`:

1. Update the YAML front-matter `stopped_at` field to a multi-line string describing Phase 4 close: 13 requirements satisfied, plan count, test count delta, baseline SHA verified, Phase 5 next.
2. Update `last_updated` to today's ISO timestamp.
3. Update `progress.completed_phases` to 4.
4. Update `progress.completed_plans` to (Phase 3 close: 23) + 8 = 31.
5. Update the prose **Current Position** + **Status** + **Last activity** sections to reflect Phase 4 close.
6. Update the **Performance Metrics** table — add a new row per plan and a Phase 4 total.
7. Update **Recent Trend** with the last 5 plans (04-04..04-08).
8. Append new D-04-* Decisions logged across Phase 4 plans (gathered from each plan's SUMMARY).
9. Update **Session Continuity** Last session / Stopped at / Resume file.
10. Add new entries to **UAT Items Pending** for Phase 4: visit Backend → Settings → Goods Received — Invoice History; upload a fixture HTM; observe preview screen; click Apply → confirmation modal → success card; trigger override-and-reimport; trigger initial-reset (with allow_initial_reset=true on a dev server).
  </action>
  <verify>
    <automated>grep -c '^| UI-' /home/forge/nailscosmetics.lv/plugins/logingrupa/goodsreceivedshopaholic/.planning/REQUIREMENTS.md && grep -c 'Closed.*04-' /home/forge/nailscosmetics.lv/plugins/logingrupa/goodsreceivedshopaholic/.planning/REQUIREMENTS.md && grep -c '8/8' /home/forge/nailscosmetics.lv/plugins/logingrupa/goodsreceivedshopaholic/.planning/ROADMAP.md</automated>
  </verify>
  <done>
    - REQUIREMENTS.md: 13 Phase 4 traceability rows flipped to Closed.
    - ROADMAP.md: Phase 4 row Complete + 8-plan list.
    - STATE.md: progress, decisions, performance metrics updated.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Phase 4 SUMMARY with closure table + concrete metrics</name>
  <files>.planning/phases/04-backend-controller-upload-preview-apply-ui-console/04-08-SUMMARY.md</files>
  <action>
Following the Phase 2 02-07-SUMMARY.md and Phase 3 03-08-SUMMARY.md format, write `.planning/phases/04-backend-controller-upload-preview-apply-ui-console/04-08-SUMMARY.md` with:

1. **Phase 4 closure table**: 13-row table cross-referencing UI-01..UI-12 + QA-10 to (a) the originating plan number, (b) the file path that ships the contract, (c) the concrete test pin (file::test_name) that asserts it.

2. **Per-plan metrics table**: 8 rows with plan id / files modified count / production LoC / new test count / wall-clock estimate.

3. **Aggregate metrics**: Phase 4 cumulative production LoC, new test files, new it() cases, new assertions. Compare against Phase 3 baseline (145 tests / 708 assertions).

4. **make all output**: capture stage timings + Pest summary line.

5. **Baseline integrity**: verify phpstan-baseline.neon SHA == Phase 3 close hash; document the exact SHA observed.

6. **Decisions log delta**: list new D-04-* decisions logged across Phase 4 plans.

7. **UAT items**: 5-7 manual verification steps to be done on staging before Phase 5 starts (visit Backend → Settings → Goods Received — Invoice History; upload sample HTM; click Apply with debounce check; trigger override flow; trigger initial-reset with allow_initial_reset=true; run `php artisan goodsreceived:recompute_active_from_stock`; verify backend permission roles enforce 4 split gates).

8. **Next phase pointer**: Phase 5 — OPS-01..06; suggest plan-phase decomposition kickoff via `/gsd-discuss-phase 5` followed by `/gsd-plan-phase 5`.

Hard rules:
- File header `# Phase 4 — 04-08 Final QA Gate Summary`.
- Markdown only; no PHP code unless quoting actual baseline file contents.
- Cross-reference REQUIREMENTS.md / ROADMAP.md / STATE.md by relative path.
  </action>
  <verify>
    <automated>test -s /home/forge/nailscosmetics.lv/plugins/logingrupa/goodsreceivedshopaholic/.planning/phases/04-backend-controller-upload-preview-apply-ui-console/04-08-SUMMARY.md && wc -l /home/forge/nailscosmetics.lv/plugins/logingrupa/goodsreceivedshopaholic/.planning/phases/04-backend-controller-upload-preview-apply-ui-console/04-08-SUMMARY.md</automated>
  </verify>
  <done>
    - 04-08-SUMMARY.md exists, contains 13-row closure table, 8-row plan-metrics table, baseline SHA proof.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phpstan-baseline.neon | the integrity-of-quality-bar contract; an unexplained delta is a red flag |
| REQUIREMENTS.md traceability | the source-of-truth for which contracts are pinned |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08-01 | Tampering | baseline silently grown to suppress new L10 errors | mitigate | Explicit SHA comparison against Phase 3 close hash; any delta blocks the plan; fix-at-source policy enforced |
| T-04-08-02 | Repudiation | Phase close not traceable | mitigate | 13-row closure table cross-references requirement → plan → test pin |
| T-04-08-03 | Tampering | REQUIREMENTS.md flipped without test backing | mitigate | Each "Closed" entry references a concrete plan that ships a concrete test pin; no flip without underlying contract |
</threat_model>

<verification>
- `cd <project_root> && cd plugins/logingrupa/goodsreceivedshopaholic && make all` — exit 0.
- `sha256sum plugins/logingrupa/goodsreceivedshopaholic/phpstan-baseline.neon` — matches Phase 3 close hash.
- `grep -c 'Closed.*04-' plugins/logingrupa/goodsreceivedshopaholic/.planning/REQUIREMENTS.md` — at least 13.
- `grep -c '8/8' plugins/logingrupa/goodsreceivedshopaholic/.planning/ROADMAP.md` — Phase 4 row updated.
</verification>

<success_criteria>
1. make all green; baseline SHA unchanged.
2. 13 REQ rows flipped to Closed in REQUIREMENTS.md.
3. ROADMAP.md Phase 4 row Complete with 8-plan list.
4. STATE.md reflects Phase 4 close + Phase 5 next.
5. SUMMARY.md ships the 13-row closure table + 8-row plan-metrics table.
</success_criteria>

<output>
Created `.planning/phases/04-backend-controller-upload-preview-apply-ui-console/04-08-SUMMARY.md` with closure metrics. Phase 4 is then COMPLETE; orchestrator may proceed to `/gsd-discuss-phase 5` (Phase 5 — OPS-01..06).
</output>
