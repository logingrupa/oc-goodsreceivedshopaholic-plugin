---
phase: 02-pure-parsers-dtos-exceptions-ean-matcher
plan: 07
type: execute
wave: 4
depends_on:
  - 02-01
  - 02-02
  - 02-03
  - 02-04
  - 02-05
  - 02-06
files_modified: []
autonomous: true
requirements:
  - PARSE-07
must_haves:
  truths:
    - "All 3 hermetic HTM fixtures still present in tests/fixtures/invoices/ (PARSE-07 — verification-only; copy was completed in Phase 1 plan 01-04)"
    - "No production code reads from `<project_root>/storage/app/uploads/` — confirmed by grep gate"
    - "All Phase 2 tests in tests/unit/ pass green"
    - "make pint-test exits 0"
    - "make analyse (PHPStan level 10) exits 0 with zero new errors"
    - "make phpmd exits 0"
    - "make test exits 0"
    - "make all exits 0 (pint-test → analyse → phpmd → test)"
    - "phpstan-baseline.neon is unchanged from start of Phase 2"
  artifacts:
    - path: "tests/fixtures/invoices/Nr_PRO026712_no_28112024.HTM"
      provides: "Earliest hermetic fixture (2024-11-28) — used by InvoiceNumberResolverTest + HtmInvoiceParserTest"
    - path: "tests/fixtures/invoices/Nr_PRO029691_no_09072025.HTM"
      provides: "Mid-2025 hermetic fixture"
    - path: "tests/fixtures/invoices/Nr_PRO033328_no_13042026.HTM"
      provides: "Latest hermetic fixture (2026-04-13)"
  key_links:
    - from: "tests/unit/Parser/HtmInvoiceParserTest.php"
      to: "tests/fixtures/invoices/Nr_PRO033328_no_13042026.HTM"
      via: "file_get_contents"
      pattern: "fixtures/invoices/Nr_PRO033328"
    - from: "tests/unit/Parser/InvoiceNumberResolverTest.php"
      to: "tests/fixtures/invoices/"
      via: "file_get_contents"
      pattern: "fixtures/invoices/"
---

<objective>
Final QA gate for Phase 2. This plan modifies no source files — it runs the full QA pipeline (`make all`), verifies hermetic-fixture invariants (PARSE-07 marker), and confirms zero baseline drift. If everything is green, Phase 2 is shippable to plan-checker.

Purpose: Catch any regression introduced by plans 02-01 through 02-06 that individually passed their own gates but interact poorly. Single source of truth for "Phase 2 is done."
Output: Pass/fail evidence for plan-checker; no code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-pure-parsers-dtos-exceptions-ean-matcher/02-CONTEXT.md
@plugins/logingrupa/goodsreceivedshopaholic/Makefile
@plugins/logingrupa/goodsreceivedshopaholic/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hermetic-fixture invariants (PARSE-07)</name>
  <files>(no files modified — verification only)</files>
  <action>
    PARSE-07 requirement: 3 hermetic fixtures in `tests/fixtures/invoices/` AND tests never read outside `tests/`. The fixture copy was performed in Phase 1 plan 01-04. This plan re-verifies the invariants are still true after Phase 2 work landed.

    Run these checks:

    1. **All 3 fixtures present**:
       ```
       ls tests/fixtures/invoices/Nr_PRO026712_no_28112024.HTM tests/fixtures/invoices/Nr_PRO029691_no_09072025.HTM tests/fixtures/invoices/Nr_PRO033328_no_13042026.HTM
       ```
       Each command must succeed (file exists).

    2. **Hermetic test invariant — no production code reads from project_root storage**:
       ```
       grep -rE "storage/app/uploads/invoices" classes/ tests/ 2>/dev/null
       ```
       Must produce zero matches. If anything matches: STOP, that file is leaking the boundary; flag for fix before this plan can pass.

    3. **Hermetic test invariant — tests reference only `tests/fixtures/`**:
       ```
       grep -rnE "fixtures/invoices" tests/unit/ | grep -v "tests/fixtures/invoices"
       ```
       Should produce zero matches OR only paths via `__DIR__ . '/../../fixtures/invoices/...'` (which is the correct hermetic pattern). Manual review: every match must resolve via `__DIR__` relative path or absolute path inside `plugins/logingrupa/goodsreceivedshopaholic/tests/fixtures/invoices/`.

    4. **Fixture content sanity** — first 3 bytes of each are UTF-8 BOM (0xEF 0xBB 0xBF):
       ```
       for f in tests/fixtures/invoices/Nr_PRO*.HTM; do head -c 3 "$f" | od -An -tx1 | tr -d ' \n'; echo " $f"; done
       ```
       Each line must start with `efbbbf`. Confirms BOM-handling assumption (D-12) is still valid input shape — if a fixture got rewritten without BOM, parser tests would fail anyway, but this catches it earlier.

    No code changes in this task — all checks are read-only verification. Output the results inline so the plan-checker can audit.
  </action>
  <verify>
    <automated>ls tests/fixtures/invoices/Nr_PRO026712_no_28112024.HTM tests/fixtures/invoices/Nr_PRO029691_no_09072025.HTM tests/fixtures/invoices/Nr_PRO033328_no_13042026.HTM &amp;&amp; ! grep -rqE "storage/app/uploads/invoices" classes/ tests/ &amp;&amp; for f in tests/fixtures/invoices/Nr_PRO*.HTM; do head -c 3 "$f" | od -An -tx1 | tr -d ' \n' | grep -q '^efbbbf$' || exit 1; done</automated>
  </verify>
  <done>
    3 fixtures present, no leakage to project storage, all 3 BOMs intact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run make all (full QA pipeline)</name>
  <files>(no files modified)</files>
  <action>
    Run the full QA gate from the plugin directory:

    ```
    make all
    ```

    `make all` chains: `pint-test` → `analyse` → `phpmd` → `test`. Any failure aborts.

    Expected behavior on green:
      1. Pint reports `PASS  Style check successful!` for every file under `classes/` and `tests/`.
      2. PHPStan reports `[OK] No errors` at level 10.
      3. PHPMD scans `classes,components,models,Plugin.php` (per Makefile line 22) and reports zero violations. (Note: `tests/` is not phpmd-scanned by design.)
      4. Pest runs every test file, including:
         - `tests/unit/Dto/*` (4 files, plan 02-01)
         - `tests/unit/Exception/*` (2 files, plan 02-02)
         - `tests/unit/Parser/QuantityNormalizerTest.php`, `PriceNormalizerTest.php`, `InvoiceNumberResolverTest.php`, `HtmInvoiceParserTest.php` (plans 02-03, 02-04, 02-05)
         - `tests/unit/Match/EanMatcherServiceTest.php` (plan 02-06)
         - Plus Phase 1 surviving tests (`tests/unit/Models/*`, `TearDownFlushesSingletonsTest.php`)

    If any step fails:
      1. Read the failure output carefully.
      2. Identify which plan introduced the regression — usually obvious from the failing file path.
      3. Fix at the source (do NOT add to phpstan-baseline.neon).
      4. Re-run `make all`.
      5. If unable to resolve, document the failure mode and flag plan-checker.

    Commit policy: do NOT commit here. Orchestrator commits after plan-checker review per Phase 2 commit_policy.
  </action>
  <verify>
    <automated>make all</automated>
  </verify>
  <done>
    `make all` exits 0. All 4 sub-gates green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Confirm zero baseline drift</name>
  <files>(no files modified)</files>
  <action>
    Per Engineering Quality Bar in PROJECT.md (line ~96-101): baseline file is for legacy only; new code adds nothing.

    Run:
    ```
    git diff --exit-code phpstan-baseline.neon
    ```

    Exit code must be 0 (no diff). If non-zero:
      - Read the diff.
      - Each new line in the baseline is a level-10 error suppressed for some new code.
      - Fix the type at the source. Common patterns:
        - Add explicit `@param` / `@return` PHPDoc.
        - Cast `mixed` returns: `(string) $obRow->code`.
        - Annotate union strings: `/** @var 'a'|'b'|'c' $sStrategy */`.
      - Discard the baseline change (`git checkout phpstan-baseline.neon`) and re-run `make analyse` until clean.

    Also verify no new files dropped into baseline-style ignores in `phpstan.neon`:
    ```
    git diff --exit-code phpstan.neon
    ```
    Phase 2 should not have modified the PHPStan config.
  </action>
  <verify>
    <automated>git diff --exit-code phpstan-baseline.neon &amp;&amp; git diff --exit-code phpstan.neon</automated>
  </verify>
  <done>
    Baseline unchanged. PHPStan config unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 4: Phase-2 file inventory + smoke summary</name>
  <files>(no files modified)</files>
  <action>
    Produce a summary that plan-checker can audit at a glance. Run these read-only commands and capture the output:

    1. **Production code files added by Phase 2**:
       ```
       ls -1 classes/dto/*.php classes/exception/*.php classes/parser/*.php classes/match/*.php
       ```
       Expected count: 4 + 9 + 4 + 1 = **18 files**.

    2. **Test files added by Phase 2**:
       ```
       ls -1 tests/unit/Dto/*.php tests/unit/Exception/*.php tests/unit/Parser/*.php tests/unit/Match/*.php
       ```
       Expected count: 4 + 2 + 4 + 1 = **11 files**.

    3. **Strict-types coverage gate**:
       ```
       for f in $(ls classes/dto/*.php classes/exception/*.php classes/parser/*.php classes/match/*.php); do grep -q 'declare(strict_types=1);' "$f" || { echo "MISSING: $f"; exit 1; }; done
       ```
       Must complete with no MISSING output. Every Phase 2 production file must declare strict types (D-35).

    4. **Final-class gate** (DTOs are `final readonly`; exceptions are `final` except the abstract base; parser/normalizer/resolver/matcher are `final`):
       ```
       grep -L 'final \(readonly \)\?class\|abstract class' classes/dto/*.php classes/exception/*.php classes/parser/*.php classes/match/*.php
       ```
       Should produce no output (every file has either `final class`, `final readonly class`, or `abstract class`).

    5. **No `else` keyword in production code** (D-37 max-nesting hygiene):
       ```
       grep -rEcn '^\s*else' classes/ | grep -v ':0$'
       ```
       Should produce no output.

    Output a markdown table summarizing pass/fail of each check; this becomes the body of `02-07-SUMMARY.md`.

    No commits. No code changes. Pure verification + summary.
  </action>
  <verify>
    <automated>FILE_COUNT_PROD=$(ls -1 classes/dto/*.php classes/exception/*.php classes/parser/*.php classes/match/*.php 2>/dev/null | wc -l) &amp;&amp; FILE_COUNT_TEST=$(ls -1 tests/unit/Dto/*.php tests/unit/Exception/*.php tests/unit/Parser/*.php tests/unit/Match/*.php 2>/dev/null | wc -l) &amp;&amp; [ "$FILE_COUNT_PROD" = "18" ] &amp;&amp; [ "$FILE_COUNT_TEST" = "11" ] &amp;&amp; for f in classes/dto/*.php classes/exception/*.php classes/parser/*.php classes/match/*.php; do grep -q 'declare(strict_types=1);' "$f" || exit 1; done &amp;&amp; ! grep -rqE '^\s*else' classes/</automated>
  </verify>
  <done>
    18 production + 11 test files exist. Every production file declares strict types, no `else` keyword. Markdown summary table written to plan output.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan modifies no code. The Phase 2 attack surface is covered by the threat models in plans 02-01 through 02-06. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07-01 | Repudiation | Phase 2 ships with regressions that individually passed sub-plan gates | mitigate | Full `make all` run is the cross-plan integration check. PHPStan level 10 catches type drift; phpmd catches style; pest catches behavioral regression. Baseline-unchanged gate prevents silent suppression. |
| T-02-07-02 | Tampering (hermetic invariant breach) | A Phase 2 test or class accidentally reads from `<project_root>/storage/app/uploads/` | mitigate | Task 1 grep gate catches any such read. Hermetic fixtures (PARSE-07) keep tests independent of production data. |
</threat_model>

<verification>
- 3 hermetic fixtures present in tests/fixtures/invoices/, all with UTF-8 BOM intact
- No grep matches for `storage/app/uploads/invoices` outside tests/fixtures itself
- `make all` exits 0
- `git diff --exit-code phpstan-baseline.neon` exits 0
- `git diff --exit-code phpstan.neon` exits 0
- 18 production files + 11 test files present, all production files declare strict types
- No `else` keyword in production code
</verification>

<success_criteria>
Phase 2 is shippable. The Parse → Match pipeline is fully implemented, tested, type-safe at level 10, and lint-clean. Phase 3 (Apply Layer + Orchestrators) can begin against this stable foundation.

The 11 Phase 2 requirements are now satisfied:
- PARSE-01: 4 readonly DTOs (plan 02-01)
- PARSE-02: 8 typed exceptions + abstract base (plan 02-02)
- PARSE-03: HtmInvoiceParser (plan 02-05)
- PARSE-04: InvoiceNumberResolver (plan 02-04)
- PARSE-05: QuantityNormalizer (plan 02-03)
- PARSE-06: PriceNormalizer (plan 02-03)
- PARSE-07: 3 hermetic fixtures present + hermetic invariant verified (this plan)
- MATCH-01: EanMatcherService 2-query batch (plan 02-06)
- MATCH-02: Unmatched lines yield match_strategy='none' (plan 02-06)
- QA-01: 5 real-fixture pin tests (plan 02-05)
- QA-02: Decimal-qty rejection (plan 02-03) + leading-zero EAN preservation (plan 02-06)
</success_criteria>

<output>
After completion, create `.planning/phases/02-pure-parsers-dtos-exceptions-ean-matcher/02-07-SUMMARY.md` containing:
1. Pass/fail markdown table for Tasks 1-4
2. File inventory (18 production + 11 test files)
3. `make all` final output snippet (truncated to last 30 lines if long)
4. PHPStan baseline diff (should be empty)
5. Note that Phase 2 is shippable; Phase 3 can proceed
