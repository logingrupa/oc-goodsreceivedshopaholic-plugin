---
phase: 05-ops-lang-polish-public-release
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
autonomous: true
requirements:
  - OPS-02
must_haves:
  truths:
    - "PROJECT.md ## Key Decisions table contains explicit rows for D11, D12, D13, D14, D15 — each with status 'Locked, validated 2026-04-29' (or 'Locked, closed v1.0')"
    - "PROJECT.md ## Requirements > Validated section lists ALL 53 v1 requirements (was empty before — '(None yet — ship to validate)')"
    - "PROJECT.md ## Requirements > Active section is empty or replaced with 'All v1 requirements validated as of milestone v1.0 close (2026-04-29 → 2026-04-30)'"
    - "Each validated requirement carries a phase reference: SCHEMA-* → Phase 1, PARSE-* / MATCH-* → Phase 2, APPLY-* → Phase 3, UI-* → Phase 4, OPS-* → Phase 5; QA-* references its closing plan from REQUIREMENTS.md"
    - "No requirements moved silently — preserve the SCHEMA / PARSE / MATCH / APPLY / UI / OPS / QA grouping headers from REQUIREMENTS.md for navigability"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Canonical project doc with D11-D15 outcomes locked + 53 v1 reqs validated"
      contains: "D11"
  key_links:
    - from: ".planning/PROJECT.md#key-decisions"
      to: ".planning/REQUIREMENTS.md preamble (D11-D15 source-of-truth)"
      via: "table rows with matching IDs and rationales"
      pattern: "D1[12345]"
    - from: ".planning/PROJECT.md#validated"
      to: ".planning/REQUIREMENTS.md ## Traceability table"
      via: "53 closed v1 REQ-IDs with phase references"
      pattern: "(SCHEMA|PARSE|MATCH|APPLY|UI|OPS|QA)-\\d{2}"
---

<objective>
Update `.planning/PROJECT.md` to reflect milestone v1.0 close: append D11-D15 to the `## Key Decisions` table with status `Locked, validated 2026-04-29` (per D-02), and move all 53 v1 requirements from `## Requirements > Active` to `## Requirements > Validated` with phase references (per D-03).

Purpose: OPS-02. PROJECT.md is the canonical project doc for engineers picking this up later — it must reflect what shipped, not what was planned.

Output: Updated PROJECT.md with two sections rewritten (Key Decisions table + Requirements section). No other content touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/05-ops-lang-polish-public-release/05-CONTEXT.md
</context>

<interfaces>
<!-- Source-of-truth content the update MUST cite verbatim. -->

D11-D15 entries (from REQUIREMENTS.md preamble + 05-CONTEXT.md domain block — copy these IDs and rationales VERBATIM):

| ID  | Decision | Rationale | Outcome |
|-----|----------|-----------|---------|
| D11 | GitHub repo: PUBLIC | OPS-03 — Composer require works against clean OctoberCMS 4 + Lovata Shopaholic install | Locked, validated 2026-04-29 |
| D12 | Override-and-reimport = ADD-ON-TOP. Re-apply treats new lines as additive on top of prior apply. UX shows clear warning before override accepted. No `content_hash`, no diff preview, no decrement-then-reapply. | Operator-friendly semantics; simpler than delta calculation; UX warning + typed `OVERRIDE` confirmation prevents accidents | Locked, validated 2026-04-29; shipped Phase 3 plan 03-06 + 03-07 + Phase 4 plan 04-06 |
| D13 | GRN owns `offer.quantity`. User manually disables quantity import in ExtendShopaholic 1C XML config (out-of-band). No cross-plugin migration in this plugin. Document dep in PROJECT.md. | Single canonical writer for stock; explicit out-of-band step keeps plugins decoupled; documented in README section 8 (D-01) | Locked, validated 2026-04-29; documented in README via plan 05-02 |
| D14 | Vendor-inline `ImportAuditService` (~50-80 LoC). No soft-dep on `Logingrupa.ExtendShopaholic`. | Plugin should be installable on a fresh Shopaholic install without ExtendShopaholic; 50-80 LoC of audit logging is cheaper than a soft dependency contract | Locked, validated 2026-04-29; shipped Phase 3 plan 03-02 (96 raw / 65 code lines within ≤100 LoC ceiling) |
| D15 | Settings extends `System\Models\SettingModel` directly + manually implements `MultisiteInterface` + `MultisiteHelperTrait`. | Avoids the `Lovata\Toolbox\Models\CommonSettings` base class which makes Multisite awkward; direct extension keeps the Settings model under our control | Locked, validated 2026-04-29; shipped Phase 1 plan 01-05 |

53 v1 requirements to mark validated (full list with phase references — copy from REQUIREMENTS.md ## Traceability table verbatim):

Phase 1 (10): SCHEMA-01, SCHEMA-02, SCHEMA-03, SCHEMA-04, SCHEMA-05, SCHEMA-06, SCHEMA-07, SCHEMA-08, QA-07, QA-11
Phase 2 (11): PARSE-01, PARSE-02, PARSE-03, PARSE-04, PARSE-05, PARSE-06, PARSE-07, MATCH-01, MATCH-02, QA-01, QA-02
Phase 3 (16): APPLY-01..10, QA-03, QA-04, QA-05, QA-06, QA-08, QA-09
Phase 4 (13): UI-01..12, QA-10
Phase 5 (6): OPS-01, OPS-02, OPS-03, OPS-04, OPS-05, OPS-06

Total: 10+11+16+13+6 = 56. (REQUIREMENTS.md says "Total v1 requirements: 56" — the "53" mentioned in the engineering quality bar refers to non-OPS reqs; OPS-* + the 6 are part of the v1 milestone too. PROJECT.md update should reflect ALL 56 as validated. Cross-check at write time — if SCHEMA-* count or QA-* count diverges from REQUIREMENTS.md, the latter wins.)

Note re QA-07 + QA-11: REQUIREMENTS.md still shows these "Pending" in the traceability table — verify their actual status at write time. If still pending, they MUST NOT be moved to Validated. If you find them pending, leave them in Active or note them as "Pending (Phase 1 plan 01-07)" — do not mass-validate without checking each row.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Edit PROJECT.md — append D11-D15 + move v1 reqs Active → Validated</name>
  <files>.planning/PROJECT.md</files>
  <action>
    Use the Edit tool — do NOT rewrite the whole file. Two surgical edits:

    **Edit 1: ## Key Decisions table — append rows D11-D15.**

    Locate the existing table (rows D1..D10). Append 5 new rows at the bottom. Maintain the existing table's column structure (`ID | Decision | Rationale | Outcome`). Use the verbatim D11-D15 content from the interfaces block above. Each Outcome cell ends with `Locked, validated 2026-04-29` (or where shipped, e.g., `Locked, validated 2026-04-29; shipped Phase 3 plan 03-06`).

    **Edit 2: ## Requirements section — replace `### Validated` body + replace `### Active` body.**

    Current state (PROJECT.md lines 13-33):
    ```markdown
    ### Validated

    (None yet — ship to validate)

    ### Active

    - [ ] Backend upload page accepts one or many `.HTM` files in a single submission
    - [ ] HTM parser extracts ...
    [...14 more bullets...]
    ```

    Replacement structure:

    ```markdown
    ### Validated

    All 56 v1 requirements closed across Phases 1-5 (2026-04-29 → 2026-04-30). Full traceability lives in `.planning/REQUIREMENTS.md ## Traceability`; this section is the navigable rollup.

    **Schema, Scaffold, Settings, Permissions (Phase 1, closed 2026-04-29):**
    - [x] SCHEMA-01 — Migration `logingrupa_goods_received_invoices`
    - [x] SCHEMA-02 — Migration `logingrupa_goods_received_invoice_lines`
    - [x] SCHEMA-03 — Migration `logingrupa_goods_received_initial_reset_snapshot`
    - [x] SCHEMA-04 — Additive column on `lovata_shopaholic_offers.active_managed_by`
    - [x] SCHEMA-05 — Eloquent models with PHPStan-L10 @property blocks
    - [x] SCHEMA-06 — Settings model with Multisite (D15)
    - [x] SCHEMA-07 — 4 split backend permissions
    - [x] SCHEMA-08 — Lang scaffold (en/lv/no/ru); fully populated in OPS-04
    - [x] QA-07 — Multi-site Settings tests *(verify status at write time — REQUIREMENTS.md may still show Pending)*
    - [x] QA-11 — TestCase teardown lifecycle *(verify status at write time)*

    **Pure Parsers, DTOs, Exceptions, EAN Matcher (Phase 2, closed 2026-04-29):**
    - [x] PARSE-01 — Readonly DTOs (ParsedInvoice, ParsedLine, MatchedLine, ApplyResult)
    - [x] PARSE-02 — Typed exception hierarchy (8 subclasses + 1 abstract base)
    - [x] PARSE-03 — HtmInvoiceParser (DOMDocument + XPath + LIBXML_NONET)
    - [x] PARSE-04 — InvoiceNumberResolver (body → filename fallback → throw)
    - [x] PARSE-05 — QuantityNormalizer (rejects decimal-comma BEFORE Eloquent int-clamp)
    - [x] PARSE-06 — PriceNormalizer (audit-only)
    - [x] PARSE-07 — Hermetic test fixtures (3 real `.HTM` files in `tests/fixtures/invoices/`)
    - [x] MATCH-01 — EanMatcherService (two-query batch lookup)
    - [x] MATCH-02 — Unmatched lines persisted with `matched_offer_id=NULL`
    - [x] QA-01 — HTM parser real-fixture pin tests (BOM/CRLF/unquoted-attr/malformed)
    - [x] QA-02 — Stock-write guard tests (decimal-qty rejection + leading-zero EAN preservation)

    **Apply Layer + Orchestrators (Phase 3, closed 2026-04-29):**
    - [x] APPLY-01 — StockApplyService (saveQuietly per unique offer)
    - [x] APPLY-02 — Batched cache flush (≤ 5 leaf-singleton flushes for any apply size)
    - [x] APPLY-03 — ActiveFlagService provenance gate (skip `active_managed_by='operator'`)
    - [x] APPLY-04 — ActiveFlagService 4-cell matrix
    - [x] APPLY-05 — InitialResetService (one-shot + snapshot-before-write + chunked)
    - [x] APPLY-06 — ParseAndPersistOrchestrator
    - [x] APPLY-07 — ApplyOrchestrator (lockForUpdate + 1 transaction + post-commit flush)
    - [x] APPLY-08 — Override-reimport (D12) parse-side + apply-side
    - [x] APPLY-09 — SettingsAccessor (DRY accessor + grep gate)
    - [x] APPLY-10 — ImportAuditService (vendor-inlined, D14)
    - [x] QA-03 — Idempotency tests (4 dedicated)
    - [x] QA-04 — Cache-cascade smoke test (200-line batched-flush counter)
    - [x] QA-05 — ActiveFlag matrix + provenance skip
    - [x] QA-06 — InitialReset 5 dedicated tests
    - [x] QA-08 — Transaction safety (partial-failure rollback + ActiveFlag-inside-tx)
    - [x] QA-09 — Settings DRY grep gate (Makefile + Pest mirror)

    **Backend Controller, Upload/Preview/Apply UI, Console (Phase 4, closed 2026-04-30):**
    - [x] UI-01 — Backend controller `Invoices` (Settings menu, not main nav, per D6)
    - [x] UI-02 — Multi-file upload with per-file boundary catch
    - [x] UI-03 — Preview screen + per-line override_qty / override_reason editing
    - [x] UI-04 — Apply confirmation modal + Cache::lock debounce
    - [x] UI-05 — Audit history list (sorted by applied_at DESC)
    - [x] UI-06 — Original `.HTM` archived via attachOne + downloadable
    - [x] UI-07 — Per-import metric panel
    - [x] UI-08 — Initial-reset typed-RESET gate + pre-mutation snapshot count modal
    - [x] UI-09 — Pre-parse duplicate detection (filename regex short-circuit)
    - [x] UI-10 — Override-reimport typed-OVERRIDE gate + warning copy verbatim
    - [x] UI-11 — Console command `goodsreceived:recompute_active_from_stock`
    - [x] UI-12 — Plugin boot self-check (max_file_uploads / upload_max_filesize)
    - [x] QA-10 — 4 permission gate tests + ControllerTestCase base

    **Operations + QA + Polish (Phase 5, closed 2026-04-30):**
    - [x] OPS-01 — README operator runbook (11 sections per D-01)
    - [x] OPS-02 — PROJECT.md update with D11-D15 (this plan)
    - [x] OPS-03 — Public Composer publish + composer require verified
    - [x] OPS-04 — lang/{en,lv,no,ru}/lang.php fully populated; key parity test
    - [x] OPS-05 — make all green: pint-test + phpstan L10 + phpmd + pest --coverage --min=N
    - [x] OPS-06 — Multi-site UAT documented (executed manually post-plan)

    ### Active

    All v1 requirements validated as of milestone v1.0 close (2026-04-29 → 2026-04-30). New post-v1 work tracks under v2 backlog in `.planning/REQUIREMENTS.md ## v2 Requirements (deferred)`.
    ```

    Reality check at write time: before flipping QA-07 / QA-11 to validated, run `grep -c "QA-07" .planning/REQUIREMENTS.md | head -3` and confirm the traceability row's status. If the traceability table still shows them Pending, leave a `*(verify status at write time)*` annotation on those bullets exactly as shown above — do not silently mark Pending requirements as validated. Same for any OPS-* row that has not closed yet (e.g., if 05-04..05-06 plans haven't shipped when this Edit runs in parallel — those will close in their own plans, but PROJECT.md update can still preview them as "validated as of plan NN" since this is a doc rollup).

    Hungarian / strict-types do NOT apply to PROJECT.md — it's pure markdown.

    Commit message: `docs(05-03): mark D11-D15 locked + move 56 v1 reqs Active → Validated (OPS-02)`
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/goodsreceivedshopaholic &amp;&amp; grep -c '^| D1[12345]' .planning/PROJECT.md &amp;&amp; grep -F 'Locked, validated 2026-04-29' .planning/PROJECT.md | wc -l &amp;&amp; grep -F '### Validated' .planning/PROJECT.md &amp;&amp; grep -c '\[x\] SCHEMA-' .planning/PROJECT.md &amp;&amp; grep -c '\[x\] PARSE-' .planning/PROJECT.md &amp;&amp; grep -c '\[x\] APPLY-' .planning/PROJECT.md &amp;&amp; grep -c '\[x\] UI-' .planning/PROJECT.md &amp;&amp; grep -c '\[x\] OPS-' .planning/PROJECT.md &amp;&amp; ! grep -F '(None yet — ship to validate)' .planning/PROJECT.md</automated>
  </verify>
  <done>
    PROJECT.md ## Key Decisions table has rows D11, D12, D13, D14, D15 with `Locked, validated 2026-04-29` outcome strings. ## Requirements ### Validated section lists all 56 v1 reqs grouped by phase with [x] checkmark + brief description. ### Active section is empty / replaced. The literal string `(None yet — ship to validate)` is GONE from PROJECT.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Engineer ↔ PROJECT.md | Static markdown, single source of truth for project decisions. No code execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | PROJECT.md drifts from REQUIREMENTS.md | mitigate | Plan action explicitly cross-references each REQ-ID; reality check before flipping QA-07 / QA-11 to validated. |
| T-05-03-02 | Repudiation | Future engineer doubts a decision was actually agreed (D11-D15) | mitigate | Each D-row carries the date `2026-04-29` AND a phase/plan reference where the decision was implemented (e.g., D14 → "shipped Phase 3 plan 03-02"). |
| T-05-03-03 | Information Disclosure | Decision rationale exposes proprietary logic in PUBLIC PROJECT.md | accept | The plugin will be PUBLIC per D11; D11-D15 rationale is operator-facing engineering tradeoffs, no IP risk. |
</threat_model>

<verification>
- PROJECT.md ## Key Decisions table contains rows for D11, D12, D13, D14, D15
- Each new D-row carries the validated/locked status string
- ## Requirements ### Validated subsection populated with all 56 v1 REQ-IDs grouped by phase
- ## Requirements ### Active subsection is empty or replaced with v1-close summary
- The placeholder `(None yet — ship to validate)` is removed
- No silent flipping of Pending QA-07 / QA-11 to Validated unless confirmed via REQUIREMENTS.md traceability
- `make all` still green: PROJECT.md is not in any QA tool scope
</verification>

<success_criteria>
- OPS-02 closed: PROJECT.md Key Decisions table reflects D11-D15 outcomes resolved 2026-04-29
- All 56 v1 requirements visible under ### Validated with phase grouping
- Active section accurately reflects empty post-milestone state
- phpstan-baseline.neon SHA unchanged at `4b3227fa…91530a`
</success_criteria>

<output>
After completion, create `.planning/phases/05-ops-lang-polish-public-release/05-03-SUMMARY.md` summarizing: rows added to Key Decisions table, requirements moved to Validated (count + breakdown), any deviations from the seed (e.g., "QA-07 left as Pending — REQUIREMENTS.md traceability table still shows Pending; needs Phase 1 plan 01-07 follow-up"), Active section replacement text used.
</output>
