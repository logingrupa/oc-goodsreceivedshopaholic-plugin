---
phase: 05-ops-lang-polish-public-release
plan: 06
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05"]
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - OPS-05
  - OPS-06
must_haves:
  truths:
    - "`make all` exits 0: pint-test + lint-settings-accessor + analyse + phpmd + test + coverage all green in one run"
    - "phpstan-baseline.neon SHA unchanged at `4b3227fab5b697264e8532b59f5cdd96c86a0ff1fa484cc1a869af36ae91530a` (Phase 5 introduced zero new baseline-suppressed errors)"
    - "Test suite: 232 (Phase 4 close baseline) + lang-completeness tests from 05-01 = ~239+ tests, all green, including under coverage instrumentation"
    - "All 6 OPS-* requirements have status flipped from Pending to Closed in `.planning/REQUIREMENTS.md ## Traceability` table (with date 2026-04-30 and plan reference 05-NN)"
    - "ROADMAP.md Phase 5 row marked complete with `Plans: 6 plans` and the per-plan summary lines updated to `[x] 05-NN-PLAN.md — {brief}` and the Progress table row flipped to `Complete | 2026-04-30`"
    - "STATE.md updated to reflect milestone v1.0 closure: position = 'milestone v1.0 complete', any pending items resolved or moved to v2"
    - "Final SUMMARY.md for plan 05-06 produced + a milestone-level closure note (cumulative test count, plan count, decision count, file count for v1)"
    - "Operator has signed off via the checkpoint OR has explicitly deferred UAT execution to after this plan (UAT is documented but execution is post-plan per D-15)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated ## Traceability with all 6 OPS-* + any prior Pending QA-* flipped"
      contains: "OPS-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 5 row complete + Plans list with all 6 ticked"
      contains: "Phase 5"
    - path: ".planning/STATE.md"
      provides: "Milestone v1.0 closure marker"
      contains: "milestone v1.0"
    - path: ".planning/phases/05-ops-lang-polish-public-release/05-06-SUMMARY.md"
      provides: "Final phase 5 + milestone v1.0 closure summary"
      contains: "milestone v1.0"
  key_links:
    - from: "make all (full pipeline)"
      to: "pint-test + lint-settings-accessor + analyse + phpmd + test + coverage"
      via: "Makefile all target"
      pattern: "all:.*pint-test"
    - from: "REQUIREMENTS.md ## Traceability"
      to: "Phase 5 plans (05-01..05-06)"
      via: "OPS-* status update with plan reference"
      pattern: "OPS-\\d{2}.*Closed.*05-"
    - from: "ROADMAP.md Phase 5 row"
      to: "Progress table"
      via: "Plans Complete + Status + Completed columns"
      pattern: "5\\..*Complete"
---

<objective>
Run the FULL `make all` pipeline INCLUDING the new coverage gate from plan 05-04. Verify zero regressions across pint / phpstan / phpmd / pest / coverage. Confirm phpstan-baseline.neon SHA unchanged. Update REQUIREMENTS.md + ROADMAP.md + STATE.md to reflect milestone v1.0 closure. Produce a milestone-level summary so the next conversation has clean ground to start v2 from.

Purpose: OPS-05 final pipeline gate + OPS-06 documentation finalization. This is the milestone v1.0 close — every prior plan in Phases 1-5 has been pinned individually; this plan confirms the cumulative result is green and updates the planning artifacts to reflect closure.

Output: A green `make all` run + 3 doc updates (REQUIREMENTS.md, ROADMAP.md, STATE.md) + a final phase + milestone summary. Has 1 human checkpoint at the end so operator can review the milestone closure before signing off (UAT execution is post-plan per D-15, so this is purely a documentation-and-status-gate checkpoint).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-ops-lang-polish-public-release/05-CONTEXT.md
@.planning/phases/05-ops-lang-polish-public-release/05-01-SUMMARY.md
@.planning/phases/05-ops-lang-polish-public-release/05-02-SUMMARY.md
@.planning/phases/05-ops-lang-polish-public-release/05-03-SUMMARY.md
@.planning/phases/05-ops-lang-polish-public-release/05-04-SUMMARY.md
@.planning/phases/05-ops-lang-polish-public-release/05-05-SUMMARY.md
@phpstan-baseline.neon
@Makefile
@CLAUDE.md
</context>

<interfaces>
<!-- Cumulative numbers to verify and report. Pin these at execution time — they are predictions, not contracts. -->

Phase 4 close baseline (per 04-08 SUMMARY):
- Test count: 232/232
- Assertions: 1037
- Pest runtime: 9.63s
- Make all runtime: 10.86s
- PHPStan: 33/33 clean
- phpstan-baseline.neon SHA: `4b3227fab5b697264e8532b59f5cdd96c86a0ff1fa484cc1a869af36ae91530a`

Phase 5 expected additions (per Phase 5 plan summaries):
- 05-01: +7 lang-completeness tests (key parity x3 + non-empty + English-leftover smoke + placeholder + typed-literal preservation) → 239 tests
- 05-02..05-05: +0 production code (docs only)
- Coverage gate added: a measured threshold N (~75-90%) baked into make all

Cumulative milestone v1.0 expected stats:
- Phases: 5 (1, 2, 3, 4, 5)
- Plans: 8 + 7 + 8 + 8 + 6 = 37 plans
- Closed REQ-IDs: 56 (10 Phase 1 + 11 Phase 2 + 16 Phase 3 + 13 Phase 4 + 6 Phase 5)
- Production LoC (cumulative across all phases): TBD at execution time — measure with `find classes components console controllers models Plugin.php -name '*.php' | xargs wc -l | tail -1`
- Test files: TBD at execution time — measure with `find tests -name '*.php' | wc -l`
- D-XX decisions across phases: ~80+ (D-01..D-15 from CONTEXT + ~5-7 per plan x 37 plans = ~200-260; this is a rough estimate, actual count comes from phase SUMMARYs)

<!-- ROADMAP.md Phase 5 row currently reads (line 25): -->
- [ ] **Phase 5: Ops, Lang, Polish, Public Release** - Full lang packs, README + runbook, multi-site verification, public Composer publish, make all green

<!-- After this plan, MUST become: -->
- [x] **Phase 5: Ops, Lang, Polish, Public Release** - Full lang packs, README + runbook, multi-site verification (UAT documented; execution operator-action), public Composer publish (documented; gh repo create operator-action), make all green incl coverage gate

<!-- Progress table row currently (line 144): -->
| 5. Ops, Lang, Polish, Public Release | 0/TBD | Not started | - |

<!-- After this plan, MUST become: -->
| 5. Ops, Lang, Polish, Public Release | 6/6 | Complete | 2026-04-30 |
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Run full make all + verify baseline + measure cumulative stats</name>
  <files>(no file modifications — pure measurement)</files>
  <action>
    **Step 1: Run the full QA pipeline.**

    ```bash
    cd /home/forge/nailscosmetics.lv/plugins/logingrupa/goodsreceivedshopaholic
    time make all 2>&1 | tee /tmp/make-all-05-06.log
    echo "Exit code: $?"
    ```

    Expected exit code: 0. Expected last lines: each sub-target's "OK" / green output. Save the full log to `/tmp/make-all-05-06.log` for the SUMMARY.

    If exit non-zero, STOP. Do NOT touch the planning docs. Surface the failure to the operator with the relevant tail of the log:
    ```bash
    tail -80 /tmp/make-all-05-06.log
    ```

    Per Tiger-Style: If make all fails, fix the root cause (likely in the failing 05-NN plan, not here). Do NOT lower the coverage threshold or skip a sub-target to make the gate pass.

    **Step 2: Verify phpstan-baseline.neon SHA.**

    ```bash
    sha256sum phpstan-baseline.neon
    ```

    Expected: `4b3227fab5b697264e8532b59f5cdd96c86a0ff1fa484cc1a869af36ae91530a` (matches Phase 4 close baseline). If different, surface to operator — Phase 5 should NOT have introduced any new baseline-suppressed errors. If it did, investigate which plan regressed PHPStan compliance.

    **Step 3: Measure cumulative milestone v1.0 stats.**

    ```bash
    # Test count
    cd $(git rev-parse --show-toplevel) && vendor/bin/pest --configuration plugins/logingrupa/goodsreceivedshopaholic/phpunit.xml --testdox 2>&1 | tail -5
    # Production LoC
    cd /home/forge/nailscosmetics.lv/plugins/logingrupa/goodsreceivedshopaholic && find classes components console controllers models Plugin.php -name '*.php' 2>/dev/null | xargs wc -l 2>/dev/null | tail -1
    # Test LoC
    find tests -name '*.php' | xargs wc -l | tail -1
    # Test file count
    find tests -name '*Test.php' | wc -l
    # Coverage % (from latest run)
    grep -E '(Total|Lines)' /tmp/make-all-05-06.log | tail -5
    # Plan count per phase
    ls .planning/phases/01-schema-scaffold-settings-permissions/*PLAN*.md | wc -l
    ls .planning/phases/02-pure-parsers-dtos-exceptions-ean-matcher/*PLAN*.md | wc -l
    ls .planning/phases/03-apply-layer-orchestrators/*PLAN*.md | wc -l
    ls .planning/phases/04-backend-controller-upload-preview-apply-ui-console/*PLAN*.md | wc -l
    ls .planning/phases/05-ops-lang-polish-public-release/*PLAN*.md | wc -l
    ```

    Record these numbers — they're going into the SUMMARY.

    Per Tiger-Style boundary doc: hard numbers > vague summaries. The SUMMARY should cite measured values, not estimates.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/goodsreceivedshopaholic && make all 2>&1 | tail -5 && echo "exit=$?" && sha256sum phpstan-baseline.neon</automated>
  </verify>
  <done>
    `make all` exits 0. phpstan-baseline.neon SHA matches `4b3227fab5b697264e8532b59f5cdd96c86a0ff1fa484cc1a869af36ae91530a`. Cumulative stats measured and recorded for the SUMMARY.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md / ROADMAP.md / STATE.md to reflect milestone v1.0 closure</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
    Three surgical edits — use Edit tool, do NOT rewrite whole files.

    **Edit 1: REQUIREMENTS.md ## Traceability table.**

    Locate the OPS-* rows (currently lines ~191-196):
    ```
    | OPS-01 | Phase 5 | Pending |
    | OPS-02 | Phase 5 | Pending |
    | OPS-03 | Phase 5 | Pending |
    | OPS-04 | Phase 5 | Pending |
    | OPS-05 | Phase 5 | Pending |
    | OPS-06 | Phase 5 | Pending |
    ```

    Update each to:
    ```
    | OPS-01 | Phase 5 | Closed (2026-04-30) — plans 05-02 + 05-05 |
    | OPS-02 | Phase 5 | Closed (2026-04-30) — plan 05-03 |
    | OPS-03 | Phase 5 | Closed (2026-04-30) — plan 05-05 (docs); operator action: gh repo create + git tag v1.0.0 (post-plan per D-05) |
    | OPS-04 | Phase 5 | Closed (2026-04-30) — plan 05-01 |
    | OPS-05 | Phase 5 | Closed (2026-04-30) — plan 05-04 (coverage gate) + plan 05-06 (final pipeline run) |
    | OPS-06 | Phase 5 | Closed (2026-04-30) — plan 05-05 (UAT-CHECKLIST docs); operator action: execute UAT on .no/.lv/.lt (post-plan per D-15) |
    ```

    Also update the per-OPS bullet items in the v1 Requirements > Operations + QA + Polish (Phase 5) section (lines 76-81) — change `[ ]` to `[x]` and append a brief `*(2026-04-30: closed by plan 05-NN — ...)*` annotation citing the plan(s) that closed each.

    Also: re-check QA-07 + QA-11 (still showing Pending in traceability per REQUIREMENTS.md). If Phase 1 plan 01-07 actually closed them, flip to Closed with the plan reference. If they're genuinely still pending, leave them Pending and note this in the SUMMARY as v2 follow-up. Do NOT silently flip without verifying.

    **Edit 2: ROADMAP.md.**

    Two surgical edits in this file:

    (a) Phase 5 list item (currently line 25):
    ```
    - [ ] **Phase 5: Ops, Lang, Polish, Public Release** - Full lang packs, README + runbook, multi-site verification, public Composer publish, make all green
    ```
    →
    ```
    - [x] **Phase 5: Ops, Lang, Polish, Public Release** - Full lang packs, README + runbook, multi-site verification (UAT-CHECKLIST docs; execution post-plan per D-15), public Composer publish (docs; gh repo create operator-action per D-05), make all green incl coverage gate
    ```

    (b) Phase 5 Plans list under "### Phase 5: ..." (currently line 131):
    ```
    **Plans**: TBD
    ```
    →
    ```
    **Plans**: 6 plans
      - [x] 05-01-PLAN.md - Lang populate {lv,no,ru} + LangCompletenessTest (OPS-04)
      - [x] 05-02-PLAN.md - README operator runbook sections 1-11 (OPS-01)
      - [x] 05-03-PLAN.md - PROJECT.md update D11-D15 + 56 reqs Validated (OPS-02)
      - [x] 05-04-PLAN.md - Coverage gate Makefile + phpunit.xml <source> (OPS-05)
      - [x] 05-05-PLAN.md - README ## Publishing + ## Verification + UAT-CHECKLIST.md (OPS-03 + OPS-06)
      - [x] 05-06-PLAN.md - Final QA gate + milestone v1.0 closure (this plan)
    ```

    (c) Progress table row (currently line 144):
    ```
    | 5. Ops, Lang, Polish, Public Release | 0/TBD | Not started | - |
    ```
    →
    ```
    | 5. Ops, Lang, Polish, Public Release | 6/6 | Complete | 2026-04-30 |
    ```

    **Edit 3: STATE.md.**

    Read STATE.md first (it may not exist — if absent, create it; if exists, append a milestone closure entry near the top following its existing structure). Minimum content for the milestone closure:

    ```markdown
    ## Position

    Milestone v1.0 COMPLETE (2026-04-30).

    All 5 phases shipped. 56 v1 requirements validated. `make all` green including coverage gate. phpstan-baseline.neon unchanged from Phase 1 close.

    **Cumulative v1.0 stats (measured 2026-04-30):**
    - Phases: 5 (1, 2, 3, 4, 5)
    - Plans: 37 (8 + 7 + 8 + 8 + 6)
    - Closed REQ-IDs: 56
    - Test files: <measured>, total tests <measured>, total assertions <measured>
    - Production LoC: <measured>
    - Coverage: <measured%> (gate at --min=<N>)
    - phpstan-baseline.neon SHA: 4b3227fab5b697264e8532b59f5cdd96c86a0ff1fa484cc1a869af36ae91530a (unchanged since Phase 1)

    **Operator-action follow-ups (post-plan, per D-05 + D-15):**
    - Run `gh repo create logingrupa/oc-goodsreceived-plugin --public --source=. --remote=origin --push`
    - `git tag v1.0.0 && git push --tags`
    - Optional: submit to Packagist
    - Execute multi-site UAT per `.planning/UAT-CHECKLIST.md` on .no/.lv/.lt staging

    **Next**: v2 backlog (deferred items in REQUIREMENTS.md ## v2 Requirements — V2-OP-01..03, V2-DIFF-01..05, V2-OPS-01..02).
    ```

    Replace `<measured>` placeholders with the actual numbers from Task 1 Step 3.

    Commit message: `docs(05-06): mark milestone v1.0 complete; close 6 OPS-* reqs; finalize roadmap`
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/goodsreceivedshopaholic && grep -c 'OPS-0.*Closed.*2026-04-30' .planning/REQUIREMENTS.md && grep -F '- [x] **Phase 5:' .planning/ROADMAP.md && grep -F '6/6 | Complete | 2026-04-30' .planning/ROADMAP.md && grep -F 'milestone v1.0' .planning/STATE.md && ! grep -F 'Plans**: TBD' .planning/ROADMAP.md</automated>
  </verify>
  <done>
    REQUIREMENTS.md ## Traceability shows 6 OPS-* rows as Closed (2026-04-30) with plan references. v1 Requirements > Phase 5 section has [x] checkmarks on all 6 OPS-* bullets. ROADMAP.md Phase 5 list item ticked, Plans list populated with all 6 plan filenames, Progress table shows 6/6 Complete 2026-04-30. STATE.md reflects milestone v1.0 closure with measured cumulative stats.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Milestone v1.0 closure complete:
    - `make all` exits 0 (full pipeline incl coverage gate)
    - phpstan-baseline.neon SHA unchanged: 4b3227fab5b697264e8532b59f5cdd96c86a0ff1fa484cc1a869af36ae91530a
    - Test count: <measured at execution>
    - Coverage: <measured>% at gate --min=<N>
    - REQUIREMENTS.md: all 6 OPS-* flipped to Closed (2026-04-30) with plan refs
    - ROADMAP.md: Phase 5 row complete; 6/6 plans listed
    - STATE.md: milestone v1.0 closure marker + cumulative stats
    - Operator-action follow-ups documented for post-plan UAT + repo publish
  </what-built>
  <how-to-verify>
    1. Run `make all` again — confirm exit 0
    2. `grep -c 'OPS-0.*Closed' .planning/REQUIREMENTS.md` — should be 6
    3. Open `.planning/ROADMAP.md` — Phase 5 ticked, all 6 plans listed, Progress table 6/6
    4. Open `.planning/STATE.md` — milestone v1.0 closure block present with measured numbers
    5. Confirm by typing "approved" — milestone v1.0 is sealed; next conversation can start v2 backlog work
    6. If you see anything wrong (a number that doesn't match, a missing checkmark), describe it and the executor will fix
  </how-to-verify>
  <resume-signal>Type "approved" to seal milestone v1.0; or describe what needs fixing</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Engineer ↔ planning artifacts | Internal docs; no code execution. Used by next conversation's planner to know where to start. |
| `make all` ↔ CI / production gate | The gate decides what ships. False-green here means a broken plugin reaches operators. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Tampering | A failing test silently masked by `\|\| true` or skipped via `--filter` to make `make all` exit 0 | mitigate | Plan action explicitly forbids lowering coverage threshold or skipping sub-targets to mask failures; checkpoint requires operator review of measured numbers. |
| T-05-06-02 | Spoofing | REQUIREMENTS.md flipped Pending → Closed without the work actually being done | mitigate | Each Closed row carries a plan reference (05-NN); plan SUMMARYs are the audit trail; checkpoint requires operator sign-off. |
| T-05-06-03 | Repudiation | Future engineer claims milestone v1.0 was never actually closed | mitigate | Triple-recorded in REQUIREMENTS.md + ROADMAP.md + STATE.md, all with the same date 2026-04-30; checkpoint sign-off forms the final audit step. |
| T-05-06-04 | Information Disclosure | STATE.md cumulative stats accidentally leak proprietary line counts to PUBLIC repo (.planning/ is in repo) | accept | LoC counts are public-domain engineering metadata, not IP; the plugin is going PUBLIC anyway per D-11. |
</threat_model>

<verification>
- `make all` exits 0 in full pipeline run (pint-test + lint-settings-accessor + analyse + phpmd + test + coverage)
- phpstan-baseline.neon SHA matches Phase 4 close: `4b3227fab5b697264e8532b59f5cdd96c86a0ff1fa484cc1a869af36ae91530a`
- All 6 OPS-* REQ-IDs flipped to Closed in REQUIREMENTS.md ## Traceability
- ROADMAP.md Phase 5 row complete; 6/6 plans listed; Progress table updated
- STATE.md has milestone v1.0 closure block with measured numbers
- Operator approves at checkpoint
- No silent gating: all numbers in summaries are MEASURED, not estimated
</verification>

<success_criteria>
- OPS-05 fully closed: `make all` green incl coverage
- OPS-06 documented closure: UAT-CHECKLIST.md ships, executor confirms (actual UAT execution is post-plan operator action per D-15)
- Milestone v1.0 sealed across REQUIREMENTS.md + ROADMAP.md + STATE.md
- Operator has clean handoff to either:
  (a) Run the post-plan operator actions (gh repo create + UAT)
  (b) Start the next conversation on v2 backlog
- phpstan-baseline.neon SHA unchanged at `4b3227fa…91530a`
</success_criteria>

<output>
After completion, create `.planning/phases/05-ops-lang-polish-public-release/05-06-SUMMARY.md` summarizing:
- Final `make all` exit code + duration
- Test count delta (Phase 4 close 232 → milestone close N)
- Assertion delta (1037 → N)
- Coverage % measured + threshold pinned
- phpstan-baseline.neon SHA confirmation (unchanged)
- REQUIREMENTS.md changes (which OPS-* rows flipped, any QA-* rows handled)
- ROADMAP.md changes (Phase 5 row + Plans list + Progress table)
- STATE.md milestone closure block content
- Cumulative milestone v1.0 stats (phases / plans / reqs / LoC / tests)
- Operator-action follow-ups still outstanding (gh repo create, git tag v1.0.0, UAT execution)
- Operator's checkpoint decision

Also append a brief milestone-level note (could be embedded in 05-06-SUMMARY or a separate file) listing the 5 most important learnings / patterns established during v1 build (cross-phase) — this is engineering discipline, helps the v2 planner avoid repeating mistakes.
</output>
