title: 'logingrupa.goodsreceivedshopaholic::lang.controller.list_title'
modelClass: Logingrupa\GoodsReceivedShopaholic\Models\Invoice
list: $/logingrupa/goodsreceivedshopaholic/models/invoice/columns.yaml
recordUrl: 'logingrupa/goodsreceivedshopaholic/invoices/update/:id'
noRecordsMessage: 'backend::lang.list.no_records'
defaultSort:
    column: applied_at
    direction: desc
showSetup: true
showCheckboxes: true
toolbar:
    buttons: list_toolbar
    search:
        prompt: 'backend::lang.list.search_prompt'
filter:
    scopes:
        status:
            label: 'logingrupa.goodsreceivedshopaholic::lang.column.status'
            type: group
            conditions: 'status in (:filtered)'
            options:
                parsed: 'Parsed'
                applied: 'Applied'
# country_code filter dropped — each site (.no/.lv/.lt) has own DB so
# every row already has the same country_code. Column kept on the model
# for audit trail (proves file matched the distributor namespace at parse
# time) but filtering it on a single-country DB is pointless.
